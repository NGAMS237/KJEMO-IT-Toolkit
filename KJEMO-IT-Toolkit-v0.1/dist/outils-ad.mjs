/**
 * outils-ad.mjs — LOT 3 · KJEMO IT Toolkit
 * ----------------------------------------
 * Catalogue Active Directory avancé : structure et OU, utilisateurs et groupes,
 * cycle de vie des comptes, maintenance des objets, santé du domaine.
 *
 * Dépendances : noyau.mjs, validateurs.mjs, rapport-ps.mjs et les éléments
 * communs d'outils-serveur.mjs. Aucun import circulaire : generators.mjs
 * importe ce module, jamais l'inverse.
 *
 * Quatre règles gouvernent ce catalogue :
 *
 *  1. DIAGNOSTIC PAR DÉFAUT. Toute opération modifiante commence en mode
 *     Diagnostic et n'écrit qu'après un choix explicite.
 *
 *  2. AUCUN SECRET DANS LE NAVIGATEUR. Aucun champ de mot de passe, aucun
 *     identifiant dans un script, une URL, un presse-papiers ou un rapport.
 *     Quand un mot de passe est nécessaire, le script le demande localement
 *     avec Read-Host -AsSecureString ou Get-Credential, au moment de son
 *     exécution.
 *
 *  3. AUCUNE SUPPRESSION AUTOMATIQUE. Un utilisateur, un groupe, une OU ou un
 *     ordinateur ne sont jamais supprimés par un script de ce lot. Désactiver,
 *     déplacer en quarantaine, exporter : oui. Supprimer : c'est une décision
 *     humaine, documentée en cas exceptionnel.
 *
 *  4. L'ANNUAIRE EST UNE BASE DE DONNÉES PARTAGÉE. Chaque écriture est précédée
 *     d'un constat, rendue idempotente, et suivie d'une relecture.
 */

import { psB64, assertValid } from './noyau.mjs';
import {
  validateIPv4,
  validateShareName,
  validateSamAccountName,
  validateGroupName,
  validateOuName,
  validerFqdn,
  validerNomHote,
  validerDn,
  validerCheminOu,
  validerListeOu,
  validerUpn,
  validerPorteeGroupe,
  validerCategorieGroupe,
  validerCheminUnc,
  validerLettreLecteur,
  validerNomOrdinateur,
  validerJours,
  validerColonnesCsv,
  validerCheminWindowsLocal,
  validerFormatRapport,
  validerModeExecution,
  validerChoix,
  validerEntier,
  escapeLdapRdn,
  domainToDn,
  cheminOuVersDn,
  PORTEES_GROUPE,
  CATEGORIES_GROUPE,
} from './validateurs.mjs';
import { blocModeDiagnostic } from './rapport-ps.mjs';
import { champMode, champFormat, verifier, assembler, PREREQS_SERVEUR } from './outils-serveur.mjs';

// ---------------------------------------------------------------------------
// Éléments communs au catalogue Active Directory
// ---------------------------------------------------------------------------

/**
 * Systèmes visés. Le module ActiveDirectory est la dépendance réelle : il vient
 * du rôle AD DS sur un contrôleur, ou des outils d'administration à distance
 * sur un poste de gestion.
 */
export const OS_AD = [
  'Windows Server 2019 — module ActiveDirectory intégré au rôle AD DS',
  'Windows Server 2022 — module ActiveDirectory intégré au rôle AD DS',
  'Windows Server 2025 — cmdlets documentées identiques, non éprouvées en laboratoire par ce projet',
  'Windows 10 et 11 avec les outils d’administration RSAT-AD-PowerShell, pour l’administration à distance',
  'Windows PowerShell 5.1 — cible de référence',
  'PowerShell 7 — le module ActiveDirectory s’y importe avec -UseWindowsPowerShell',
];

/** Prérequis communs à tout outil qui interroge ou modifie l'annuaire. */
export const PREREQS_AD = PREREQS_SERVEUR.concat([
  'Module ActiveDirectory disponible : rôle AD DS installé, ou outils d’administration RSAT-AD-PowerShell.',
  'Compte disposant des droits sur la partie de l’annuaire visée. Les droits nécessaires sont précisés par outil.',
  'Aucun mot de passe n’est saisi dans le site : quand il en faut un, le script le demande localement à l’exécution.',
]);

/** Erreurs fréquentes communes au module ActiveDirectory. */
export const ERREURS_AD = [
  {
    message: 'Le terme « Get-ADUser » n’est pas reconnu comme nom d’applet de commande',
    code: 'CommandNotFoundException',
    cause: 'Le module ActiveDirectory n’est pas présent sur la machine qui exécute le script.',
    fix: 'Installer les outils d’administration, puis rouvrir la console.',
    command: 'Get-WindowsFeature RSAT-AD-PowerShell | Format-Table Name,InstallState',
  },
  {
    message: 'Unable to contact the server. This may be because this server does not exist, it is currently down, or it does not have the Active Directory Web Services running.',
    code: 'ADServerDownException',
    cause: 'Le service Active Directory Web Services est arrêté sur le contrôleur, ou le contrôleur est injoignable.',
    fix: 'Vérifier le service ADWS sur le contrôleur, et la résolution DNS du domaine depuis cette machine.',
    command: 'Get-Service ADWS ; Resolve-DnsName -Name <domaine> -Type SRV',
  },
  {
    message: 'Access is denied',
    code: '0x5',
    cause: 'Le compte utilisé n’a pas les droits sur l’objet ou l’unité d’organisation visée.',
    fix: 'Utiliser un compte délégué sur cette OU, ou vérifier la délégation avec l’outil d’audit de délégation.',
  },
];

/** Champ « domaine » commun, avec l'exemple de laboratoire du projet. */
export function champDomaine(aide = 'Domaine Active Directory visé. Le nom distinctif en est déduit automatiquement.') {
  return { id: 'adDomain', label: 'Domaine Active Directory', default: 'hopitalbn.lan', help: aide };
}

/**
 * Préambule PowerShell commun : import du module ActiveDirectory, avec un
 * message clair s'il manque, et relevé du contexte de domaine.
 */
export function blocModuleAd() {
  return [
    '# --- Module ActiveDirectory ------------------------------------------------',
    "# Le module vient du role AD DS, ou des outils d'administration RSAT.",
    '$moduleOk = $false',
    'try {',
    '  Import-Module ActiveDirectory -ErrorAction Stop',
    '  $moduleOk = $true',
    "  [void](Add-KjemoResultat -Categorie 'Prerequis' -Controle 'Module ActiveDirectory' -Etat 'OK' -Valeur 'charge')",
    '} catch {',
    "  [void](Add-KjemoResultat -Categorie 'Prerequis' -Controle 'Module ActiveDirectory' -Etat 'PROBLEME' -Valeur $_.Exception.Message -Commentaire 'Installe RSAT-AD-PowerShell, ou execute depuis un controleur de domaine.')",
    '}',
    '',
    '$domaineObj = $null',
    'if ($moduleOk) {',
    '  try { $domaineObj = Get-ADDomain -ErrorAction Stop } catch { }',
    '}',
    'if ($domaineObj) {',
    '  [void](Add-KjemoResultat -Categorie \'Domaine\' -Controle \'Domaine courant\' -Etat \'INFO\' -Valeur ("$($domaineObj.DNSRoot) — $($domaineObj.DistinguishedName)"))',
    '} else {',
    "  [void](Add-KjemoResultat -Categorie 'Domaine' -Controle 'Domaine courant' -Etat 'ATTENTION' -Valeur 'non determine' -Commentaire 'Machine hors domaine, ou controleur injoignable.')",
    '}',
  ].join('\n');
}

/**
 * Bloc de rappel : ce lot ne supprime jamais un objet d'annuaire.
 * Affiché en fin de script des outils modifiants, pour que la personne qui lit
 * le script sache où s'arrête ce qu'il fait.
 */
export function blocAucuneSuppression(objet = 'objet') {
  return [
    '',
    "Write-Host ''",
    `Write-Host '--- Portee de ce script ---'`,
    `Write-Host 'Aucun ${objet} n''est supprime par ce script. La suppression d''un objet'`,
    "Write-Host 'Active Directory est une decision humaine : elle se fait a la main, apres'",
    "Write-Host 'sauvegarde, et en connaissance des dependances.'",
  ].join('\n');
}

// ---------------------------------------------------------------------------
// A. STRUCTURE ET OU
// ---------------------------------------------------------------------------

/**
 * 1. ad-ou-hierarchy — créer une arborescence d'OU d'un seul tenant.
 *
 * Le piège n'est pas la commande mais l'ordre : New-ADOrganizationalUnit échoue
 * si le parent n'existe pas encore. Le formulaire trie donc les OU par
 * profondeur, et ajoute les niveaux intermédiaires oubliés — écrire
 * « Medecin/Specialiste » sans « Medecin » est une omission, pas un choix.
 */
export const outilOuHierarchie = {
  id: 'ad-ou-hierarchy',
  icon: '\u25eb',
  category: 'Active Directory',
  subcategory: 'Structure et OU',
  title: 'Créer une hiérarchie d\u2019unités d\u2019organisation',
  risk: 'caution',
  summary: 'Trie les OU parents avant les enfants, complète les niveaux manquants, ignore celles qui existent et n\u2019écrase jamais rien.',
  fields: [
    champDomaine(),
    {
      id: 'ouList', label: 'Liste des OU, une par ligne', type: 'textarea',
      default: 'Médecin\nMédecin/Généraliste\nMédecin/Spécialiste\nInfirmière\nInfirmière/Principale\nInfirmière/Auxiliaire\nPatient\nAdministration',
      help: 'Chemin parent/enfant séparé par des barres obliques. Les lignes commençant par # sont ignorées. Accents et apostrophes acceptés.',
    },
    {
      id: 'ouProtect', label: 'Protection contre la suppression accidentelle', type: 'select', default: 'Oui',
      options: [['Oui', 'Oui — recommandé'], ['Non', 'Non']],
      help: 'Coche la case « Protéger contre la suppression accidentelle » de chaque OU créée.',
    },
    champMode(),
  ],
  validate(v) {
    const errors = {};
    verifier(errors, 'adDomain', validerFqdn(v.adDomain));
    verifier(errors, 'ouList', validerListeOu(v.ouList));
    verifier(errors, 'ouProtect', validerChoix(v.ouProtect, ['Oui', 'Non'], 'La protection'));
    verifier(errors, 'mode', validerModeExecution(v.mode));
    return errors;
  },
  generate(v) {
    assertValid(this, v);
    const domaine = validerFqdn(v.adDomain).value;
    const ous = validerListeOu(v.ouList).value;
    const proteger = v.ouProtect === 'Oui';

    // Chaque OU est décrite par son nom, le DN de son parent et son DN complet.
    // Tout est calculé ici, en JavaScript, avec l'échappement RFC 4514 : le
    // script n'a plus qu'à exécuter, ce qui le rend lisible et vérifiable.
    const lignesTable = ous.map((ou) => {
      const dnParent = ou.parent ? cheminOuVersDn(ou.parent, domaine) : domainToDn(domaine);
      const dn = cheminOuVersDn(ou.segments, domaine);
      return '  [pscustomobject]@{ Chemin = ' + psB64(ou.chemin)
        + '; Nom = ' + psB64(ou.nom)
        + '; Parent = ' + psB64(dnParent)
        + '; Dn = ' + psB64(dn)
        + '; Implicite = $' + (ou.implicite ? 'true' : 'false') + ' }';
    });

    const corps = [
      `$Mode = ${psB64(v.mode)}`,
      `$Domaine = ${psB64(domaine)}`,
      `$Proteger = $${proteger ? 'true' : 'false'}`,
      "$KjemoFormat = 'Console'",
      "",
      '# Les OU sont deja triees : parents d\'abord, enfants ensuite. Les noms',
      '# passent par un encodage Base64 UTF-8, ce qui preserve exactement les',
      '# accents et les apostrophes, et les DN sont echappes selon RFC 4514.',
      '$OUs = @(',
      lignesTable.join(',\n'),
      ')',
      "",
      blocModuleAd(),
      "",
      '# --- Etat initial : quelles OU existent deja ? ----------------------------',
      '$aCreer = New-Object System.Collections.ArrayList',
      '$existantes = New-Object System.Collections.ArrayList',
      '$refusees = New-Object System.Collections.ArrayList',
      "",
      'foreach ($ou in $OUs) {',
      '  if (-not $moduleOk) { [void]$refusees.Add($ou); continue }',
      '  $deja = $null',
      '  try { $deja = Get-ADOrganizationalUnit -Identity $ou.Dn -ErrorAction SilentlyContinue } catch { }',
      '  if ($deja) {',
      '    [void]$existantes.Add($ou)',
      "    [void](Add-KjemoResultat -Categorie 'OU existantes' -Controle $ou.Chemin -Etat 'OK' -Valeur $ou.Dn -Commentaire 'Deja presente : ni recreee, ni modifiee.')",
      '    continue',
      '  }',
      '  # Le parent doit exister, ou etre cree plus tot dans cette meme execution.',
      '  $parentOk = $false',
      '  try {',
      '    $parentObj = Get-ADObject -Identity $ou.Parent -ErrorAction SilentlyContinue',
      '    if ($parentObj) { $parentOk = $true }',
      '  } catch { }',
      '  if (-not $parentOk) {',
      '    $creePlusTot = $aCreer | Where-Object { $_.Dn -eq $ou.Parent }',
      '    if ($creePlusTot) { $parentOk = $true }',
      '  }',
      '  if (-not $parentOk) {',
      '    [void]$refusees.Add($ou)',
      "    [void](Add-KjemoResultat -Categorie 'OU refusees' -Controle $ou.Chemin -Etat 'PROBLEME' -Valeur $ou.Parent -Commentaire \"Parent introuvable : l''OU ne peut pas etre creee a cet emplacement.\")",
      '    continue',
      '  }',
      '  [void]$aCreer.Add($ou)',
      "  $note = ''",
      "  if ($ou.Implicite) { $note = 'Niveau intermediaire ajoute automatiquement : il manquait dans la liste.' }",
      "  [void](Add-KjemoResultat -Categorie 'OU a creer' -Controle $ou.Chemin -Etat 'INFO' -Valeur $ou.Dn -Commentaire $note)",
      '}',
      "",
      '[void](Add-KjemoResultat -Categorie \'Resume\' -Controle \'Inventaire\' -Etat \'INFO\' -Valeur ("a creer : $($aCreer.Count) ; existantes : $($existantes.Count) ; refusees : $($refusees.Count)"))',
      "",
      '# --- Creation, uniquement en mode Appliquer -------------------------------',
      "if ($Mode -eq 'Appliquer' -and $moduleOk) {",
      '  foreach ($ou in $aCreer) {',
      '    try {',
      '      New-ADOrganizationalUnit -Name $ou.Nom -Path $ou.Parent -ProtectedFromAccidentalDeletion $Proteger -ErrorAction Stop',
      "      [void](Add-KjemoResultat -Categorie 'Action' -Controle $ou.Chemin -Etat 'OK' -Valeur 'creee')",
      '    } catch {',
      "      [void](Add-KjemoResultat -Categorie 'Action' -Controle $ou.Chemin -Etat 'PROBLEME' -Valeur $_.Exception.Message -Commentaire 'Creation refusee par l''annuaire.')",
      '    }',
      '  }',
      "",
      '  # Relecture : ce qui compte, c\'est l\'arborescence reelle apres coup.',
      "  Write-Host ''",
      "  Write-Host '--- Arborescence apres creation ---'",
      '  foreach ($ou in $OUs) {',
      '    $verif = $null',
      '    try { $verif = Get-ADOrganizationalUnit -Identity $ou.Dn -Properties ProtectedFromAccidentalDeletion -ErrorAction SilentlyContinue } catch { }',
      '    if ($verif) {',
      '      [void](Add-KjemoResultat -Categorie \'Verification\' -Controle $ou.Chemin -Etat \'OK\' -Valeur ("presente ; protegee : $($verif.ProtectedFromAccidentalDeletion)"))',
      '    } else {',
      "      [void](Add-KjemoResultat -Categorie 'Verification' -Controle $ou.Chemin -Etat 'PROBLEME' -Valeur 'absente apres execution')",
      '    }',
      '  }',
      '} else {',
      "  Write-Host ''",
      "  Write-Host '--- Simulation (-WhatIf) ---'",
      '  foreach ($ou in $aCreer) {',
      '    New-ADOrganizationalUnit -Name $ou.Nom -Path $ou.Parent -ProtectedFromAccidentalDeletion $Proteger -WhatIf',
      '  }',
      '}',
      blocModeDiagnostic(),
      blocAucuneSuppression('OU'),
    ];

    return assembler({
      titre: 'Creer une hierarchie d\'unites d\'organisation',
      outil: 'ad-ou-hierarchy',
      diagnostic: v.mode !== 'Appliquer',
      admin: true,
      parametres: [
        ['Domaine', psB64(domaine)],
        ['NombreOU', String(ous.length)],
        ['Protection', psB64(v.ouProtect)],
        ['Mode', psB64(v.mode)],
      ],
      corps,
      prefixeFichier: 'kjemo-ad-ou',
      formats: 'Console',
    });
  },
  gui: [
    'Utilisateurs et ordinateurs Active Directory (dsa.msc).',
    'Clic droit sur le domaine ou sur une OU parente > Nouveau > Unité d\u2019organisation.',
    'Saisir le nom, laisser cochée « Protéger le conteneur contre une suppression accidentelle ».',
    'Répéter parent par parent : la console impose le même ordre que le script.',
    'Affichage > Fonctionnalités avancées pour voir les objets système et les onglets Sécurité.',
  ],
  keywords: [
    'ou', 'unite d organisation', 'arborescence', 'hierarchie', 'structure ad',
    'new-adorganizationalunit', 'creer des ou', 'dsa.msc', 'organisation du domaine',
  ],
  requiresAdmin: true,
  os: OS_AD,
  prereqs: PREREQS_AD.concat([
    'Droit de créer des unités d\u2019organisation dans le domaine ou dans l\u2019OU parente.',
    'Plan de nommage arrêté : renommer une OU après coup casse les chemins écrits ailleurs.',
  ]),
  commonErrors: ERREURS_AD.concat([
    {
      message: 'New-ADOrganizationalUnit : The object already exists',
      code: 'ADException',
      cause: 'Une OU du même nom existe déjà au même emplacement.',
      fix: 'Le script la détecte avant d\u2019agir et la laisse intacte : rien n\u2019est écrasé.',
    },
    {
      message: 'New-ADOrganizationalUnit : Directory object not found',
      cause: 'L\u2019OU parente n\u2019existe pas encore : c\u2019est ce qui arrive quand on crée les enfants avant les parents.',
      fix: 'Le tri par profondeur du formulaire élimine ce cas. Si l\u2019erreur apparaît, le parent a été supprimé entre-temps.',
    },
    {
      message: 'L\u2019OU ne peut pas être supprimée ensuite',
      cause: 'La protection contre la suppression accidentelle est active — c\u2019est voulu.',
      fix: 'Décocher la protection dans dsa.msc (Fonctionnalités avancées > onglet Objet) avant toute suppression réfléchie.',
    },
  ]),
  reversible: true,
  verifyAfter: [
    'Get-ADOrganizationalUnit -Filter * affiche les OU créées avec leur DN.',
    'Dans dsa.msc, l\u2019arborescence correspond au plan saisi.',
    'La propriété ProtectedFromAccidentalDeletion vaut True si la protection a été demandée.',
  ],
  rollback: {
    summary: 'Une OU créée par erreur et encore vide se supprime, à la main, après avoir retiré sa protection. Une OU qui contient des objets ne se supprime pas : on déplace d\u2019abord son contenu.',
    diagnostic: '# Constater ce qui existe, et ce que chaque OU contient.\nGet-ADOrganizationalUnit -Filter * -Properties ProtectedFromAccidentalDeletion | Format-Table Name,DistinguishedName,ProtectedFromAccidentalDeletion\nGet-ADObject -SearchBase \'<dn-de-l-ou>\' -Filter * -SearchScope OneLevel | Format-Table Name,ObjectClass',
    command: '# Retirer la protection, puis supprimer une OU VIDE creee par erreur.\n# Les deux etapes sont volontairement separees : la premiere est anodine,\n# la seconde ne l\'est pas. -Confirm est explicite.\nSet-ADOrganizationalUnit -Identity \'<dn-de-l-ou>\' -ProtectedFromAccidentalDeletion $false\nRemove-ADOrganizationalUnit -Identity \'<dn-de-l-ou>\' -Confirm',
    exceptional: '# AVERTISSEMENT CRITIQUE — suppression d\'une OU qui contient des objets.\n#\n# Supprimer une OU peuplee supprime tout ce qu\'elle contient : comptes,\n# groupes, ordinateurs, et les OU filles. Les objets supprimes ne reviennent\n# que par la corbeille Active Directory, si elle est activee, ou par une\n# restauration d\'annuaire. Cette operation n\'est PAS automatisee ici.\n#\n# Avant d\'y penser :\n#   1. Exporter le contenu de l\'OU (Get-ADObject -SearchBase ... | Export-Csv).\n#   2. Deplacer ce qui doit etre conserve vers une autre OU.\n#   3. Verifier qu\'aucune GPO n\'est liee a cette OU.\n#   4. Verifier que la corbeille Active Directory est activee.\n#\n# Reference officielle :\n# https://learn.microsoft.com/powershell/module/activedirectory/remove-adorganizationalunit\n#\n# Commande, a executer manuellement et en connaissance de cause :\n#   Remove-ADOrganizationalUnit -Identity \'<dn>\' -Recursive -Confirm',
    warning: 'La protection contre la suppression accidentelle est activée par défaut, et c\u2019est une bonne chose : elle a déjà évité bien des restaurations d\u2019annuaire.',
  },
  checks: [
    'Vérifier le plan d\u2019OU avant de l\u2019appliquer : la structure conditionne les délégations et les GPO à venir.',
    'Le mode Diagnostic dit exactement ce qui sera créé, ce qui existe déjà et ce qui est refusé.',
    'Les niveaux intermédiaires ajoutés automatiquement sont signalés comme tels dans le rapport.',
  ],
  source: 'https://learn.microsoft.com/powershell/module/activedirectory/new-adorganizationalunit',
  sources: [
    { label: 'New-ADOrganizationalUnit', url: 'https://learn.microsoft.com/powershell/module/activedirectory/new-adorganizationalunit' },
    { label: 'Get-ADOrganizationalUnit', url: 'https://learn.microsoft.com/powershell/module/activedirectory/get-adorganizationalunit' },
    { label: 'Set-ADOrganizationalUnit', url: 'https://learn.microsoft.com/powershell/module/activedirectory/set-adorganizationalunit' },
    { label: 'Remove-ADOrganizationalUnit', url: 'https://learn.microsoft.com/powershell/module/activedirectory/remove-adorganizationalunit' },
  ],
};

// ---------------------------------------------------------------------------
// B. UTILISATEURS ET GROUPES
// ---------------------------------------------------------------------------

/**
 * 2. ad-users-csv — importer des comptes depuis un fichier CSV.
 *
 * Le navigateur ne lit pas le disque : le formulaire décrit le fichier, et le
 * script le lit au moment de son exécution. Aucun mot de passe ne transite par
 * le site — le script demande une politique au lancement, et crée les comptes
 * désactivés si aucun mot de passe n'est fourni.
 */
export const outilUtilisateursCsv = {
  id: 'ad-users-csv',
  icon: '\u25a4',
  category: 'Active Directory',
  subcategory: 'Utilisateurs et groupes',
  title: 'Importer des utilisateurs depuis un CSV',
  risk: 'caution',
  summary: 'Vérifie le schéma, prévisualise les lignes, détecte les doublons de SamAccountName, UPN et courriel, puis crée les comptes sans jamais manipuler de mot de passe côté site.',
  fields: [
    champDomaine(),
    { id: 'csvPath', label: 'Chemin local du fichier CSV', default: 'C:\\Imports\\utilisateurs.csv', help: 'Chemin sur la machine qui exécutera le script. Le site ne lit pas le fichier.' },
    {
      id: 'csvColumns', label: 'Colonnes attendues', default: 'Prenom,Nom,SamAccountName,Upn,Courriel,CheminOu',
      help: 'Séparées par des virgules. SamAccountName et CheminOu sont obligatoires.',
    },
    { id: 'csvDelimiter', label: 'Séparateur', type: 'select', default: ';', options: [[';', 'Point-virgule (Excel français)'], [',', 'Virgule'], ['\t', 'Tabulation']] },
    { id: 'csvPreview', label: 'Lignes à prévisualiser', default: '5' },
    {
      id: 'csvPassword', label: 'Politique de mot de passe', type: 'select', default: 'Comptes désactivés',
      options: [
        ['Comptes désactivés', 'Créer les comptes désactivés, sans mot de passe'],
        ['Mot de passe temporaire', 'Demander un mot de passe temporaire à l\u2019exécution'],
      ],
      help: 'Dans les deux cas, aucun mot de passe n\u2019est saisi ici : le script le demande localement.',
    },
    champMode(),
  ],
  validate(v) {
    const errors = {};
    verifier(errors, 'adDomain', validerFqdn(v.adDomain));
    verifier(errors, 'csvPath', validerCheminWindowsLocal(v.csvPath));
    if (!/\.csv$/i.test(String(v.csvPath ?? '').trim()) && !errors.csvPath) {
      errors.csvPath = 'Le fichier doit porter l\u2019extension .csv.';
    }
    verifier(errors, 'csvColumns', validerColonnesCsv(v.csvColumns, { obligatoires: ['SamAccountName', 'CheminOu'] }));
    verifier(errors, 'csvDelimiter', validerChoix(v.csvDelimiter, [';', ',', '\t'], 'Le séparateur'));
    verifier(errors, 'csvPreview', validerEntier(v.csvPreview, 1, 50, 'Le nombre de lignes à prévisualiser'));
    verifier(errors, 'csvPassword', validerChoix(v.csvPassword, ['Comptes désactivés', 'Mot de passe temporaire'], 'La politique de mot de passe'));
    verifier(errors, 'mode', validerModeExecution(v.mode));
    return errors;
  },
  generate(v) {
    assertValid(this, v);
    const domaine = validerFqdn(v.adDomain).value;
    const colonnes = validerColonnesCsv(v.csvColumns, { obligatoires: ['SamAccountName', 'CheminOu'] }).value;
    const motDePasseDemande = v.csvPassword === 'Mot de passe temporaire';

    const corps = [
      `$Mode = ${psB64(v.mode)}`,
      `$Domaine = ${psB64(domaine)}`,
      `$DnDomaine = ${psB64(domainToDn(domaine))}`,
      `$Fichier = ${psB64(validerCheminWindowsLocal(v.csvPath).value)}`,
      `$Separateur = ${psB64(v.csvDelimiter)}`,
      `$Apercu = [int](${psB64(v.csvPreview)})`,
      `$ColonnesAttendues = @(${colonnes.map((c) => psB64(c)).join(', ')})`,
      `$PolitiqueMotDePasse = ${psB64(v.csvPassword)}`,
      "$KjemoFormat = 'Console'",
      "",
      blocModuleAd(),
      "",
      '# --- 1. Le fichier existe-t-il, et a-t-il les bonnes colonnes ? -----------',
      'if (-not (Test-Path -LiteralPath $Fichier)) {',
      "  [void](Add-KjemoResultat -Categorie 'Fichier' -Controle 'Existence' -Etat 'PROBLEME' -Valeur $Fichier -Commentaire 'Fichier introuvable sur cette machine.')",
      '  return',
      '}',
      "[void](Add-KjemoResultat -Categorie 'Fichier' -Controle 'Existence' -Etat 'OK' -Valeur $Fichier)",
      "",
      '$lignes = @()',
      'try {',
      '  $lignes = @(Import-Csv -LiteralPath $Fichier -Delimiter $Separateur -Encoding UTF8)',
      '} catch {',
      "  [void](Add-KjemoResultat -Categorie 'Fichier' -Controle 'Lecture' -Etat 'PROBLEME' -Valeur $_.Exception.Message -Commentaire 'Verifie le separateur et l''encodage du fichier.')",
      '  return',
      '}',
      'if (@($lignes).Count -eq 0) {',
      "  [void](Add-KjemoResultat -Categorie 'Fichier' -Controle 'Contenu' -Etat 'PROBLEME' -Valeur '0 ligne' -Commentaire 'Fichier vide, ou separateur incorrect : une seule colonne serait alors lue.')",
      '  return',
      '}',
      '[void](Add-KjemoResultat -Categorie \'Fichier\' -Controle \'Lignes lues\' -Etat \'OK\' -Valeur ("$(@($lignes).Count) ligne(s)"))',
      "",
      '$colonnesReelles = @($lignes[0].PSObject.Properties.Name)',
      '[void](Add-KjemoResultat -Categorie \'Schema\' -Controle \'Colonnes du fichier\' -Etat \'INFO\' -Valeur ($colonnesReelles -join \', \'))',
      '$manquantes = @($ColonnesAttendues | Where-Object { $colonnesReelles -notcontains $_ })',
      'if (@($manquantes).Count -gt 0) {',
      '  [void](Add-KjemoResultat -Categorie \'Schema\' -Controle \'Colonnes manquantes\' -Etat \'PROBLEME\' -Valeur ($manquantes -join \', \') -Commentaire "Le script ne peut pas creer de compte sans ces colonnes.")',
      '  return',
      '}',
      "[void](Add-KjemoResultat -Categorie 'Schema' -Controle 'Colonnes attendues' -Etat 'OK' -Valeur ($ColonnesAttendues -join ', '))",
      "",
      '# --- 2. Apercu des premieres lignes ---------------------------------------',
      "Write-Host ''",
      "Write-Host '--- Apercu du fichier ---'",
      '$lignes | Select-Object -First $Apercu | Format-Table -AutoSize',
      "",
      '# --- 3. Controles ligne par ligne -----------------------------------------',
      '$vusSam = @{}',
      '$vusUpn = @{}',
      '$vusMail = @{}',
      '$aCreer = New-Object System.Collections.ArrayList',
      '$ignores = New-Object System.Collections.ArrayList',
      '$erreurs = New-Object System.Collections.ArrayList',
      '$numero = 0',
      "",
      'foreach ($ligne in $lignes) {',
      '  $numero++',
      '  $sam = ("" + $ligne.SamAccountName).Trim()',
      '  $cheminOu = ("" + $ligne.CheminOu).Trim()',
      "  $upn = ''",
      '  if ($colonnesReelles -contains \'Upn\') { $upn = ("" + $ligne.Upn).Trim() }',
      "  $mail = ''",
      '  if ($colonnesReelles -contains \'Courriel\') { $mail = ("" + $ligne.Courriel).Trim() }',
      "",
      '  # 3a. Champs obligatoires',
      "  if ($sam -eq '' -or $cheminOu -eq '') {",
      '    [void]$erreurs.Add([pscustomobject]@{ Ligne = $numero; Sam = $sam; Raison = \'SamAccountName ou CheminOu vide\' })',
      '    [void](Add-KjemoResultat -Categorie \'Erreurs\' -Controle "Ligne $numero" -Etat \'PROBLEME\' -Valeur $sam -Commentaire \'SamAccountName ou CheminOu vide.\')',
      '    continue',
      '  }',
      '  # 3b. Longueur et caracteres interdits du SamAccountName',
      '  if ($sam.Length -gt 20) {',
      '    [void]$erreurs.Add([pscustomobject]@{ Ligne = $numero; Sam = $sam; Raison = \'SamAccountName de plus de 20 caracteres\' })',
      '    [void](Add-KjemoResultat -Categorie \'Erreurs\' -Controle "Ligne $numero" -Etat \'PROBLEME\' -Valeur $sam -Commentaire \'SamAccountName limite a 20 caracteres.\')',
      '    continue',
      '  }',
      '  # 3c. Doublons DANS le fichier',
      '  if ($vusSam.ContainsKey($sam.ToLower())) {',
      '    [void]$erreurs.Add([pscustomobject]@{ Ligne = $numero; Sam = $sam; Raison = "SamAccountName en double avec la ligne $($vusSam[$sam.ToLower()])" })',
      '    [void](Add-KjemoResultat -Categorie \'Erreurs\' -Controle "Ligne $numero" -Etat \'PROBLEME\' -Valeur $sam -Commentaire "Doublon dans le fichier, deja vu ligne $($vusSam[$sam.ToLower()]).")',
      '    continue',
      '  }',
      '  $vusSam[$sam.ToLower()] = $numero',
      "  if ($upn -ne '') {",
      '    if ($vusUpn.ContainsKey($upn.ToLower())) {',
      '      [void](Add-KjemoResultat -Categorie \'Erreurs\' -Controle "Ligne $numero" -Etat \'PROBLEME\' -Valeur $upn -Commentaire "UPN en double dans le fichier, deja vu ligne $($vusUpn[$upn.ToLower()]).")',
      '      [void]$erreurs.Add([pscustomobject]@{ Ligne = $numero; Sam = $sam; Raison = \'UPN en double dans le fichier\' })',
      '      continue',
      '    }',
      '    $vusUpn[$upn.ToLower()] = $numero',
      '  }',
      "  if ($mail -ne '') {",
      '    if ($vusMail.ContainsKey($mail.ToLower())) {',
      '      [void](Add-KjemoResultat -Categorie \'Erreurs\' -Controle "Ligne $numero" -Etat \'PROBLEME\' -Valeur $mail -Commentaire "Courriel en double dans le fichier, deja vu ligne $($vusMail[$mail.ToLower()]).")',
      '      [void]$erreurs.Add([pscustomobject]@{ Ligne = $numero; Sam = $sam; Raison = \'Courriel en double dans le fichier\' })',
      '      continue',
      '    }',
      '    $vusMail[$mail.ToLower()] = $numero',
      '  }',
      "",
      '  if (-not $moduleOk) { continue }',
      "",
      '  # 3d. Doublons DANS l\'annuaire',
      '  $existant = $null',
      '  try { $existant = Get-ADUser -Filter "SamAccountName -eq \'$sam\'" -ErrorAction SilentlyContinue } catch { }',
      '  if ($existant) {',
      '    [void]$ignores.Add([pscustomobject]@{ Ligne = $numero; Sam = $sam; Raison = \'Compte deja present dans l\'\'annuaire\' })',
      '    [void](Add-KjemoResultat -Categorie \'Ignores\' -Controle "Ligne $numero" -Etat \'OK\' -Valeur $sam -Commentaire "Compte deja present : ni recree, ni modifie.")',
      '    continue',
      '  }',
      "  if ($upn -ne '') {",
      '    $existantUpn = $null',
      '    try { $existantUpn = Get-ADUser -Filter "UserPrincipalName -eq \'$upn\'" -ErrorAction SilentlyContinue } catch { }',
      '    if ($existantUpn) {',
      '      [void]$erreurs.Add([pscustomobject]@{ Ligne = $numero; Sam = $sam; Raison = \'UPN deja utilise dans l\'\'annuaire\' })',
      '      [void](Add-KjemoResultat -Categorie \'Erreurs\' -Controle "Ligne $numero" -Etat \'PROBLEME\' -Valeur $upn -Commentaire "UPN deja porte par un autre compte : il doit etre unique dans la foret.")',
      '      continue',
      '    }',
      '  }',
      "",
      '  # 3e. L\'OU cible existe-t-elle ?',
      '  $segments = @($cheminOu.Split(\'/\') | Where-Object { $_ -ne \'\' })',
      '  [array]::Reverse($segments)',
      '  $dnOu = (($segments | ForEach-Object { "OU=" + ($_ -replace \'([,+"\\\\<>;=])\', \'\\$1\') }) -join \',\') + \',\' + $DnDomaine',
      '  $ouObj = $null',
      '  try { $ouObj = Get-ADOrganizationalUnit -Identity $dnOu -ErrorAction SilentlyContinue } catch { }',
      '  if (-not $ouObj) {',
      '    [void]$erreurs.Add([pscustomobject]@{ Ligne = $numero; Sam = $sam; Raison = "OU introuvable : $dnOu" })',
      "    [void](Add-KjemoResultat -Categorie 'Erreurs' -Controle \"Ligne $numero\" -Etat 'PROBLEME' -Valeur $dnOu -Commentaire \"OU introuvable : cree la hierarchie d''OU avant l''import.\")",
      '    continue',
      '  }',
      "",
      '  [void]$aCreer.Add([pscustomobject]@{',
      '    Ligne = $numero; Sam = $sam; Upn = $upn; Courriel = $mail; Dn = $dnOu',
      '    Prenom = ("" + $ligne.Prenom).Trim(); Nom = ("" + $ligne.Nom).Trim()',
      '  })',
      '  [void](Add-KjemoResultat -Categorie \'A creer\' -Controle "Ligne $numero" -Etat \'INFO\' -Valeur $sam -Commentaire $dnOu)',
      '}',
      "",
      '[void](Add-KjemoResultat -Categorie \'Resume\' -Controle \'Bilan des controles\' -Etat \'INFO\' -Valeur ("a creer : $($aCreer.Count) ; ignores : $($ignores.Count) ; erreurs : $($erreurs.Count)"))',
      "",
      '# --- 4. Creation, uniquement en mode Appliquer ----------------------------',
      "if ($Mode -eq 'Appliquer' -and $moduleOk -and $aCreer.Count -gt 0) {",
      "",
      '  # Le mot de passe n\'est jamais ecrit dans ce script : il est demande ici,',
      '  # localement, et reste un SecureString en memoire.',
      '  $motDePasse = $null',
      (motDePasseDemande
        ? "  Write-Host ''\n"
          + "  Write-Host 'Saisis le mot de passe temporaire commun aux comptes crees.'\n"
          + "  Write-Host 'Il n''est ni affiche, ni journalise, ni ecrit dans le rapport.'\n"
          + '  $motDePasse = Read-Host -Prompt \'Mot de passe temporaire\' -AsSecureString'
        : "  Write-Host ''\n"
          + "  Write-Host 'Politique retenue : les comptes sont crees DESACTIVES, sans mot de passe.'\n"
          + "  Write-Host 'Chaque compte devra recevoir un mot de passe puis etre active.'"),
      "",
      '  foreach ($compte in $aCreer) {',
      '    $parametres = @{',
      '      Name              = $compte.Sam',
      '      SamAccountName    = $compte.Sam',
      '      Path              = $compte.Dn',
      '      Enabled           = $false',
      '    }',
      "    if ($compte.Prenom -ne '') { $parametres['GivenName'] = $compte.Prenom }",
      "    if ($compte.Nom -ne '') { $parametres['Surname'] = $compte.Nom }",
      "    if ($compte.Upn -ne '') { $parametres['UserPrincipalName'] = $compte.Upn }",
      "    if ($compte.Courriel -ne '') { $parametres['EmailAddress'] = $compte.Courriel }",
      '    if ($null -ne $motDePasse) {',
      "      $parametres['AccountPassword'] = $motDePasse",
      "      $parametres['ChangePasswordAtLogon'] = $true",
      "      $parametres['Enabled'] = $true",
      '    }',
      '    try {',
      '      New-ADUser @parametres -ErrorAction Stop',
      "      $etatCompte = 'cree, desactive'",
      "      if ($null -ne $motDePasse) { $etatCompte = 'cree, actif, mot de passe a changer a la premiere ouverture' }",
      '      [void](Add-KjemoResultat -Categorie \'Action\' -Controle $compte.Sam -Etat \'OK\' -Valeur $etatCompte)',
      '    } catch {',
      '      [void](Add-KjemoResultat -Categorie \'Action\' -Controle $compte.Sam -Etat \'PROBLEME\' -Valeur $_.Exception.Message)',
      '      [void]$erreurs.Add([pscustomobject]@{ Ligne = $compte.Ligne; Sam = $compte.Sam; Raison = $_.Exception.Message })',
      '    }',
      '  }',
      "",
      '  # Relecture : les comptes existent-ils vraiment, et ou ?',
      "  Write-Host ''",
      "  Write-Host '--- Comptes apres import ---'",
      '  foreach ($compte in $aCreer) {',
      '    $verif = $null',
      '    try { $verif = Get-ADUser -Identity $compte.Sam -Properties Enabled,DistinguishedName -ErrorAction SilentlyContinue } catch { }',
      '    if ($verif) {',
      '      [void](Add-KjemoResultat -Categorie \'Verification\' -Controle $compte.Sam -Etat \'OK\' -Valeur ("actif : $($verif.Enabled) ; $($verif.DistinguishedName)"))',
      '    } else {',
      "      [void](Add-KjemoResultat -Categorie 'Verification' -Controle $compte.Sam -Etat 'PROBLEME' -Valeur 'compte absent apres import')",
      '    }',
      '  }',
      '} elseif ($Mode -ne \'Appliquer\') {',
      "  Write-Host ''",
      "  Write-Host '--- Simulation ---'",
      '  Write-Host "$($aCreer.Count) compte(s) seraient crees. Aucune ecriture n\'a eu lieu."',
      '}',
      "",
      '# --- 5. Journal par utilisateur --------------------------------------------',
      "$dossierJournal = Join-Path $env:USERPROFILE 'Desktop'",
      '$journal = Join-Path $dossierJournal ("kjemo-ad-import-" + (Get-Date -Format \'yyyyMMdd-HHmmss\') + ".csv")',
      '$toutes = @()',
      "$toutes += @($aCreer | ForEach-Object { [pscustomobject]@{ Ligne = $_.Ligne; Compte = $_.Sam; Statut = 'A creer ou cree'; Detail = $_.Dn } })",
      "$toutes += @($ignores | ForEach-Object { [pscustomobject]@{ Ligne = $_.Ligne; Compte = $_.Sam; Statut = 'Ignore'; Detail = $_.Raison } })",
      "$toutes += @($erreurs | ForEach-Object { [pscustomobject]@{ Ligne = $_.Ligne; Compte = $_.Sam; Statut = 'Erreur'; Detail = $_.Raison } })",
      'if (@($toutes).Count -gt 0) {',
      '  $toutes | Export-Csv -LiteralPath $journal -NoTypeInformation -Encoding UTF8',
      '  Write-Host "Journal par utilisateur : $journal"',
      '}',
      blocModeDiagnostic(),
      blocAucuneSuppression('compte'),
    ];

    return assembler({
      titre: 'Importer des utilisateurs Active Directory depuis un CSV',
      outil: 'ad-users-csv',
      diagnostic: v.mode !== 'Appliquer',
      admin: true,
      parametres: [
        ['Domaine', psB64(domaine)],
        ['Fichier', psB64(v.csvPath)],
        ['Politique', psB64(v.csvPassword)],
        ['Mode', psB64(v.mode)],
      ],
      corps,
      prefixeFichier: 'kjemo-ad-import',
      formats: 'Console',
    });
  },
  gui: [
    'Préparer le CSV dans Excel, puis l\u2019enregistrer en « CSV UTF-8 (délimité par des points-virgules) ».',
    'Vérifier les en-têtes : ils doivent correspondre exactement aux colonnes attendues.',
    'Pour quelques comptes seulement : dsa.msc > clic droit sur l\u2019OU > Nouveau > Utilisateur.',
    'Après import : dsa.msc > l\u2019OU concernée, vérifier que les comptes sont là et dans le bon état.',
  ],
  keywords: [
    'import csv', 'creer des utilisateurs', 'new-aduser', 'import-csv', 'comptes en masse',
    'doublon samaccountname', 'upn', 'import utilisateurs ad', 'rentree', 'liste d eleves',
  ],
  requiresAdmin: true,
  os: OS_AD,
  prereqs: PREREQS_AD.concat([
    'Droit de créer des comptes dans les OU visées.',
    'Fichier CSV présent sur la machine qui exécute le script, encodé en UTF-8.',
    'Les OU cibles doivent exister : créer d\u2019abord la hiérarchie avec l\u2019outil dédié.',
    'Aucun mot de passe dans le CSV : le script en demande un à l\u2019exécution, ou crée les comptes désactivés.',
  ]),
  commonErrors: ERREURS_AD.concat([
    {
      message: 'Le fichier est lu comme une seule colonne',
      cause: 'Le séparateur choisi ne correspond pas à celui du fichier : Excel en français enregistre avec des points-virgules.',
      fix: 'Changer le séparateur dans le formulaire, ou réenregistrer le fichier avec le bon délimiteur.',
    },
    {
      message: 'New-ADUser : The specified account already exists',
      cause: 'Un compte porte déjà ce SamAccountName.',
      fix: 'Le script détecte ce cas avant d\u2019agir et classe la ligne en « ignoré ».',
    },
    {
      message: 'Les accents apparaissent mal dans l\u2019annuaire',
      cause: 'Le CSV n\u2019est pas en UTF-8.',
      fix: 'Réenregistrer le fichier en « CSV UTF-8 » depuis Excel, puis relancer.',
    },
    {
      message: 'New-ADUser : The password does not meet the length, complexity, or history requirement',
      code: '0x52D',
      cause: 'Le mot de passe temporaire saisi ne respecte pas la politique du domaine.',
      fix: 'Saisir un mot de passe conforme, ou créer les comptes désactivés puis attribuer les mots de passe ensuite.',
    },
  ]),
  reversible: true,
  verifyAfter: [
    'Le journal CSV écrit sur le Bureau liste chaque ligne avec son statut : créé, ignoré ou en erreur.',
    'Get-ADUser -Filter * -SearchBase <OU> affiche les comptes dans la bonne OU.',
    'Les comptes créés sans mot de passe sont désactivés : Enabled vaut False.',
    'Aucun mot de passe n\u2019apparaît nulle part — ni dans le script, ni dans le journal.',
  ],
  rollback: {
    summary: 'Un compte créé par erreur se désactive immédiatement — geste réversible et sans perte. La suppression, elle, n\u2019est pas automatisée : un compte porte un SID dont dépendent des permissions ailleurs.',
    diagnostic: '# Lister ce que l\'import a cree, avant toute decision.\nGet-ADUser -Filter * -SearchBase \'<dn-de-l-ou>\' -Properties WhenCreated,Enabled | Sort-Object WhenCreated -Descending | Format-Table SamAccountName,Enabled,WhenCreated,DistinguishedName',
    command: '# Desactiver un compte cree par erreur : reversible, immediat, sans perte.\n# -Confirm est explicite : PowerShell demandera confirmation.\nDisable-ADAccount -Identity \'<samaccountname>\' -Confirm\n\n# Verifier ensuite l\'etat reel du compte.\nGet-ADUser -Identity \'<samaccountname>\' -Properties Enabled | Format-List SamAccountName,Enabled,DistinguishedName',
    exceptional: '# AVERTISSEMENT CRITIQUE — suppression d\'un compte utilisateur.\n#\n# Supprimer un compte detruit son SID. Les permissions accordees a ce SID sur\n# des partages, des boites aux lettres ou des applications ne se retrouvent pas\n# en recreant un compte du meme nom : ce serait un autre SID. Cette operation\n# n\'est PAS automatisee ici.\n#\n# Avant d\'y penser :\n#   1. Verifier que la corbeille Active Directory est activee.\n#   2. Exporter les attributs du compte et ses appartenances de groupes.\n#   3. Verifier ce qui depend de ce compte : partages, applications, services.\n#   4. Preferer la desactivation, qui repond a presque tous les besoins.\n#\n# Reference officielle :\n# https://learn.microsoft.com/powershell/module/activedirectory/remove-aduser\n#\n# Commande, a executer manuellement :\n#   Remove-ADUser -Identity \'<samaccountname>\' -Confirm',
    warning: 'Un import en masse se teste sur trois lignes avant de passer à trois cents. Le mode Diagnostic sert exactement à cela.',
  },
  checks: [
    'Relire l\u2019aperçu des premières lignes : c\u2019est là qu\u2019un mauvais séparateur se voit immédiatement.',
    'Vérifier que les OU cibles existent avant de lancer l\u2019import.',
    'Ne jamais mettre de mot de passe dans le CSV : le script n\u2019en lit aucun.',
    'Commencer par un fichier réduit, en mode Diagnostic.',
  ],
  source: 'https://learn.microsoft.com/powershell/module/activedirectory/new-aduser',
  sources: [
    { label: 'New-ADUser', url: 'https://learn.microsoft.com/powershell/module/activedirectory/new-aduser' },
    { label: 'Get-ADUser', url: 'https://learn.microsoft.com/powershell/module/activedirectory/get-aduser' },
    { label: 'Import-Csv', url: 'https://learn.microsoft.com/powershell/module/microsoft.powershell.utility/import-csv' },
    { label: 'Read-Host', url: 'https://learn.microsoft.com/powershell/module/microsoft.powershell.utility/read-host' },
    { label: 'Disable-ADAccount', url: 'https://learn.microsoft.com/powershell/module/activedirectory/disable-adaccount' },
  ],
};

/**
 * 3. ad-groups — créer des groupes, avec la portée qui convient.
 *
 * La portée n'est pas un détail : c'est elle qui décide si le groupe peut
 * contenir des comptes d'un autre domaine, et s'il peut recevoir des
 * permissions ailleurs dans la forêt. Le modèle AGDLP en dépend entièrement.
 */
export const outilGroupes = {
  id: 'ad-groups',
  icon: '\u25cb',
  category: 'Active Directory',
  subcategory: 'Utilisateurs et groupes',
  title: 'Créer et vérifier des groupes Active Directory',
  risk: 'caution',
  summary: 'Crée des groupes de sécurité ou de distribution avec la bonne portée, sans jamais écraser un groupe existant, et rappelle le modèle AGDLP.',
  fields: [
    champDomaine(),
    {
      id: 'grpList', label: 'Groupes à créer, un par ligne', type: 'textarea',
      default: 'GG-Medecins\nGG-Infirmieres\nGG-Administration\nDL-Dossiers-Patients-RW\nDL-Dossiers-Patients-R',
      help: 'Un nom par ligne. Convention utile : GG- pour les groupes globaux de personnes, DL- pour les groupes de domaine local porteurs de permissions.',
    },
    { id: 'grpOu', label: 'OU de destination', default: 'Administration', help: 'Chemin parent/enfant, relatif au domaine.' },
    {
      id: 'grpCategory', label: 'Catégorie', type: 'select', default: 'Security',
      options: [['Security', 'Sécurité — porte des permissions'], ['Distribution', 'Distribution — messagerie seulement']],
    },
    {
      id: 'grpScope', label: 'Portée', type: 'select', default: 'Global',
      options: [
        ['Global', 'Globale — regroupe des comptes du domaine'],
        ['DomainLocal', 'Domaine local — porte les permissions sur une ressource'],
        ['Universal', 'Universelle — traverse les domaines de la forêt'],
      ],
      help: 'AGDLP : comptes dans un groupe Global, permissions sur un groupe de Domaine local, le Global membre du Domaine local.',
    },
    { id: 'grpDescription', label: 'Description', default: 'Groupe créé par KJEMO IT Toolkit', help: 'Une description explicite évite les groupes orphelins dont personne ne sait à quoi ils servent.' },
    champMode(),
  ],
  validate(v) {
    const errors = {};
    verifier(errors, 'adDomain', validerFqdn(v.adDomain));

    const lignes = String(v.grpList ?? '').split(/\r?\n/).map((l) => l.trim()).filter((l) => l !== '');
    if (lignes.length === 0) errors.grpList = 'Indique au moins un groupe, un par ligne.';
    else if (lignes.length > 50) errors.grpList = 'Pas plus de 50 groupes par exécution.';
    else {
      for (const nom of lignes) {
        const r = validateGroupName(nom);
        if (!r.ok) { errors.grpList = `« ${nom} » : ${r.error}`; break; }
      }
      const vus = lignes.map((l) => l.toLowerCase());
      if (!errors.grpList && new Set(vus).size !== vus.length) {
        errors.grpList = 'La liste contient deux fois le même groupe.';
      }
    }

    verifier(errors, 'grpOu', validerCheminOu(v.grpOu));
    verifier(errors, 'grpCategory', validerCategorieGroupe(v.grpCategory));
    verifier(errors, 'grpScope', validerPorteeGroupe(v.grpScope));
    const desc = String(v.grpDescription ?? '');
    if (desc.length > 1024) errors.grpDescription = 'La description ne doit pas dépasser 1024 caractères.';
    verifier(errors, 'mode', validerModeExecution(v.mode));
    return errors;
  },
  generate(v) {
    assertValid(this, v);
    const domaine = validerFqdn(v.adDomain).value;
    const segments = validerCheminOu(v.grpOu).value;
    const dnOu = cheminOuVersDn(segments, domaine);
    const groupes = String(v.grpList).split(/\r?\n/).map((l) => l.trim()).filter((l) => l !== '');

    const corps = [
      `$Mode = ${psB64(v.mode)}`,
      `$Domaine = ${psB64(domaine)}`,
      `$DnOu = ${psB64(dnOu)}`,
      `$Categorie = ${psB64(v.grpCategory)}`,
      `$Portee = ${psB64(v.grpScope)}`,
      `$Description = ${psB64(v.grpDescription)}`,
      `$Groupes = @(${groupes.map((g) => psB64(g)).join(', ')})`,
      "$KjemoFormat = 'Console'",
      "",
      blocModuleAd(),
      "",
      '# --- 1. L\'OU de destination existe-t-elle ? -------------------------------',
      '$ouOk = $false',
      'if ($moduleOk) {',
      '  try {',
      '    $ouObj = Get-ADOrganizationalUnit -Identity $DnOu -ErrorAction SilentlyContinue',
      '    if ($ouObj) { $ouOk = $true }',
      '  } catch { }',
      '}',
      'if ($ouOk) {',
      "  [void](Add-KjemoResultat -Categorie 'Destination' -Controle 'OU cible' -Etat 'OK' -Valeur $DnOu)",
      '} else {',
      "  [void](Add-KjemoResultat -Categorie 'Destination' -Controle 'OU cible' -Etat 'PROBLEME' -Valeur $DnOu -Commentaire 'OU introuvable : cree-la avant, avec l''outil de hierarchie d''OU.')",
      '}',
      "",
      '# --- 2. Etat des groupes demandes ------------------------------------------',
      '$aCreer = New-Object System.Collections.ArrayList',
      '$existants = New-Object System.Collections.ArrayList',
      'foreach ($nom in $Groupes) {',
      '  if (-not $moduleOk) { continue }',
      '  $existant = $null',
      '  try { $existant = Get-ADGroup -Filter "Name -eq \'$nom\'" -Properties GroupScope,GroupCategory,DistinguishedName,Description -ErrorAction SilentlyContinue } catch { }',
      '  if ($existant) {',
      '    [void]$existants.Add($existant)',
      "    $ecart = ''",
      '    if ($existant.GroupScope -ne $Portee) { $ecart = "Portee differente : $($existant.GroupScope) au lieu de $Portee. " }',
      '    if ($existant.GroupCategory -ne $Categorie) { $ecart = $ecart + "Categorie differente : $($existant.GroupCategory) au lieu de $Categorie." }',
      "    $etatG = 'OK'",
      '    if ($ecart -ne \'\') { $etatG = \'ATTENTION\' }',
      "    [void](Add-KjemoResultat -Categorie 'Groupes existants' -Controle $nom -Etat $etatG -Valeur (\"$($existant.GroupCategory) / $($existant.GroupScope) — $($existant.DistinguishedName)\") -Commentaire ($ecart + \" Le groupe n''est ni recree, ni modifie.\"))",
      '  } else {',
      '    [void]$aCreer.Add($nom)',
      "    [void](Add-KjemoResultat -Categorie 'Groupes a creer' -Controle $nom -Etat 'INFO' -Valeur \"$Categorie / $Portee dans $DnOu\")",
      '  }',
      '}',
      '[void](Add-KjemoResultat -Categorie \'Resume\' -Controle \'Inventaire\' -Etat \'INFO\' -Valeur ("a creer : $($aCreer.Count) ; existants : $($existants.Count)"))',
      "",
      '# --- 3. Rappel du modele AGDLP ---------------------------------------------',
      "# A comme comptes, G comme groupe Global, DL comme groupe de Domaine Local,",
      "# P comme permissions. Les comptes entrent dans un groupe Global ; les",
      "# permissions se posent sur un groupe de Domaine Local ; le Global devient",
      "# membre du Domaine Local. Dans une foret multi-domaines, un groupe",
      "# Universel s'intercale : AGUDLP.",
      "[void](Add-KjemoResultat -Categorie 'Modele' -Controle 'AGDLP' -Etat 'INFO' -Valeur 'comptes -> groupe Global -> groupe de Domaine local -> permissions' -Commentaire 'Poser une permission directement sur un compte, ou sur un groupe Global, rend les droits impossibles a maintenir.')",
      "if ($Portee -eq 'Global' -and $Categorie -eq 'Security') {",
      "  [void](Add-KjemoResultat -Categorie 'Modele' -Controle 'Usage attendu' -Etat 'INFO' -Valeur 'groupe de personnes' -Commentaire 'Un groupe Global rassemble des comptes ; il ne devrait pas porter de permissions directement.')",
      "} elseif ($Portee -eq 'DomainLocal') {",
      "  [void](Add-KjemoResultat -Categorie 'Modele' -Controle 'Usage attendu' -Etat 'INFO' -Valeur 'groupe de permissions' -Commentaire 'Un groupe de Domaine local porte les droits sur une ressource et contient des groupes Globaux.')",
      "} elseif ($Portee -eq 'Universal') {",
      "  [void](Add-KjemoResultat -Categorie 'Modele' -Controle 'Usage attendu' -Etat 'INFO' -Valeur 'foret multi-domaines' -Commentaire 'Un groupe Universel est replique dans le catalogue global : a reserver aux besoins inter-domaines.')",
      '}',
      "",
      '# --- 4. Creation, uniquement en mode Appliquer -----------------------------',
      "if ($Mode -eq 'Appliquer' -and $moduleOk -and $ouOk) {",
      '  foreach ($nom in $aCreer) {',
      '    try {',
      '      New-ADGroup -Name $nom -SamAccountName $nom -GroupCategory $Categorie -GroupScope $Portee -Path $DnOu -Description $Description -ErrorAction Stop',
      "      [void](Add-KjemoResultat -Categorie 'Action' -Controle $nom -Etat 'OK' -Valeur 'cree')",
      '    } catch {',
      "      [void](Add-KjemoResultat -Categorie 'Action' -Controle $nom -Etat 'PROBLEME' -Valeur $_.Exception.Message)",
      '    }',
      '  }',
      "  Write-Host ''",
      "  Write-Host '--- Groupes apres creation ---'",
      '  foreach ($nom in $Groupes) {',
      '    $verif = $null',
      '    try { $verif = Get-ADGroup -Filter "Name -eq \'$nom\'" -Properties GroupScope,GroupCategory,DistinguishedName -ErrorAction SilentlyContinue } catch { }',
      '    if ($verif) {',
      '      [void](Add-KjemoResultat -Categorie \'Verification\' -Controle $nom -Etat \'OK\' -Valeur ("$($verif.GroupCategory) / $($verif.GroupScope) — $($verif.DistinguishedName)"))',
      '    } else {',
      "      [void](Add-KjemoResultat -Categorie 'Verification' -Controle $nom -Etat 'PROBLEME' -Valeur 'absent apres execution')",
      '    }',
      '  }',
      '} else {',
      "  Write-Host ''",
      "  Write-Host '--- Simulation (-WhatIf) ---'",
      '  foreach ($nom in $aCreer) {',
      '    New-ADGroup -Name $nom -SamAccountName $nom -GroupCategory $Categorie -GroupScope $Portee -Path $DnOu -Description $Description -WhatIf',
      '  }',
      '}',
      blocModeDiagnostic(),
      blocAucuneSuppression('groupe'),
    ];

    return assembler({
      titre: 'Creer des groupes Active Directory',
      outil: 'ad-groups',
      diagnostic: v.mode !== 'Appliquer',
      admin: true,
      parametres: [
        ['Domaine', psB64(domaine)],
        ['OuCible', psB64(dnOu)],
        ['Portee', psB64(v.grpScope)],
        ['Mode', psB64(v.mode)],
      ],
      corps,
      prefixeFichier: 'kjemo-ad-groupes',
      formats: 'Console',
    });
  },
  gui: [
    'Utilisateurs et ordinateurs Active Directory (dsa.msc).',
    'Clic droit sur l\u2019OU de destination > Nouveau > Groupe.',
    'Choisir l\u2019étendue (Globale, Domaine local, Universelle) et le type (Sécurité ou Distribution).',
    'Renseigner la description : c\u2019est ce qui évite les groupes dont plus personne ne connaît l\u2019usage.',
    'Onglet Membres et Membre de, pour construire la chaîne AGDLP.',
  ],
  keywords: [
    'groupe', 'new-adgroup', 'agdlp', 'agudlp', 'portee', 'global', 'domaine local',
    'universel', 'groupe de securite', 'distribution', 'droits ntfs par groupe',
  ],
  requiresAdmin: true,
  os: OS_AD,
  prereqs: PREREQS_AD.concat([
    'Droit de créer des groupes dans l\u2019OU visée.',
    'Convention de nommage arrêtée : renommer un groupe plus tard n\u2019est pas anodin quand des permissions y font référence.',
  ]),
  commonErrors: ERREURS_AD.concat([
    {
      message: 'New-ADGroup : The specified group already exists',
      cause: 'Un groupe porte déjà ce nom dans le domaine.',
      fix: 'Le script le détecte avant d\u2019agir et signale si la portée ou la catégorie diffèrent de ce qui est demandé.',
    },
    {
      message: 'Impossible de changer la portée d\u2019un groupe existant',
      cause: 'Active Directory n\u2019autorise que certaines conversions de portée, et seulement si les membres le permettent.',
      fix: 'Créer un nouveau groupe avec la bonne portée, y basculer les membres, puis retirer l\u2019ancien après vérification.',
    },
    {
      message: 'Les permissions ne suivent pas les utilisateurs ajoutés au groupe',
      cause: 'Le jeton de sécurité de la session ouverte ne contient pas les nouveaux groupes.',
      fix: 'Fermer puis rouvrir la session de l\u2019utilisateur. Un redémarrage n\u2019est pas nécessaire.',
    },
  ]),
  reversible: true,
  verifyAfter: [
    'Get-ADGroup affiche chaque groupe avec sa portée, sa catégorie et son DN.',
    'Les groupes existants sont signalés, avec l\u2019écart éventuel de portée ou de catégorie.',
    'Dans dsa.msc, les groupes apparaissent dans l\u2019OU demandée.',
  ],
  rollback: {
    summary: 'Un groupe créé par erreur et sans membre ni permission se supprime proprement. Dès qu\u2019il porte des permissions, sa suppression laisse des ACL orphelines : on vide d\u2019abord, on documente, puis on décide.',
    diagnostic: '# Constater avant de defaire : membres, appartenances et anciennete.\nGet-ADGroup -Identity \'<nom-du-groupe>\' -Properties Members,MemberOf,WhenCreated,Description | Format-List Name,GroupScope,GroupCategory,WhenCreated,Description\nGet-ADGroupMember -Identity \'<nom-du-groupe>\' | Format-Table Name,ObjectClass',
    command: '# Retirer un groupe cree par erreur, apres avoir verifie qu\'il est vide et\n# qu\'aucune ressource ne s\'y refere. -Confirm est explicite.\nRemove-ADGroup -Identity \'<nom-du-groupe>\' -Confirm',
    exceptional: '# AVERTISSEMENT CRITIQUE — suppression d\'un groupe porteur de permissions.\n#\n# Supprimer un groupe detruit son SID. Toutes les ACL qui le mentionnent\n# deviennent orphelines : elles affichent un SID brut, et les droits qu\'elles\n# accordaient disparaissent sans que rien ne l\'annonce. Recreer un groupe du\n# meme nom ne les retablit pas : ce serait un autre SID.\n#\n# Avant d\'y penser :\n#   1. Auditer ou ce groupe est utilise (partages, NTFS, applications).\n#   2. Exporter la liste de ses membres.\n#   3. Verifier que la corbeille Active Directory est activee.\n#\n# Reference officielle :\n# https://learn.microsoft.com/powershell/module/activedirectory/remove-adgroup\n#\n# Commande, a executer manuellement :\n#   Remove-ADGroup -Identity \'<nom>\' -Confirm',
    warning: 'Un groupe de sécurité supprimé laisse des ACL orphelines partout où il était référencé. C\u2019est précisément ce que l\u2019outil d\u2019audit des permissions du LOT 2 détecte sous le nom « ACL orpheline ».',
  },
  checks: [
    'Choisir la portée d\u2019abord : elle conditionne tout le reste.',
    'Séparer les groupes de personnes (Global) des groupes de permissions (Domaine local).',
    'Renseigner une description : un groupe sans description devient un groupe qu\u2019on n\u2019ose plus toucher.',
  ],
  source: 'https://learn.microsoft.com/powershell/module/activedirectory/new-adgroup',
  sources: [
    { label: 'New-ADGroup', url: 'https://learn.microsoft.com/powershell/module/activedirectory/new-adgroup' },
    { label: 'Get-ADGroup', url: 'https://learn.microsoft.com/powershell/module/activedirectory/get-adgroup' },
    { label: 'Remove-ADGroup', url: 'https://learn.microsoft.com/powershell/module/activedirectory/remove-adgroup' },
  ],
};

/**
 * 4. ad-group-members-csv — ajouter des membres, sans jamais en retirer.
 *
 * L'ajout est réversible et sans perte ; le retrait ne l'est pas toujours,
 * parce qu'il faut savoir ce qui dépendait de cette appartenance. Ce script
 * ajoute, et c'est tout.
 */
export const outilMembresCsv = {
  id: 'ad-group-members-csv',
  icon: '\u25c7',
  category: 'Active Directory',
  subcategory: 'Utilisateurs et groupes',
  title: 'Importer les membres de groupes depuis un CSV',
  risk: 'caution',
  summary: 'Valide chaque compte et chaque groupe, détecte les doublons du fichier et les membres déjà présents, puis ajoute — sans jamais retirer personne.',
  fields: [
    champDomaine(),
    { id: 'memCsvPath', label: 'Chemin local du fichier CSV', default: 'C:\\Imports\\membres.csv', help: 'Deux colonnes : le compte et le groupe.' },
    { id: 'memColumns', label: 'Colonnes attendues', default: 'SamAccountName,Groupe', help: 'Les deux colonnes sont obligatoires.' },
    { id: 'memDelimiter', label: 'Séparateur', type: 'select', default: ';', options: [[';', 'Point-virgule (Excel français)'], [',', 'Virgule'], ['\t', 'Tabulation']] },
    champMode(),
  ],
  validate(v) {
    const errors = {};
    verifier(errors, 'adDomain', validerFqdn(v.adDomain));
    verifier(errors, 'memCsvPath', validerCheminWindowsLocal(v.memCsvPath));
    if (!/\.csv$/i.test(String(v.memCsvPath ?? '').trim()) && !errors.memCsvPath) {
      errors.memCsvPath = 'Le fichier doit porter l\u2019extension .csv.';
    }
    verifier(errors, 'memColumns', validerColonnesCsv(v.memColumns, { obligatoires: ['SamAccountName', 'Groupe'] }));
    verifier(errors, 'memDelimiter', validerChoix(v.memDelimiter, [';', ',', '\t'], 'Le séparateur'));
    verifier(errors, 'mode', validerModeExecution(v.mode));
    return errors;
  },
  generate(v) {
    assertValid(this, v);
    const domaine = validerFqdn(v.adDomain).value;
    const colonnes = validerColonnesCsv(v.memColumns, { obligatoires: ['SamAccountName', 'Groupe'] }).value;

    const corps = [
      `$Mode = ${psB64(v.mode)}`,
      `$Domaine = ${psB64(domaine)}`,
      `$Fichier = ${psB64(validerCheminWindowsLocal(v.memCsvPath).value)}`,
      `$Separateur = ${psB64(v.memDelimiter)}`,
      `$ColonnesAttendues = @(${colonnes.map((c) => psB64(c)).join(', ')})`,
      "$KjemoFormat = 'Console'",
      "",
      blocModuleAd(),
      "",
      '# --- 1. Lecture et schema du fichier ---------------------------------------',
      'if (-not (Test-Path -LiteralPath $Fichier)) {',
      "  [void](Add-KjemoResultat -Categorie 'Fichier' -Controle 'Existence' -Etat 'PROBLEME' -Valeur $Fichier -Commentaire 'Fichier introuvable sur cette machine.')",
      '  return',
      '}',
      '$lignes = @()',
      'try { $lignes = @(Import-Csv -LiteralPath $Fichier -Delimiter $Separateur -Encoding UTF8) } catch {',
      "  [void](Add-KjemoResultat -Categorie 'Fichier' -Controle 'Lecture' -Etat 'PROBLEME' -Valeur $_.Exception.Message)",
      '  return',
      '}',
      'if (@($lignes).Count -eq 0) {',
      "  [void](Add-KjemoResultat -Categorie 'Fichier' -Controle 'Contenu' -Etat 'PROBLEME' -Valeur '0 ligne' -Commentaire 'Fichier vide, ou separateur incorrect.')",
      '  return',
      '}',
      '$colonnesReelles = @($lignes[0].PSObject.Properties.Name)',
      '$manquantes = @($ColonnesAttendues | Where-Object { $colonnesReelles -notcontains $_ })',
      'if (@($manquantes).Count -gt 0) {',
      '  [void](Add-KjemoResultat -Categorie \'Schema\' -Controle \'Colonnes manquantes\' -Etat \'PROBLEME\' -Valeur ($manquantes -join \', \'))',
      '  return',
      '}',
      '[void](Add-KjemoResultat -Categorie \'Fichier\' -Controle \'Lignes lues\' -Etat \'OK\' -Valeur ("$(@($lignes).Count) ligne(s)"))',
      "",
      '# --- 2. Controles ligne par ligne -------------------------------------------',
      '$paires = @{}',
      '$aAjouter = New-Object System.Collections.ArrayList',
      '$ignores = New-Object System.Collections.ArrayList',
      '$erreurs = New-Object System.Collections.ArrayList',
      '$numero = 0',
      'foreach ($ligne in $lignes) {',
      '  $numero++',
      '  $sam = ("" + $ligne.SamAccountName).Trim()',
      '  $groupe = ("" + $ligne.Groupe).Trim()',
      "  if ($sam -eq '' -or $groupe -eq '') {",
      '    [void]$erreurs.Add([pscustomobject]@{ Ligne = $numero; Compte = $sam; Groupe = $groupe; Raison = \'Colonne vide\' })',
      '    [void](Add-KjemoResultat -Categorie \'Erreurs\' -Controle "Ligne $numero" -Etat \'PROBLEME\' -Valeur "$sam / $groupe" -Commentaire \'Compte ou groupe vide.\')',
      '    continue',
      '  }',
      '  $cle = ($sam + \'|\' + $groupe).ToLower()',
      '  if ($paires.ContainsKey($cle)) {',
      '    [void]$ignores.Add([pscustomobject]@{ Ligne = $numero; Compte = $sam; Groupe = $groupe; Raison = "Doublon du fichier, deja vu ligne $($paires[$cle])" })',
      '    [void](Add-KjemoResultat -Categorie \'Ignores\' -Controle "Ligne $numero" -Etat \'INFO\' -Valeur "$sam -> $groupe" -Commentaire "Doublon dans le fichier, deja vu ligne $($paires[$cle]).")',
      '    continue',
      '  }',
      '  $paires[$cle] = $numero',
      '  if (-not $moduleOk) { continue }',
      "",
      '  $compteObj = $null',
      '  try { $compteObj = Get-ADUser -Filter "SamAccountName -eq \'$sam\'" -ErrorAction SilentlyContinue } catch { }',
      '  if (-not $compteObj) {',
      '    [void]$erreurs.Add([pscustomobject]@{ Ligne = $numero; Compte = $sam; Groupe = $groupe; Raison = \'Compte introuvable\' })',
      '    [void](Add-KjemoResultat -Categorie \'Erreurs\' -Controle "Ligne $numero" -Etat \'PROBLEME\' -Valeur $sam -Commentaire \'Compte introuvable dans l\'\'annuaire.\')',
      '    continue',
      '  }',
      '  $groupeObj = $null',
      '  try { $groupeObj = Get-ADGroup -Filter "Name -eq \'$groupe\'" -ErrorAction SilentlyContinue } catch { }',
      '  if (-not $groupeObj) {',
      '    [void]$erreurs.Add([pscustomobject]@{ Ligne = $numero; Compte = $sam; Groupe = $groupe; Raison = \'Groupe introuvable\' })',
      '    [void](Add-KjemoResultat -Categorie \'Erreurs\' -Controle "Ligne $numero" -Etat \'PROBLEME\' -Valeur $groupe -Commentaire \'Groupe introuvable dans l\'\'annuaire.\')',
      '    continue',
      '  }',
      '  $dejaMembre = $false',
      '  try {',
      '    $membres = @(Get-ADGroupMember -Identity $groupeObj.DistinguishedName -ErrorAction SilentlyContinue)',
      '    if ($membres | Where-Object { $_.distinguishedName -eq $compteObj.DistinguishedName }) { $dejaMembre = $true }',
      '  } catch { }',
      '  if ($dejaMembre) {',
      '    [void]$ignores.Add([pscustomobject]@{ Ligne = $numero; Compte = $sam; Groupe = $groupe; Raison = \'Deja membre\' })',
      '    [void](Add-KjemoResultat -Categorie \'Ignores\' -Controle "Ligne $numero" -Etat \'OK\' -Valeur "$sam -> $groupe" -Commentaire \'Deja membre : rien a faire.\')',
      '    continue',
      '  }',
      '  [void]$aAjouter.Add([pscustomobject]@{ Ligne = $numero; Compte = $compteObj.DistinguishedName; CompteNom = $sam; Groupe = $groupeObj.DistinguishedName; GroupeNom = $groupe })',
      '  [void](Add-KjemoResultat -Categorie \'A ajouter\' -Controle "Ligne $numero" -Etat \'INFO\' -Valeur "$sam -> $groupe")',
      '}',
      '[void](Add-KjemoResultat -Categorie \'Resume\' -Controle \'Bilan\' -Etat \'INFO\' -Valeur ("a ajouter : $($aAjouter.Count) ; ignores : $($ignores.Count) ; erreurs : $($erreurs.Count)"))',
      "",
      '# --- 3. Ajout, uniquement en mode Appliquer ----------------------------------',
      "if ($Mode -eq 'Appliquer' -and $moduleOk) {",
      '  foreach ($paire in $aAjouter) {',
      '    try {',
      '      Add-ADGroupMember -Identity $paire.Groupe -Members $paire.Compte -ErrorAction Stop',
      '      [void](Add-KjemoResultat -Categorie \'Action\' -Controle "$($paire.CompteNom) -> $($paire.GroupeNom)" -Etat \'OK\' -Valeur \'ajoute\')',
      '    } catch {',
      '      [void](Add-KjemoResultat -Categorie \'Action\' -Controle "$($paire.CompteNom) -> $($paire.GroupeNom)" -Etat \'PROBLEME\' -Valeur $_.Exception.Message)',
      '      [void]$erreurs.Add([pscustomobject]@{ Ligne = $paire.Ligne; Compte = $paire.CompteNom; Groupe = $paire.GroupeNom; Raison = $_.Exception.Message })',
      '    }',
      '  }',
      "  Write-Host ''",
      "  Write-Host '--- Appartenances apres import ---'",
      '  foreach ($paire in $aAjouter) {',
      '    $ok = $false',
      '    try {',
      '      $m = @(Get-ADGroupMember -Identity $paire.Groupe -ErrorAction SilentlyContinue)',
      '      if ($m | Where-Object { $_.distinguishedName -eq $paire.Compte }) { $ok = $true }',
      '    } catch { }',
      "    $etatV = 'PROBLEME'",
      "    if ($ok) { $etatV = 'OK' }",
      '    [void](Add-KjemoResultat -Categorie \'Verification\' -Controle "$($paire.CompteNom) -> $($paire.GroupeNom)" -Etat $etatV -Valeur $ok)',
      '  }',
      '} else {',
      "  Write-Host ''",
      "  Write-Host '--- Simulation (-WhatIf) ---'",
      '  foreach ($paire in $aAjouter) {',
      '    Add-ADGroupMember -Identity $paire.Groupe -Members $paire.Compte -WhatIf',
      '  }',
      '}',
      "",
      '# --- 4. Journal ---------------------------------------------------------------',
      '$journal = Join-Path (Join-Path $env:USERPROFILE \'Desktop\') ("kjemo-ad-membres-" + (Get-Date -Format \'yyyyMMdd-HHmmss\') + ".csv")',
      '$toutes = @()',
      "$toutes += @($aAjouter | ForEach-Object { [pscustomobject]@{ Ligne = $_.Ligne; Compte = $_.CompteNom; Groupe = $_.GroupeNom; Statut = 'A ajouter ou ajoute'; Detail = '' } })",
      "$toutes += @($ignores | ForEach-Object { [pscustomobject]@{ Ligne = $_.Ligne; Compte = $_.Compte; Groupe = $_.Groupe; Statut = 'Ignore'; Detail = $_.Raison } })",
      "$toutes += @($erreurs | ForEach-Object { [pscustomobject]@{ Ligne = $_.Ligne; Compte = $_.Compte; Groupe = $_.Groupe; Statut = 'Erreur'; Detail = $_.Raison } })",
      'if (@($toutes).Count -gt 0) {',
      '  $toutes | Export-Csv -LiteralPath $journal -NoTypeInformation -Encoding UTF8',
      '  Write-Host "Journal : $journal"',
      '}',
      "",
      "Write-Host ''",
      "Write-Host '--- Portee de ce script ---'",
      "Write-Host 'Ce script AJOUTE des membres. Il n''en retire aucun : retirer une'",
      "Write-Host 'appartenance peut couper un acces dont personne n''avait note la'",
      "Write-Host 'dependance. Le retrait se fait a la main, apres verification.'",
      blocModeDiagnostic(),
    ];

    return assembler({
      titre: 'Importer les membres de groupes depuis un CSV',
      outil: 'ad-group-members-csv',
      diagnostic: v.mode !== 'Appliquer',
      admin: true,
      parametres: [
        ['Domaine', psB64(domaine)],
        ['Fichier', psB64(v.memCsvPath)],
        ['Mode', psB64(v.mode)],
      ],
      corps,
      prefixeFichier: 'kjemo-ad-membres',
      formats: 'Console',
    });
  },
  gui: [
    'dsa.msc > le groupe > Propriétés > onglet Membres > Ajouter.',
    'Ou : le compte > Propriétés > onglet Membre de > Ajouter, pour partir de l\u2019utilisateur.',
    'Pour vérifier : Get-ADGroupMember, ou l\u2019onglet Membres du groupe.',
    'Après ajout, l\u2019utilisateur doit fermer et rouvrir sa session pour que son jeton contienne le groupe.',
  ],
  keywords: [
    'membre de groupe', 'add-adgroupmember', 'import membres', 'appartenance',
    'csv groupes', 'ajouter au groupe', 'jeton de securite', 'droits ne suivent pas',
  ],
  requiresAdmin: true,
  os: OS_AD,
  prereqs: PREREQS_AD.concat([
    'Droit de modifier l\u2019appartenance des groupes visés.',
    'Les comptes et les groupes doivent exister : le script ne crée ni l\u2019un ni l\u2019autre.',
    'Fichier CSV en UTF-8, avec les deux colonnes attendues.',
  ]),
  commonErrors: ERREURS_AD.concat([
    {
      message: 'Add-ADGroupMember : The specified account name is already a member of the group',
      cause: 'Le compte appartient déjà au groupe.',
      fix: 'Le script détecte ce cas avant d\u2019agir et classe la ligne en « ignoré ».',
    },
    {
      message: 'Add-ADGroupMember : The server is unwilling to process the request',
      cause: 'La portée du groupe n\u2019autorise pas ce type de membre — un groupe Global ne peut pas contenir un compte d\u2019un autre domaine.',
      fix: 'Revoir la portée du groupe, ou passer par un groupe Universel dans une forêt multi-domaines.',
    },
    {
      message: 'L\u2019utilisateur ne voit pas ses nouveaux droits',
      cause: 'Le jeton de sécurité de sa session date d\u2019avant l\u2019ajout.',
      fix: 'Fermer puis rouvrir la session.',
    },
  ]),
  reversible: true,
  verifyAfter: [
    'Le journal CSV liste chaque paire compte/groupe avec son statut.',
    'Get-ADGroupMember montre les nouveaux membres.',
    'Dans dsa.msc, l\u2019onglet Membres du groupe est à jour.',
  ],
  rollback: {
    summary: 'Retirer une appartenance ajoutée par erreur est simple et réversible — encore faut-il savoir ce qui en dépendait. Le journal de l\u2019import donne exactement la liste des paires ajoutées.',
    diagnostic: '# Relire ce qui a ete ajoute, et l\'etat actuel du groupe.\nGet-ADGroupMember -Identity \'<groupe>\' | Format-Table Name,SamAccountName,ObjectClass\nGet-ADUser -Identity \'<compte>\' -Properties MemberOf | Select-Object -ExpandProperty MemberOf',
    command: '# Retirer une appartenance ajoutee par erreur. -Confirm est explicite :\n# PowerShell demandera confirmation avant de retirer le membre.\nRemove-ADGroupMember -Identity \'<groupe>\' -Members \'<compte>\' -Confirm',
    exceptional: '',
    warning: 'Retirer un compte d\u2019un groupe lui coupe les accès qui en dépendaient — parfois des accès dont personne n\u2019avait noté la dépendance. Vérifier avant, surtout sur les groupes d\u2019administration.',
  },
  checks: [
    'Vérifier que les comptes et les groupes existent avant de lancer l\u2019import.',
    'Le mode Diagnostic donne la liste exacte des ajouts qui auront lieu.',
    'Ce script n\u2019enlève jamais personne d\u2019un groupe : c\u2019est volontaire.',
  ],
  source: 'https://learn.microsoft.com/powershell/module/activedirectory/add-adgroupmember',
  sources: [
    { label: 'Add-ADGroupMember', url: 'https://learn.microsoft.com/powershell/module/activedirectory/add-adgroupmember' },
    { label: 'Get-ADGroupMember', url: 'https://learn.microsoft.com/powershell/module/activedirectory/get-adgroupmember' },
    { label: 'Remove-ADGroupMember', url: 'https://learn.microsoft.com/powershell/module/activedirectory/remove-adgroupmember' },
  ],
};

/**
 * 5. ad-home-profile-paths — dossiers personnels et profils itinérants.
 *
 * Trois attributs d'annuaire, mais surtout deux couches de permissions et un
 * partage qui doivent suivre. Le script relève l'état existant AVANT de le
 * changer, et écrit les anciennes valeurs dans un fichier de reprise.
 */
export const outilCheminsProfils = {
  id: 'ad-home-profile-paths',
  icon: '\u25e8',
  category: 'Active Directory',
  subcategory: 'Utilisateurs et groupes',
  title: 'Dossiers personnels et profils itinérants',
  risk: 'caution',
  summary: 'Vérifie partage, dossiers et permissions, sauvegarde les anciennes valeurs, puis pose HomeDirectory, HomeDrive et ProfilePath de façon idempotente.',
  fields: [
    champDomaine(),
    { id: 'hpOu', label: 'OU des comptes concernés', default: 'Médecin', help: 'Chemin parent/enfant, relatif au domaine. Tous les comptes de cette OU sont traités.' },
    { id: 'hpHomeRoot', label: 'Racine des dossiers personnels (UNC)', default: '\\\\srv-fichiers\\Personnels', help: 'Le dossier de chaque compte est créé sous cette racine, à son nom.' },
    { id: 'hpDrive', label: 'Lettre de lecteur', default: 'H:', help: 'Lecteur auquel le dossier personnel sera monté.' },
    { id: 'hpProfileRoot', label: 'Racine des profils itinérants (UNC)', default: '\\\\srv-fichiers\\Profils', help: 'Windows ajoute le suffixe de version (.V6) de lui-même.' },
    {
      id: 'hpProfiles', label: 'Profils itinérants', type: 'select', default: 'Non',
      options: [['Non', 'Non — dossier personnel seulement'], ['Oui', 'Oui — dossier personnel et profil itinérant']],
      help: 'Le profil itinérant copie tout le profil à chaque ouverture et fermeture de session : à réserver aux cas qui le justifient.',
    },
    champMode(),
  ],
  validate(v) {
    const errors = {};
    verifier(errors, 'adDomain', validerFqdn(v.adDomain));
    verifier(errors, 'hpOu', validerCheminOu(v.hpOu));
    verifier(errors, 'hpHomeRoot', validerCheminUnc(v.hpHomeRoot));
    verifier(errors, 'hpDrive', validerLettreLecteur(v.hpDrive));
    const avecProfils = String(v.hpProfiles ?? '') === 'Oui';
    const racineProfil = String(v.hpProfileRoot ?? '').trim();
    if (avecProfils || racineProfil) {
      verifier(errors, 'hpProfileRoot', validerCheminUnc(racineProfil));
    }
    verifier(errors, 'hpProfiles', validerChoix(v.hpProfiles, ['Oui', 'Non'], 'L\u2019option de profil itinérant'));
    verifier(errors, 'mode', validerModeExecution(v.mode));
    return errors;
  },
  generate(v) {
    assertValid(this, v);
    const domaine = validerFqdn(v.adDomain).value;
    const dnOu = cheminOuVersDn(validerCheminOu(v.hpOu).value, domaine);
    const avecProfils = v.hpProfiles === 'Oui';

    const corps = [
      `$Mode = ${psB64(v.mode)}`,
      `$DnOu = ${psB64(dnOu)}`,
      `$RacinePersonnels = ${psB64(validerCheminUnc(v.hpHomeRoot).value)}`,
      `$Lecteur = ${psB64(validerLettreLecteur(v.hpDrive).value)}`,
      `$RacineProfils = ${psB64(String(v.hpProfileRoot ?? '').trim())}`,
      `$AvecProfils = $${avecProfils ? 'true' : 'false'}`,
      "$KjemoFormat = 'Console'",
      "",
      blocModuleAd(),
      "",
      '# --- 1. Les partages repondent-ils ? ---------------------------------------',
      '# Un chemin UNC inscrit dans l\'annuaire alors que le partage n\'existe pas',
      "# donne un symptome deroutant : la session s'ouvre, mais le lecteur reseau",
      '# reste absent, sans message clair.',
      'function Test-KjemoPartage {',
      '  param([Parameter(Mandatory=$true)][string]$Chemin)',
      '  $ok = $false',
      '  try { $ok = Test-Path -LiteralPath $Chemin -ErrorAction SilentlyContinue } catch { }',
      '  return $ok',
      '}',
      "",
      '$racineOk = Test-KjemoPartage -Chemin $RacinePersonnels',
      "$etatRacine = 'PROBLEME'",
      "$commentaireRacine = 'Racine injoignable : verifie le partage, le nom du serveur et les droits.'",
      "if ($racineOk) { $etatRacine = 'OK'; $commentaireRacine = '' }",
      '[void](Add-KjemoResultat -Categorie \'Partages\' -Controle \'Racine des dossiers personnels\' -Etat $etatRacine -Valeur $RacinePersonnels -Commentaire $commentaireRacine)',
      "",
      'if ($AvecProfils) {',
      '  $profilsOk = Test-KjemoPartage -Chemin $RacineProfils',
      "  $etatProfils = 'PROBLEME'",
      "  if ($profilsOk) { $etatProfils = 'OK' }",
      '  [void](Add-KjemoResultat -Categorie \'Partages\' -Controle \'Racine des profils\' -Etat $etatProfils -Valeur $RacineProfils)',
      '}',
      "",
      '# Permissions SMB et NTFS de la racine : les deux comptent, et c\'est',
      "# l'intersection qui s'applique.",
      '$nomServeur = ($RacinePersonnels -split \'\\\\\')[2]',
      '$nomPartage = ($RacinePersonnels -split \'\\\\\')[3]',
      'if ($nomServeur -and $nomPartage) {',
      '  try {',
      '    $acces = @(Get-SmbShareAccess -Name $nomPartage -CimSession $nomServeur -ErrorAction SilentlyContinue)',
      '    foreach ($a in $acces) {',
      '      [void](Add-KjemoResultat -Categorie \'Permissions SMB\' -Controle $a.AccountName -Etat \'INFO\' -Valeur ("$($a.AccessControlType) $($a.AccessRight)"))',
      '    }',
      '  } catch {',
      "    [void](Add-KjemoResultat -Categorie 'Permissions SMB' -Controle 'Lecture' -Etat 'IGNORE' -Valeur 'non lisible a distance' -Commentaire 'Execute le diagnostic depuis le serveur de fichiers pour ce controle.')",
      '  }',
      '  try {',
      '    $acl = Get-Acl -LiteralPath $RacinePersonnels -ErrorAction SilentlyContinue',
      '    if ($acl) {',
      '      foreach ($regle in @($acl.Access | Where-Object { -not $_.IsInherited })) {',
      '        [void](Add-KjemoResultat -Categorie \'Permissions NTFS\' -Controle $regle.IdentityReference.Value -Etat \'INFO\' -Valeur ("$($regle.AccessControlType) $($regle.FileSystemRights)"))',
      '      }',
      '    }',
      '  } catch { }',
      '}',
      "",
      '# --- 2. Comptes concernes et etat actuel -----------------------------------',
      '$comptes = @()',
      'if ($moduleOk) {',
      '  try { $comptes = @(Get-ADUser -SearchBase $DnOu -Filter * -Properties HomeDirectory,HomeDrive,ProfilePath,SamAccountName) } catch {',
      "    [void](Add-KjemoResultat -Categorie 'Comptes' -Controle 'Lecture' -Etat 'PROBLEME' -Valeur $_.Exception.Message -Commentaire 'OU introuvable, ou droits insuffisants.')",
      '  }',
      '}',
      '[void](Add-KjemoResultat -Categorie \'Comptes\' -Controle \'Comptes trouves\' -Etat \'INFO\' -Valeur ("$(@($comptes).Count) compte(s) dans $DnOu"))',
      "",
      '$aModifier = New-Object System.Collections.ArrayList',
      '$conformes = New-Object System.Collections.ArrayList',
      '$ancien = New-Object System.Collections.ArrayList',
      "",
      'foreach ($compte in $comptes) {',
      '  $cheminVoulu = Join-Path $RacinePersonnels $compte.SamAccountName',
      "  $profilVoulu = ''",
      '  if ($AvecProfils) { $profilVoulu = Join-Path $RacineProfils $compte.SamAccountName }',
      "",
      '  [void]$ancien.Add([pscustomobject]@{',
      '    SamAccountName = $compte.SamAccountName',
      '    HomeDirectory  = $compte.HomeDirectory',
      '    HomeDrive      = $compte.HomeDrive',
      '    ProfilePath    = $compte.ProfilePath',
      '  })',
      "",
      '  $dejaConforme = ($compte.HomeDirectory -eq $cheminVoulu) -and ($compte.HomeDrive -eq $Lecteur)',
      '  if ($AvecProfils) { $dejaConforme = $dejaConforme -and ($compte.ProfilePath -eq $profilVoulu) }',
      "",
      '  if ($dejaConforme) {',
      '    [void]$conformes.Add($compte.SamAccountName)',
      '    [void](Add-KjemoResultat -Categorie \'Comptes conformes\' -Controle $compte.SamAccountName -Etat \'OK\' -Valeur ("$($compte.HomeDrive) $($compte.HomeDirectory)") -Commentaire \'Deja conforme : aucune ecriture.\')',
      '  } else {',
      '    [void]$aModifier.Add([pscustomobject]@{ Sam = $compte.SamAccountName; Dn = $compte.DistinguishedName; Chemin = $cheminVoulu; Profil = $profilVoulu })',
      '    [void](Add-KjemoResultat -Categorie \'Comptes a modifier\' -Controle $compte.SamAccountName -Etat \'INFO\' -Valeur ("actuel : $($compte.HomeDrive) $($compte.HomeDirectory) -> voulu : $Lecteur $cheminVoulu"))',
      '  }',
      "",
      '  # Le dossier existe-t-il deja sous la racine ?',
      '  if ($racineOk) {',
      '    $existe = Test-KjemoPartage -Chemin $cheminVoulu',
      '    if (-not $existe) {',
      "      [void](Add-KjemoResultat -Categorie 'Dossiers' -Controle $compte.SamAccountName -Etat 'ATTENTION' -Valeur $cheminVoulu -Commentaire \"Dossier absent : il sera cree en mode Appliquer, avec un droit de modification pour l''utilisateur.\")",
      '    } else {',
      '      [void](Add-KjemoResultat -Categorie \'Dossiers\' -Controle $compte.SamAccountName -Etat \'OK\' -Valeur $cheminVoulu)',
      '    }',
      '  }',
      '}',
      '[void](Add-KjemoResultat -Categorie \'Resume\' -Controle \'Bilan\' -Etat \'INFO\' -Valeur ("a modifier : $($aModifier.Count) ; deja conformes : $($conformes.Count)"))',
      "",
      '# --- 3. Sauvegarde des anciennes valeurs, AVANT toute ecriture -------------',
      '$sauvegarde = Join-Path (Join-Path $env:USERPROFILE \'Desktop\') ("kjemo-ad-chemins-avant-" + (Get-Date -Format \'yyyyMMdd-HHmmss\') + ".csv")',
      'if (@($ancien).Count -gt 0) {',
      '  $ancien | Export-Csv -LiteralPath $sauvegarde -NoTypeInformation -Encoding UTF8',
      "  [void](Add-KjemoResultat -Categorie 'Sauvegarde' -Controle 'Anciennes valeurs' -Etat 'OK' -Valeur $sauvegarde -Commentaire 'Ce fichier permet de remettre les valeurs precedentes, compte par compte.')",
      '}',
      "",
      '# --- 4. Application, uniquement en mode Appliquer ---------------------------',
      "if ($Mode -eq 'Appliquer' -and $moduleOk) {",
      '  foreach ($cible in $aModifier) {',
      '    # 4a. Creer le dossier personnel s\'il manque, et donner a l\'utilisateur',
      '    #     le droit de modifier SON dossier — pas plus.',
      '    if ($racineOk -and -not (Test-KjemoPartage -Chemin $cible.Chemin)) {',
      '      try {',
      '        New-Item -ItemType Directory -Path $cible.Chemin -ErrorAction Stop | Out-Null',
      '        $aclDossier = Get-Acl -LiteralPath $cible.Chemin',
      '        $regle = New-Object System.Security.AccessControl.FileSystemAccessRule(',
      '          $cible.Sam, \'Modify\', \'ContainerInherit,ObjectInherit\', \'None\', \'Allow\')',
      '        $aclDossier.AddAccessRule($regle)',
      '        Set-Acl -LiteralPath $cible.Chemin -AclObject $aclDossier',
      "        [void](Add-KjemoResultat -Categorie 'Action' -Controle $cible.Sam -Etat 'OK' -Valeur 'dossier cree et droits poses')",
      '      } catch {',
      "        [void](Add-KjemoResultat -Categorie 'Action' -Controle $cible.Sam -Etat 'PROBLEME' -Valeur $_.Exception.Message -Commentaire 'Creation du dossier ou pose des droits refusee.')",
      '      }',
      '    }',
      '    # 4b. Ecrire les attributs d\'annuaire.',
      '    try {',
      '      if ($AvecProfils) {',
      '        Set-ADUser -Identity $cible.Dn -HomeDirectory $cible.Chemin -HomeDrive $Lecteur -ProfilePath $cible.Profil -ErrorAction Stop',
      '      } else {',
      '        Set-ADUser -Identity $cible.Dn -HomeDirectory $cible.Chemin -HomeDrive $Lecteur -ErrorAction Stop',
      '      }',
      "      [void](Add-KjemoResultat -Categorie 'Action' -Controle $cible.Sam -Etat 'OK' -Valeur 'attributs mis a jour')",
      '    } catch {',
      "      [void](Add-KjemoResultat -Categorie 'Action' -Controle $cible.Sam -Etat 'PROBLEME' -Valeur $_.Exception.Message)",
      '    }',
      '  }',
      "",
      "  Write-Host ''",
      "  Write-Host '--- Attributs apres modification ---'",
      '  foreach ($cible in $aModifier) {',
      '    $verif = $null',
      '    try { $verif = Get-ADUser -Identity $cible.Dn -Properties HomeDirectory,HomeDrive,ProfilePath -ErrorAction SilentlyContinue } catch { }',
      '    if ($verif) {',
      '      [void](Add-KjemoResultat -Categorie \'Verification\' -Controle $cible.Sam -Etat \'OK\' -Valeur ("$($verif.HomeDrive) $($verif.HomeDirectory) ; profil : $($verif.ProfilePath)"))',
      '    }',
      '  }',
      '} else {',
      "  Write-Host ''",
      "  Write-Host '--- Simulation (-WhatIf) ---'",
      '  foreach ($cible in $aModifier) {',
      '    Set-ADUser -Identity $cible.Dn -HomeDirectory $cible.Chemin -HomeDrive $Lecteur -WhatIf',
      '  }',
      '}',
      "",
      "Write-Host ''",
      "Write-Host '--- Methode alternative ---'",
      "Write-Host 'La redirection de dossiers par strategie de groupe remplace avantageusement'",
      "Write-Host 'les profils itinerants dans la plupart des cas : elle ne copie que ce qui'",
      "Write-Host 'change, et n''allonge pas l''ouverture de session. Elle releve de la'",
      "Write-Host 'categorie GPO, et n''est pas traitee dans ce lot.'",
      blocModeDiagnostic(),
    ];

    return assembler({
      titre: 'Dossiers personnels et profils itinerants',
      outil: 'ad-home-profile-paths',
      diagnostic: v.mode !== 'Appliquer',
      admin: true,
      parametres: [
        ['OuCible', psB64(dnOu)],
        ['RacinePersonnels', psB64(v.hpHomeRoot)],
        ['Lecteur', psB64(v.hpDrive)],
        ['Profils', psB64(v.hpProfiles)],
        ['Mode', psB64(v.mode)],
      ],
      corps,
      prefixeFichier: 'kjemo-ad-chemins',
      formats: 'Console',
    });
  },
  gui: [
    'dsa.msc > le compte > Propriétés > onglet Profil.',
    'Dossier de base : cocher « Connecter », choisir la lettre et saisir le chemin UNC.',
    'Chemin du profil : le renseigner seulement si un profil itinérant est réellement voulu.',
    'Sur le serveur de fichiers : vérifier que le partage existe et que les droits SMB et NTFS le permettent.',
    'La sélection multiple dans dsa.msc permet de traiter plusieurs comptes d\u2019un coup avec %username%.',
  ],
  keywords: [
    'dossier personnel', 'home directory', 'homedrive', 'profil itinerant', 'profilepath',
    'lecteur reseau', 'h:', 'unc', 'partage profils', 'redirection de dossiers',
  ],
  requiresAdmin: true,
  os: OS_AD,
  prereqs: PREREQS_AD.concat([
    'Droit de modifier les comptes de l\u2019OU visée.',
    'Partage SMB existant, avec des droits permettant au serveur de créer les dossiers.',
    'Droits NTFS cohérents sur la racine : chaque utilisateur doit pouvoir écrire dans SON dossier, et pas dans celui des autres.',
    'Exécuter depuis le serveur de fichiers permet de lire aussi les permissions SMB locales.',
  ]),
  commonErrors: ERREURS_AD.concat([
    {
      message: 'Le lecteur réseau n\u2019apparaît pas à l\u2019ouverture de session',
      cause: 'Chemin UNC incorrect, partage absent, ou droits insuffisants sur le dossier.',
      fix: 'Le diagnostic de ce script teste le partage et le dossier avant d\u2019écrire quoi que ce soit.',
    },
    {
      message: 'Le profil itinérant n\u2019est pas chargé et la session utilise un profil temporaire',
      cause: 'Droits insuffisants sur le dossier de profil, ou suffixe de version incompatible.',
      fix: 'Windows ajoute le suffixe .V6 de lui-même : ne pas l\u2019écrire dans le chemin. Vérifier les droits sur le dossier créé.',
    },
    {
      message: 'Set-ADUser : Access is denied',
      cause: 'Droits insuffisants sur les comptes de cette OU.',
      fix: 'Vérifier la délégation sur l\u2019OU avec l\u2019outil d\u2019audit de délégation.',
    },
  ]),
  reversible: true,
  verifyAfter: [
    'Le fichier CSV des anciennes valeurs est écrit sur le Bureau avant toute modification.',
    'Get-ADUser -Properties HomeDirectory,HomeDrive,ProfilePath montre les nouvelles valeurs.',
    'À l\u2019ouverture de session d\u2019un utilisateur concerné, le lecteur apparaît et il peut y écrire.',
    'Les comptes déjà conformes ne sont pas réécrits.',
  ],
  rollback: {
    summary: 'Les anciennes valeurs sont exportées avant toute écriture : l\u2019annulation consiste à les remettre, compte par compte, depuis ce fichier. Les dossiers créés, eux, contiennent des données utilisateur et ne sont pas supprimés.',
    diagnostic: '# Relire le fichier de sauvegarde et l\'etat actuel des comptes.\nImport-Csv -LiteralPath \'<fichier-kjemo-ad-chemins-avant.csv>\' | Format-Table SamAccountName,HomeDirectory,HomeDrive,ProfilePath\nGet-ADUser -SearchBase \'<dn-de-l-ou>\' -Filter * -Properties HomeDirectory,HomeDrive,ProfilePath | Format-Table SamAccountName,HomeDrive,HomeDirectory,ProfilePath',
    command: '# Remettre les valeurs precedentes depuis la sauvegarde. Le script ci-dessous\n# ne touche qu\'aux trois attributs concernes, compte par compte.\nImport-Csv -LiteralPath \'<fichier-kjemo-ad-chemins-avant.csv>\' | ForEach-Object {\n  Set-ADUser -Identity $_.SamAccountName -HomeDirectory $_.HomeDirectory -HomeDrive $_.HomeDrive -ProfilePath $_.ProfilePath -Confirm\n}',
    exceptional: '',
    warning: 'Les dossiers personnels créés contiennent des données des utilisateurs. Remettre les anciens attributs ne les supprime pas, et c\u2019est voulu : on ne détruit pas des données pour annuler un changement de chemin.',
  },
  checks: [
    'Vérifier le partage et les droits avant de pointer des centaines de comptes vers un chemin qui ne répond pas.',
    'Le profil itinérant allonge l\u2019ouverture et la fermeture de session : ne l\u2019activer que si le besoin le justifie.',
    'La redirection de dossiers par GPO est souvent la meilleure réponse — elle relève du lot GPO.',
  ],
  source: 'https://learn.microsoft.com/powershell/module/activedirectory/set-aduser',
  sources: [
    { label: 'Set-ADUser', url: 'https://learn.microsoft.com/powershell/module/activedirectory/set-aduser' },
    { label: 'Get-ADUser', url: 'https://learn.microsoft.com/powershell/module/activedirectory/get-aduser' },
    { label: 'Get-SmbShareAccess', url: 'https://learn.microsoft.com/powershell/module/smbshare/get-smbshareaccess' },
    { label: 'Get-Acl', url: 'https://learn.microsoft.com/powershell/module/microsoft.powershell.security/get-acl' },
  ],
};

/**
 * 6. ad-account-health — l'état des comptes, sans rien toucher.
 *
 * Un annuaire vieillit mal tout seul. Des comptes désactivés qu'on n'a jamais
 * retirés, des mots de passe qui n'expirent pas, des comptes qui ne se
 * connectent plus depuis deux ans : rien de tout cela ne fait de bruit, et
 * tout cela agrandit la surface d'attaque. Cet outil ne fait que regarder, et
 * conclure.
 */
export const outilSanteComptes = {
  id: 'ad-account-health',
  icon: '▤',
  category: 'Active Directory',
  subcategory: 'Cycle de vie des comptes',
  title: 'État de santé des comptes du domaine',
  risk: 'diagnostic',
  summary: 'Recense les comptes désactivés, expirés, verrouillés, inactifs, à mot de passe expiré ou sans expiration, et les comptes privilégiés. N’écrit rien dans l’annuaire.',
  fields: [
    champDomaine(),
    {
      id: 'sanOu', label: 'Limiter à une OU (facultatif)', default: '',
      help: 'Chemin parent/enfant, relatif au domaine. Laisse vide pour examiner tout le domaine.',
    },
    {
      id: 'sanInactif', label: 'Compte considéré inactif après (jours)', default: '90',
      help: 'Calculé sur LastLogonDate, qui est répliqué avec une marge de quelques jours.',
    },
    {
      id: 'sanAgeMotDePasse', label: 'Âge maximal du mot de passe (jours)', default: '365',
      help: 'Seuil de signalement. Aucun mot de passe n’est lu, écrit ni affiché : seule la DATE du dernier changement est relevée.',
    },
    champFormat('sanFormat'),
  ],
  validate(v) {
    const errors = {};
    verifier(errors, 'adDomain', validerFqdn(v.adDomain));
    const ou = String(v.sanOu ?? '').trim();
    if (ou !== '') verifier(errors, 'sanOu', validerCheminOu(ou));
    verifier(errors, 'sanInactif', validerJours(v.sanInactif, { min: 7, max: 3650, label: 'Le seuil d’inactivité' }));
    verifier(errors, 'sanAgeMotDePasse', validerJours(v.sanAgeMotDePasse, { min: 30, max: 3650, label: 'L’âge maximal du mot de passe' }));
    verifier(errors, 'sanFormat', validerFormatRapport(v.sanFormat));
    return errors;
  },
  generate(v) {
    assertValid(this, v);
    const domaine = validerFqdn(v.adDomain).value;
    const ou = String(v.sanOu ?? '').trim();
    const dnRecherche = ou === '' ? domainToDn(domaine) : cheminOuVersDn(validerCheminOu(ou).value, domaine);

    const corps = [
      `$Domaine = ${psB64(domaine)}`,
      `$DnRecherche = ${psB64(dnRecherche)}`,
      `$SeuilInactif = ${validerJours(v.sanInactif, { min: 7, max: 3650 }).value}`,
      `$SeuilMotDePasse = ${validerJours(v.sanAgeMotDePasse, { min: 30, max: 3650 }).value}`,
      `$KjemoFormat = ${psB64(v.sanFormat)}`,
      "",
      blocModuleAd(),
      "",
      '# --- 0. Perimetre ----------------------------------------------------------',
      "[void](Add-KjemoResultat -Categorie 'Perimetre' -Controle 'Racine de recherche' -Etat 'INFO' -Valeur $DnRecherche)",
      '$limiteInactif = (Get-Date).AddDays(-$SeuilInactif)',
      '$limiteMotDePasse = (Get-Date).AddDays(-$SeuilMotDePasse)',
      "",
      '$comptes = @()',
      'if ($moduleOk) {',
      '  try {',
      '    $comptes = @(Get-ADUser -SearchBase $DnRecherche -Filter * -Properties Enabled,LockedOut,AccountExpirationDate,PasswordLastSet,PasswordNeverExpires,PasswordExpired,LastLogonDate,whenCreated,DistinguishedName,Description -ErrorAction Stop)',
      '  } catch {',
      "    [void](Add-KjemoResultat -Categorie 'Perimetre' -Controle 'Lecture des comptes' -Etat 'PROBLEME' -Valeur $_.Exception.Message -Commentaire 'Verifie que la racine de recherche existe et que le compte a le droit de lire cette partie de l''annuaire.')",
      '  }',
      '}',
      "[void](Add-KjemoResultat -Categorie 'Perimetre' -Controle 'Comptes examines' -Etat 'INFO' -Valeur $comptes.Count)",
      "",
      '# --- 1. Comptes desactives -------------------------------------------------',
      '$desactives = @($comptes | Where-Object { $_.Enabled -eq $false })',
      "$etat = 'OK'",
      "if ($desactives.Count -gt 0) { $etat = 'INFO' }",
      "[void](Add-KjemoResultat -Categorie 'Comptes desactives' -Controle 'Total' -Etat $etat -Valeur $desactives.Count -Commentaire 'Un compte desactive garde ses appartenances et son SID : il est inoffensif, mais il encombre.')",
      'foreach ($c in ($desactives | Sort-Object SamAccountName | Select-Object -First 50)) {',
      "  [void](Add-KjemoResultat -Categorie 'Comptes desactives' -Controle $c.SamAccountName -Etat 'INFO' -Valeur $c.DistinguishedName)",
      '}',
      "",
      '# --- 2. Comptes expires ----------------------------------------------------',
      '$expires = @($comptes | Where-Object { $_.AccountExpirationDate -ne $null -and $_.AccountExpirationDate -lt (Get-Date) })',
      "$etat = 'OK'",
      "if ($expires.Count -gt 0) { $etat = 'ATTENTION' }",
      "[void](Add-KjemoResultat -Categorie 'Comptes expires' -Controle 'Total' -Etat $etat -Valeur $expires.Count -Commentaire 'Un compte expire ne peut plus ouvrir de session : c''est souvent la cause d''un appel au support.')",
      'foreach ($c in ($expires | Sort-Object SamAccountName | Select-Object -First 50)) {',
      '  [void](Add-KjemoResultat -Categorie \'Comptes expires\' -Controle $c.SamAccountName -Etat \'ATTENTION\' -Valeur ("expire le " + $c.AccountExpirationDate.ToString(\'yyyy-MM-dd\')))',
      '}',
      "",
      '# --- 3. Comptes verrouilles ------------------------------------------------',
      '$verrouilles = @($comptes | Where-Object { $_.LockedOut -eq $true })',
      "$etat = 'OK'",
      "if ($verrouilles.Count -gt 0) { $etat = 'ATTENTION' }",
      "[void](Add-KjemoResultat -Categorie 'Comptes verrouilles' -Controle 'Total' -Etat $etat -Valeur $verrouilles.Count -Commentaire 'Un verrouillage repete signale soit une erreur de saisie, soit une session ou un service qui rejoue un ancien mot de passe.')",
      'foreach ($c in ($verrouilles | Sort-Object SamAccountName)) {',
      "  [void](Add-KjemoResultat -Categorie 'Comptes verrouilles' -Controle $c.SamAccountName -Etat 'ATTENTION' -Valeur $c.DistinguishedName -Commentaire 'A traiter avec l''outil de recuperation de compte.')",
      '}',
      "",
      '# --- 4. Mots de passe : age et expiration ----------------------------------',
      "# Aucune valeur de mot de passe n'est lue : seules des DATES le sont.",
      '$mdpExpires = @($comptes | Where-Object { $_.Enabled -eq $true -and $_.PasswordExpired -eq $true })',
      "$etat = 'OK'",
      "if ($mdpExpires.Count -gt 0) { $etat = 'ATTENTION' }",
      "[void](Add-KjemoResultat -Categorie 'Mots de passe' -Controle 'Expires' -Etat $etat -Valeur $mdpExpires.Count)",
      '$mdpAnciens = @($comptes | Where-Object { $_.Enabled -eq $true -and $_.PasswordLastSet -ne $null -and $_.PasswordLastSet -lt $limiteMotDePasse })',
      "$etat = 'OK'",
      "if ($mdpAnciens.Count -gt 0) { $etat = 'ATTENTION' }",
      '[void](Add-KjemoResultat -Categorie \'Mots de passe\' -Controle "Non changes depuis plus de $SeuilMotDePasse jours" -Etat $etat -Valeur $mdpAnciens.Count)',
      'foreach ($c in ($mdpAnciens | Sort-Object PasswordLastSet | Select-Object -First 50)) {',
      '  [void](Add-KjemoResultat -Categorie \'Mots de passe\' -Controle $c.SamAccountName -Etat \'ATTENTION\' -Valeur ("dernier changement le " + $c.PasswordLastSet.ToString(\'yyyy-MM-dd\')))',
      '}',
      '$jamaisExpire = @($comptes | Where-Object { $_.Enabled -eq $true -and $_.PasswordNeverExpires -eq $true })',
      "$etat = 'OK'",
      "if ($jamaisExpire.Count -gt 0) { $etat = 'ATTENTION' }",
      "[void](Add-KjemoResultat -Categorie 'Mots de passe' -Controle 'Sans expiration' -Etat $etat -Valeur $jamaisExpire.Count -Commentaire 'Un mot de passe qui n''expire jamais est une exception qui doit etre justifiee compte par compte.')",
      'foreach ($c in ($jamaisExpire | Sort-Object SamAccountName | Select-Object -First 50)) {',
      "  [void](Add-KjemoResultat -Categorie 'Mots de passe' -Controle $c.SamAccountName -Etat 'ATTENTION' -Valeur 'PasswordNeverExpires' -Commentaire $c.DistinguishedName)",
      '}',
      "",
      '# --- 5. Inactivite ---------------------------------------------------------',
      '$inactifs = @($comptes | Where-Object { $_.Enabled -eq $true -and $_.LastLogonDate -ne $null -and $_.LastLogonDate -lt $limiteInactif })',
      "$etat = 'OK'",
      "if ($inactifs.Count -gt 0) { $etat = 'ATTENTION' }",
      '[void](Add-KjemoResultat -Categorie \'Inactivite\' -Controle "Sans connexion depuis plus de $SeuilInactif jours" -Etat $etat -Valeur $inactifs.Count -Commentaire \'LastLogonDate se replique avec un decalage de quelques jours : le chiffre est un ordre de grandeur, pas une preuve.\')',
      'foreach ($c in ($inactifs | Sort-Object LastLogonDate | Select-Object -First 50)) {',
      '  [void](Add-KjemoResultat -Categorie \'Inactivite\' -Controle $c.SamAccountName -Etat \'ATTENTION\' -Valeur ("derniere connexion le " + $c.LastLogonDate.ToString(\'yyyy-MM-dd\')))',
      '}',
      '$jamaisConnectes = @($comptes | Where-Object { $_.Enabled -eq $true -and $_.LastLogonDate -eq $null })',
      "$etat = 'OK'",
      "if ($jamaisConnectes.Count -gt 0) { $etat = 'ATTENTION' }",
      "[void](Add-KjemoResultat -Categorie 'Inactivite' -Controle 'Aucune connexion enregistree' -Etat $etat -Valeur $jamaisConnectes.Count -Commentaire 'Compte cree et jamais utilise, ou connexion jamais repliquee vers ce controleur.')",
      'foreach ($c in ($jamaisConnectes | Sort-Object whenCreated | Select-Object -First 50)) {',
      '  [void](Add-KjemoResultat -Categorie \'Inactivite\' -Controle $c.SamAccountName -Etat \'ATTENTION\' -Valeur ("cree le " + $c.whenCreated.ToString(\'yyyy-MM-dd\')))',
      '}',
      "",
      '# --- 6. Comptes privilegies ------------------------------------------------',
      '# Les groupes sensibles sont designes par leur SID connu, parce que leur nom',
      '# est traduit dans la langue du systeme.',
      "$groupesSensibles = @('512:Admins du domaine', '519:Administrateurs de l''entreprise', '518:Administrateurs du schema', '520:Proprietaires createurs de la strategie de groupe')",
      'if ($moduleOk -and $domaineObj) {',
      '  foreach ($entree in $groupesSensibles) {',
      "    $rid = $entree.Split(':')[0]",
      "    $libelle = $entree.Split(':')[1]",
      '    $sid = "$($domaineObj.DomainSID.Value)-$rid"',
      '    $grp = $null',
      '    try { $grp = Get-ADGroup -Identity $sid -ErrorAction SilentlyContinue } catch { }',
      '    if (-not $grp) {',
      "      [void](Add-KjemoResultat -Categorie 'Comptes privilegies' -Controle $libelle -Etat 'INFO' -Valeur 'groupe absent de ce domaine')",
      '      continue',
      '    }',
      '    $membres = @()',
      '    try { $membres = @(Get-ADGroupMember -Identity $grp -Recursive -ErrorAction SilentlyContinue) } catch { }',
      "    $etatG = 'OK'",
      "    if ($membres.Count -gt 5) { $etatG = 'ATTENTION' }",
      "    [void](Add-KjemoResultat -Categorie 'Comptes privilegies' -Controle $libelle -Etat $etatG -Valeur (\"$($membres.Count) membre(s) : \" + (($membres | ForEach-Object { $_.SamAccountName }) -join ', ')) -Commentaire 'Chaque membre d''un groupe d''administration doit correspondre a une personne identifiee et a un besoin courant.')",
      '  }',
      '}',
      "",
      '# --- 7. Conclusion ---------------------------------------------------------',
      '$aExaminer = $expires.Count + $verrouilles.Count + $mdpExpires.Count + $mdpAnciens.Count + $jamaisExpire.Count + $inactifs.Count',
      'if ($aExaminer -eq 0) {',
      "  [void](Add-KjemoResultat -Categorie 'Conclusion' -Controle 'Synthese' -Etat 'OK' -Valeur 'aucun point signale par les seuils demandes')",
      '} else {',
      "  [void](Add-KjemoResultat -Categorie 'Conclusion' -Controle 'Synthese' -Etat 'ATTENTION' -Valeur \"$aExaminer signalement(s)\" -Commentaire 'Ce rapport ne decide rien : il donne la liste a examiner. Aucun compte n''est modifie ni supprime.')",
      '}',
    ];

    return assembler({
      titre: 'Etat de sante des comptes Active Directory',
      outil: 'ad-account-health',
      diagnostic: true,
      admin: false,
      parametres: [
        ['Domaine', psB64(domaine)],
        ['RacineRecherche', psB64(dnRecherche)],
        ['SeuilInactifJours', psB64(String(v.sanInactif))],
        ['Format', psB64(v.sanFormat)],
      ],
      corps,
      prefixeFichier: 'kjemo-ad-sante-comptes',
    });
  },
  gui: [
    'Utilisateurs et ordinateurs Active Directory (dsa.msc) > Requêtes enregistrées.',
    'Clic droit > Nouveau > Requête : « Comptes d’utilisateurs désactivés », « Comptes dont le mot de passe n’expire jamais », « Jours depuis la dernière ouverture de session ».',
    'Centre d’administration Active Directory (dsac.exe) > Recherche globale, pour un filtre ponctuel.',
    'Ces vues affichent la même information ; elles ne produisent pas de rapport exportable.',
  ],
  keywords: [
    'compte desactive', 'compte verrouille', 'compte expire', 'mot de passe expire',
    'passwordneverexpires', 'lastlogondate', 'compte inactif', 'audit des comptes',
    'admins du domaine', 'comptes privilegies', 'menage annuaire',
  ],
  requiresAdmin: false,
  os: OS_AD,
  prereqs: PREREQS_AD.concat([
    'Droit de lecture sur la partie de l’annuaire examinée. La lecture des attributs listés est accordée par défaut aux utilisateurs authentifiés.',
    'Aucun droit d’écriture n’est nécessaire : ce script ne modifie rien.',
  ]),
  commonErrors: ERREURS_AD.concat([
    {
      message: 'Get-ADUser : Directory object not found',
      cause: 'La racine de recherche indiquée n’existe pas — souvent une OU mal orthographiée.',
      fix: 'Vérifier le chemin d’OU, ou laisser le champ vide pour examiner tout le domaine.',
    },
    {
      message: 'LastLogonDate est vide alors que l’utilisateur travaille tous les jours',
      cause: 'LastLogonTimeStamp n’est répliqué qu’après un décalage, réglé par défaut à 14 jours moins un aléa.',
      fix: 'Traiter ce chiffre comme un ordre de grandeur. Pour une valeur exacte, interroger chaque contrôleur sur lastLogon, attribut non répliqué.',
    },
    {
      message: 'Le nombre de comptes examinés est nettement inférieur à l’effectif réel',
      cause: 'La recherche a été limitée à une OU, ou le compte utilisé ne voit pas tout l’annuaire.',
      fix: 'Vider le champ d’OU, et vérifier les droits de lecture du compte utilisé.',
    },
  ]),
  reversible: true,
  verifyAfter: [
    'Le rapport annonce le nombre de comptes examinés : il doit correspondre au périmètre attendu.',
    'Chaque catégorie porte un total, puis le détail des comptes concernés.',
    'Aucun mot de passe, jeton ni identifiant ne figure dans le rapport : seules des dates et des noms de comptes.',
  ],
  rollback: {
    summary: 'Ce script ne modifie rien. Il n’y a donc rien à annuler : le seul effet possible est le fichier de rapport écrit sur le Bureau.',
    diagnostic: '# Retrouver les rapports produits par cet outil.\nGet-ChildItem -Path ([Environment]::GetFolderPath(\'Desktop\')) -Filter \'kjemo-ad-sante-comptes*\' | Format-Table Name,Length,LastWriteTime',
    command: '# Retirer un rapport devenu inutile. -Confirm demande l\'accord avant chaque fichier.\nGet-ChildItem -Path ([Environment]::GetFolderPath(\'Desktop\')) -Filter \'kjemo-ad-sante-comptes*\' | Remove-Item -Confirm',
    warning: 'Le rapport liste des noms de comptes et des dates. Il n’est pas secret, mais il décrit la surface d’attaque du domaine : il se range comme un document interne.',
  },
  checks: [
    'Choisir le périmètre : tout le domaine, ou une OU.',
    'Les seuils par défaut (90 jours d’inactivité, 365 jours de mot de passe) sont un point de départ, pas une norme.',
    'Rien n’est modifié : ce rapport sert à décider, pas à agir.',
  ],
  source: 'https://learn.microsoft.com/powershell/module/activedirectory/get-aduser',
  sources: [
    { label: 'Get-ADUser', url: 'https://learn.microsoft.com/powershell/module/activedirectory/get-aduser' },
    { label: 'Get-ADGroupMember', url: 'https://learn.microsoft.com/powershell/module/activedirectory/get-adgroupmember' },
    { label: 'Identificateurs de sécurité connus (SID)', url: 'https://learn.microsoft.com/windows-server/identity/ad-ds/manage/understand-security-identifiers' },
  ],
};

/**
 * 7. ad-account-recovery — remettre un compte en service, sans secret dans le
 *    navigateur.
 *
 * Trois causes possibles à « je n'arrive plus à me connecter » : le compte est
 * verrouillé, il est désactivé, il est expiré. Ce sont trois états distincts,
 * avec trois corrections distinctes. Le script commence donc par établir
 * lequel, avant de proposer quoi que ce soit.
 *
 * Le mot de passe, lui, ne traverse jamais le site : quand la réinitialisation
 * est demandée, le script l'obtient localement, avec Read-Host -AsSecureString,
 * au moment de son exécution.
 */
export const outilRecuperationCompte = {
  id: 'ad-account-recovery',
  icon: '↺',
  category: 'Active Directory',
  subcategory: 'Cycle de vie des comptes',
  title: 'Débloquer et remettre en service un compte',
  risk: 'caution',
  summary: 'Établit pourquoi un compte ne peut plus ouvrir de session — verrouillé, désactivé, expiré, mot de passe expiré — puis corrige ce qui a été demandé. Aucun mot de passe ne transite par le site.',
  fields: [
    champDomaine(),
    {
      id: 'recCompte', label: 'Compte concerné', default: 'm.tremblay',
      help: 'SamAccountName (m.tremblay) ou nom d’ouverture de session complet (m.tremblay@hopitalbn.lan).',
    },
    {
      id: 'recAction', label: 'Correction souhaitée', type: 'select', default: 'Aucune',
      options: [
        ['Aucune', 'Aucune — diagnostic seulement'],
        ['Deverrouiller', 'Déverrouiller le compte'],
        ['Reactiver', 'Réactiver le compte désactivé'],
        ['ProlongerExpiration', 'Retirer la date d’expiration du compte'],
        ['ReinitialiserIdentifiant', 'Réinitialiser l’identifiant d’accès — saisi localement à l’exécution'],
      ],
      help: 'La réinitialisation ne place aucune valeur dans ce formulaire : le script la demande à la console, en saisie masquée, au moment où il s’exécute.',
    },
    {
      id: 'recChangementObligatoire', label: 'Exiger un changement à la prochaine ouverture de session',
      type: 'select', default: 'Oui',
      options: [['Oui', 'Oui — recommandé après une réinitialisation'], ['Non', 'Non']],
      help: 'S’applique uniquement à la réinitialisation. Un identifiant remis par un tiers ne doit pas rester en service.',
    },
    champMode(),
  ],
  validate(v) {
    const errors = {};
    verifier(errors, 'adDomain', validerFqdn(v.adDomain));
    const compte = String(v.recCompte ?? '').trim();
    if (compte.includes('@')) verifier(errors, 'recCompte', validerUpn(compte));
    else verifier(errors, 'recCompte', validateSamAccountName(compte));
    verifier(errors, 'recAction', validerChoix(v.recAction,
      ['Aucune', 'Deverrouiller', 'Reactiver', 'ProlongerExpiration', 'ReinitialiserIdentifiant'], 'La correction'));
    verifier(errors, 'recChangementObligatoire', validerChoix(v.recChangementObligatoire, ['Oui', 'Non'], 'Le choix'));
    verifier(errors, 'mode', validerModeExecution(v.mode));
    return errors;
  },
  generate(v) {
    assertValid(this, v);
    const domaine = validerFqdn(v.adDomain).value;
    const compte = String(v.recCompte).trim();

    const corps = [
      `$Mode = ${psB64(v.mode)}`,
      `$Domaine = ${psB64(domaine)}`,
      `$Compte = ${psB64(compte)}`,
      `$Action = ${psB64(v.recAction)}`,
      `$ChangementObligatoire = ${psB64(v.recChangementObligatoire)}`,
      "$KjemoFormat = 'Console'",
      "",
      blocModuleAd(),
      "",
      '# --- 1. Retrouver le compte ------------------------------------------------',
      '$user = $null',
      'if ($moduleOk) {',
      '  try {',
      '    $user = Get-ADUser -Identity $Compte -Properties Enabled,LockedOut,AccountExpirationDate,PasswordLastSet,PasswordExpired,PasswordNeverExpires,LastLogonDate,LastBadPasswordAttempt,BadLogonCount,DistinguishedName,UserPrincipalName -ErrorAction Stop',
      '  } catch {',
      '    try {',
      '      $user = Get-ADUser -Filter "UserPrincipalName -eq \'$Compte\'" -Properties Enabled,LockedOut,AccountExpirationDate,PasswordLastSet,PasswordExpired,PasswordNeverExpires,LastLogonDate,LastBadPasswordAttempt,BadLogonCount,DistinguishedName,UserPrincipalName -ErrorAction SilentlyContinue',
      '    } catch { }',
      '  }',
      '}',
      'if (-not $user) {',
      "  [void](Add-KjemoResultat -Categorie 'Compte' -Controle 'Recherche' -Etat 'PROBLEME' -Valeur $Compte -Commentaire 'Compte introuvable dans ce domaine. Verifie l''orthographe, ou cherche-le avec l''outil de recherche et deplacement d''objets.')",
      '} else {',
      "  [void](Add-KjemoResultat -Categorie 'Compte' -Controle 'Identite' -Etat 'INFO' -Valeur $user.DistinguishedName -Commentaire $user.UserPrincipalName)",
      '}',
      "",
      '# --- 2. Pourquoi la connexion echoue ---------------------------------------',
      '$causes = New-Object System.Collections.ArrayList',
      'if ($user) {',
      '  if ($user.LockedOut -eq $true) {',
      '    [void]$causes.Add(\'Verrouille\')',
      "    [void](Add-KjemoResultat -Categorie 'Diagnostic' -Controle 'Verrouillage' -Etat 'PROBLEME' -Valeur 'compte verrouille' -Commentaire 'Le verrouillage vient de la strategie de mot de passe du domaine : trop de tentatives echouees.')",
      '  } else {',
      "    [void](Add-KjemoResultat -Categorie 'Diagnostic' -Controle 'Verrouillage' -Etat 'OK' -Valeur 'non verrouille')",
      '  }',
      '  if ($user.Enabled -eq $false) {',
      '    [void]$causes.Add(\'Desactive\')',
      "    [void](Add-KjemoResultat -Categorie 'Diagnostic' -Controle 'Activation' -Etat 'PROBLEME' -Valeur 'compte desactive' -Commentaire 'Un compte desactive a souvent ete ferme volontairement : verifie la raison avant de le rouvrir.')",
      '  } else {',
      "    [void](Add-KjemoResultat -Categorie 'Diagnostic' -Controle 'Activation' -Etat 'OK' -Valeur 'actif')",
      '  }',
      '  if ($user.AccountExpirationDate -ne $null -and $user.AccountExpirationDate -lt (Get-Date)) {',
      '    [void]$causes.Add(\'Expire\')',
      '    [void](Add-KjemoResultat -Categorie \'Diagnostic\' -Controle \'Expiration du compte\' -Etat \'PROBLEME\' -Valeur ("expire le " + $user.AccountExpirationDate.ToString(\'yyyy-MM-dd\')) -Commentaire \'Frequent pour les comptes de stagiaires et de remplacants.\')',
      '  } elseif ($user.AccountExpirationDate -ne $null) {',
      '    [void](Add-KjemoResultat -Categorie \'Diagnostic\' -Controle \'Expiration du compte\' -Etat \'ATTENTION\' -Valeur ("expirera le " + $user.AccountExpirationDate.ToString(\'yyyy-MM-dd\')))',
      '  } else {',
      "    [void](Add-KjemoResultat -Categorie 'Diagnostic' -Controle 'Expiration du compte' -Etat 'OK' -Valeur 'aucune date d''expiration')",
      '  }',
      '  if ($user.PasswordExpired -eq $true) {',
      '    [void]$causes.Add(\'IdentifiantExpire\')',
      "    [void](Add-KjemoResultat -Categorie 'Diagnostic' -Controle 'Identifiant d''acces' -Etat 'PROBLEME' -Valeur 'expire' -Commentaire 'L''utilisateur peut le renouveler lui-meme a l''ouverture de session, si la strategie le permet.')",
      '  } else {',
      "    [void](Add-KjemoResultat -Categorie 'Diagnostic' -Controle 'Identifiant d''acces' -Etat 'OK' -Valeur 'valide')",
      '  }',
      '  if ($user.PasswordLastSet -ne $null) {',
      '    [void](Add-KjemoResultat -Categorie \'Diagnostic\' -Controle \'Dernier renouvellement\' -Etat \'INFO\' -Valeur $user.PasswordLastSet.ToString(\'yyyy-MM-dd HH:mm\'))',
      '  }',
      '  if ($user.LastBadPasswordAttempt -ne $null) {',
      '    [void](Add-KjemoResultat -Categorie \'Diagnostic\' -Controle \'Derniere tentative echouee\' -Etat \'INFO\' -Valeur $user.LastBadPasswordAttempt.ToString(\'yyyy-MM-dd HH:mm\') -Commentaire "Tentatives echouees comptees sur ce controleur : $($user.BadLogonCount). Un chiffre qui remonte tout seul designe une session ou un service qui rejoue un ancien identifiant.")',
      '  }',
      '  if ($causes.Count -eq 0) {',
      "    [void](Add-KjemoResultat -Categorie 'Diagnostic' -Controle 'Synthese' -Etat 'OK' -Valeur 'aucun blocage cote annuaire' -Commentaire 'Si la connexion echoue quand meme, cherche du cote du poste : heure, DNS, canal securise, ou restriction d''horaire.')",
      '  } else {',
      '    [void](Add-KjemoResultat -Categorie \'Diagnostic\' -Controle \'Synthese\' -Etat \'PROBLEME\' -Valeur ($causes -join \', \'))',
      '  }',
      '}',
      "",
      '# --- 3. Correction, uniquement en mode Appliquer ---------------------------',
      "# Le mot de passe n'est jamais ecrit dans ce script. Quand la",
      "# reinitialisation est demandee, il est saisi ici, masque, et reste en",
      "# memoire le temps de l'appel.",
      "if ($Mode -eq 'Appliquer' -and $user) {",
      "  if ($Action -eq 'Deverrouiller') {",
      '    try {',
      '      Unlock-ADAccount -Identity $user.DistinguishedName -ErrorAction Stop',
      "      [void](Add-KjemoResultat -Categorie 'Action' -Controle 'Deverrouillage' -Etat 'OK' -Valeur 'effectue')",
      "    } catch { [void](Add-KjemoResultat -Categorie 'Action' -Controle 'Deverrouillage' -Etat 'PROBLEME' -Valeur $_.Exception.Message) }",
      "  } elseif ($Action -eq 'Reactiver') {",
      '    try {',
      '      Enable-ADAccount -Identity $user.DistinguishedName -ErrorAction Stop',
      "      [void](Add-KjemoResultat -Categorie 'Action' -Controle 'Reactivation' -Etat 'OK' -Valeur 'effectue')",
      "    } catch { [void](Add-KjemoResultat -Categorie 'Action' -Controle 'Reactivation' -Etat 'PROBLEME' -Valeur $_.Exception.Message) }",
      "  } elseif ($Action -eq 'ProlongerExpiration') {",
      '    try {',
      '      Clear-ADAccountExpiration -Identity $user.DistinguishedName -ErrorAction Stop',
      "      [void](Add-KjemoResultat -Categorie 'Action' -Controle 'Date d''expiration' -Etat 'OK' -Valeur 'retiree')",
      "    } catch { [void](Add-KjemoResultat -Categorie 'Action' -Controle 'Date d''expiration' -Etat 'PROBLEME' -Valeur $_.Exception.Message) }",
      "  } elseif ($Action -eq 'ReinitialiserIdentifiant') {",
      "    Write-Host ''",
      "    Write-Host '--- Saisie locale ---'",
      "    Write-Host 'La valeur saisie ci-dessous ne provient pas du site, n''y retourne pas,'",
      "    Write-Host 'et ne figure dans aucun rapport. Elle est masquee a la frappe.'",
      "    $nouvelleValeur = Read-Host -Prompt 'Nouvel identifiant d''acces pour ce compte' -AsSecureString",
      '    try {',
      '      Set-ADAccountPassword -Identity $user.DistinguishedName -Reset -NewPassword $nouvelleValeur -ErrorAction Stop',
      "      [void](Add-KjemoResultat -Categorie 'Action' -Controle 'Reinitialisation' -Etat 'OK' -Valeur 'effectuee' -Commentaire 'La valeur n''est pas journalisee.')",
      "      if ($ChangementObligatoire -eq 'Oui') {",
      '        Set-ADUser -Identity $user.DistinguishedName -ChangePasswordAtLogon $true -ErrorAction Stop',
      "        [void](Add-KjemoResultat -Categorie 'Action' -Controle 'Changement a la prochaine session' -Etat 'OK' -Valeur 'exige')",
      '      }',
      '    } catch {',
      "      [void](Add-KjemoResultat -Categorie 'Action' -Controle 'Reinitialisation' -Etat 'PROBLEME' -Valeur $_.Exception.Message -Commentaire 'Souvent : la valeur ne respecte pas la strategie de mot de passe du domaine, ou le compte n''a pas le droit de reinitialiser.')",
      '    }',
      '    $nouvelleValeur = $null',
      '    [System.GC]::Collect()',
      '  } else {',
      "    [void](Add-KjemoResultat -Categorie 'Action' -Controle 'Correction' -Etat 'IGNORE' -Valeur 'aucune correction demandee')",
      '  }',
      "",
      '  # --- 4. Relecture apres correction ---------------------------------------',
      '  $apres = $null',
      '  try { $apres = Get-ADUser -Identity $user.DistinguishedName -Properties Enabled,LockedOut,AccountExpirationDate,PasswordExpired -ErrorAction SilentlyContinue } catch { }',
      '  if ($apres) {',
      '    [void](Add-KjemoResultat -Categorie \'Verification\' -Controle \'Etat apres correction\' -Etat \'INFO\' -Valeur ("actif : $($apres.Enabled) ; verrouille : $($apres.LockedOut) ; identifiant expire : $($apres.PasswordExpired)"))',
      '  }',
      '} else {',
      "  Write-Host ''",
      "  Write-Host '--- Simulation ---'",
      "  if ($user -and $Action -ne 'Aucune') {",
      "    [void](Add-KjemoResultat -Categorie 'Action' -Controle 'Simulation' -Etat 'INFO' -Valeur \"correction prevue : $Action\" -Commentaire 'Rien n''a ete modifie. Choisis le mode Appliquer pour l''executer.')",
      '  }',
      '}',
      blocModeDiagnostic(),
      blocAucuneSuppression('compte'),
    ];

    return assembler({
      titre: 'Debloquer et remettre en service un compte',
      outil: 'ad-account-recovery',
      diagnostic: v.mode !== 'Appliquer',
      admin: true,
      parametres: [
        ['Domaine', psB64(domaine)],
        ['Compte', psB64(compte)],
        ['Correction', psB64(v.recAction)],
        ['Mode', psB64(v.mode)],
      ],
      corps,
      prefixeFichier: 'kjemo-ad-recuperation-compte',
      formats: 'Console',
    });
  },
  gui: [
    'Utilisateurs et ordinateurs Active Directory (dsa.msc) > localiser le compte.',
    'Onglet Compte : la case « Le compte est verrouillé » se décoche pour déverrouiller.',
    'Clic droit > Activer le compte, pour un compte désactivé.',
    'Onglet Compte > Expire : choisir « Jamais » pour retirer la date d’expiration.',
    'Clic droit > Réinitialiser le mot de passe : la saisie se fait dans la console, jamais dans un courriel ni un message.',
  ],
  keywords: [
    'compte verrouille', 'unlock-adaccount', 'compte desactive', 'enable-adaccount',
    'mot de passe oublie', 'reinitialiser', 'set-adaccountpassword', 'changepasswordatlogon',
    'compte expire', 'impossible de se connecter', 'badlogoncount', 'deblocage',
  ],
  requiresAdmin: true,
  os: OS_AD,
  prereqs: PREREQS_AD.concat([
    'Droit de réinitialiser les mots de passe et de déverrouiller les comptes sur l’OU concernée — c’est la délégation la plus courante d’un poste de support.',
    'Une procédure d’identification de la personne avant toute remise en service. Un identifiant remis à la mauvaise personne est une intrusion, pas un dépannage.',
    'Aucun identifiant n’est saisi dans le site : la réinitialisation se fait à la console, en saisie masquée.',
  ]),
  commonErrors: ERREURS_AD.concat([
    {
      message: 'Set-ADAccountPassword : The password does not meet the length, complexity, or history requirement of the domain',
      code: '0x800708C5',
      cause: 'La valeur saisie ne respecte pas la stratégie de mot de passe du domaine.',
      fix: 'Consulter la stratégie appliquée, puis saisir une valeur conforme.',
      command: 'Get-ADDefaultDomainPasswordPolicy | Format-List MinPasswordLength,PasswordHistoryCount,ComplexityEnabled,LockoutThreshold,LockoutDuration',
    },
    {
      message: 'Le compte se reverrouille quelques minutes après chaque déverrouillage',
      cause: 'Une session ouverte, un service, une tâche planifiée ou un téléphone rejoue l’ancien identifiant.',
      fix: 'Identifier la source avec les événements 4740 du journal Sécurité du contrôleur qui détient le rôle Émulateur PDC, puis corriger cette source.',
      command: 'Get-WinEvent -FilterHashtable @{LogName=\'Security\'; Id=4740} -MaxEvents 20 | Format-List TimeCreated,Message',
    },
    {
      message: 'Unlock-ADAccount : Access is denied',
      code: '0x5',
      cause: 'Le compte utilisé n’a pas la délégation de déverrouillage sur cette OU.',
      fix: 'Vérifier la délégation avec l’outil d’audit de délégation, et utiliser un compte qui la détient.',
    },
    {
      message: 'La connexion échoue toujours alors que le compte est sain',
      cause: 'Le blocage n’est pas dans l’annuaire : heure du poste décalée, canal sécurisé rompu, restriction d’horaire ou de poste, ou DNS.',
      fix: 'Passer à l’outil de vérification du canal sécurisé, puis au rapport complet du domaine.',
    },
  ]),
  reversible: true,
  verifyAfter: [
    'Le script relit le compte après correction et affiche activation, verrouillage et expiration de l’identifiant.',
    'L’utilisateur ouvre une session : c’est la seule vérification qui conclut vraiment.',
    'Aucun identifiant ne figure dans la console, dans le rapport ni dans un fichier.',
  ],
  rollback: {
    summary: 'Déverrouiller et réactiver sont des gestes réversibles : on peut redésactiver un compte rouvert par erreur. Une réinitialisation, elle, ne se défait pas : l’ancienne valeur n’existe plus nulle part. Elle se corrige par une nouvelle réinitialisation, avec la personne concernée.',
    diagnostic: '# Constater l\'etat courant du compte avant de revenir en arriere.\nGet-ADUser -Identity \'<compte>\' -Properties Enabled,LockedOut,AccountExpirationDate,PasswordLastSet,whenChanged | Format-List SamAccountName,Enabled,LockedOut,AccountExpirationDate,PasswordLastSet,whenChanged',
    command: '# Refermer un compte rouvert par erreur. Le compte reste en place : seule\n# l\'ouverture de session est refusee.\nDisable-ADAccount -Identity \'<compte>\' -Confirm\n\n# Remettre une date d\'expiration retiree par erreur.\nSet-ADAccountExpiration -Identity \'<compte>\' -DateTime \'2026-12-31\'',
    warning: 'Une réinitialisation d’identifiant est irréversible : l’ancienne valeur n’est stockée nulle part en clair. Elle invalide aussi les secrets dérivés — connexions enregistrées, sessions de messagerie, comptes de service qui réutilisaient cet identifiant.',
  },
  checks: [
    'Identifier la personne avant tout : un déblocage est aussi un contrôle d’accès.',
    'Lire le diagnostic avant de choisir la correction — verrouillé, désactivé et expiré ne se soignent pas pareil.',
    'La réinitialisation se saisit à la console, jamais dans un courriel, un message ni ce formulaire.',
  ],
  source: 'https://learn.microsoft.com/powershell/module/activedirectory/unlock-adaccount',
  sources: [
    { label: 'Unlock-ADAccount', url: 'https://learn.microsoft.com/powershell/module/activedirectory/unlock-adaccount' },
    { label: 'Enable-ADAccount', url: 'https://learn.microsoft.com/powershell/module/activedirectory/enable-adaccount' },
    { label: 'Set-ADAccountPassword', url: 'https://learn.microsoft.com/powershell/module/activedirectory/set-adaccountpassword' },
    { label: 'Clear-ADAccountExpiration', url: 'https://learn.microsoft.com/powershell/module/activedirectory/clear-adaccountexpiration' },
    { label: 'Read-Host', url: 'https://learn.microsoft.com/powershell/module/microsoft.powershell.utility/read-host' },
  ],
};

/**
 * 8. ad-object-search-move — retrouver un objet, puis le déplacer.
 *
 * Déplacer un objet dans l'annuaire n'est pas anodin : l'OU de destination
 * décide des stratégies de groupe qui s'appliqueront, et de qui a délégation
 * dessus. Le script commence donc par montrer où l'objet est, où il irait, et
 * ce que le déplacement change.
 */
export const outilRechercheDeplacement = {
  id: 'ad-object-search-move',
  icon: '⌕',
  category: 'Active Directory',
  subcategory: 'Maintenance des objets',
  title: 'Rechercher et déplacer un objet Active Directory',
  risk: 'caution',
  summary: 'Retrouve un utilisateur, un groupe, un ordinateur ou une OU par son nom, son SamAccountName, son UPN ou son nom distinctif, montre sa place actuelle, et le déplace vers une autre OU après vérification.',
  fields: [
    champDomaine(),
    {
      id: 'objType', label: 'Type d’objet', type: 'select', default: 'Utilisateur',
      options: [
        ['Utilisateur', 'Utilisateur'],
        ['Groupe', 'Groupe'],
        ['Ordinateur', 'Ordinateur'],
        ['UniteOrganisationnelle', 'Unité d’organisation'],
        ['Tous', 'Tous les types'],
      ],
    },
    {
      id: 'objRecherche', label: 'Objet recherché', default: 'm.tremblay',
      help: 'Nom affiché, SamAccountName, nom d’ouverture de session complet, ou nom distinctif complet.',
    },
    {
      id: 'objOuCible', label: 'OU de destination', default: 'Médecin/Spécialiste',
      help: 'Chemin parent/enfant, relatif au domaine. Le déplacement change les stratégies de groupe appliquées et les délégations en vigueur.',
    },
    champMode(),
  ],
  validate(v) {
    const errors = {};
    verifier(errors, 'adDomain', validerFqdn(v.adDomain));
    verifier(errors, 'objType', validerChoix(v.objType,
      ['Utilisateur', 'Groupe', 'Ordinateur', 'UniteOrganisationnelle', 'Tous'], 'Le type d’objet'));
    const recherche = String(v.objRecherche ?? '').trim();
    if (recherche === '') errors.objRecherche = 'Indique l’objet à retrouver.';
    else if (recherche.length > 256) errors.objRecherche = 'La recherche ne doit pas dépasser 256 caractères.';
    else if (/[*?]/.test(recherche)) errors.objRecherche = 'Les caractères génériques * et ? ne sont pas acceptés : cet outil vise un objet précis.';
    else if (/=/.test(recherche)) verifier(errors, 'objRecherche', validerDn(recherche));
    verifier(errors, 'objOuCible', validerCheminOu(v.objOuCible));
    verifier(errors, 'mode', validerModeExecution(v.mode));
    return errors;
  },
  generate(v) {
    assertValid(this, v);
    const domaine = validerFqdn(v.adDomain).value;
    const recherche = String(v.objRecherche).trim();
    const dnCible = cheminOuVersDn(validerCheminOu(v.objOuCible).value, domaine);
    const classes = {
      Utilisateur: 'user',
      Groupe: 'group',
      Ordinateur: 'computer',
      UniteOrganisationnelle: 'organizationalUnit',
      Tous: '*',
    };

    const corps = [
      `$Mode = ${psB64(v.mode)}`,
      `$Domaine = ${psB64(domaine)}`,
      `$Recherche = ${psB64(recherche)}`,
      `$DnCible = ${psB64(dnCible)}`,
      `$ClasseVoulue = ${psB64(classes[v.objType])}`,
      "$KjemoFormat = 'Console'",
      "",
      blocModuleAd(),
      "",
      '# --- 1. Retrouver l\'objet ---------------------------------------------------',
      "# Quatre facons de designer le meme objet : nom distinctif, SamAccountName,",
      '# nom d\'ouverture de session complet, nom affiche. On les essaie dans cet',
      '# ordre, du plus precis au plus large.',
      '$trouves = @()',
      'if ($moduleOk) {',
      "  if ($Recherche -like '*=*') {",
      '    try { $trouves = @(Get-ADObject -Identity $Recherche -Properties DistinguishedName,ObjectClass,Name,whenChanged -ErrorAction SilentlyContinue) } catch { }',
      '  }',
      '  if ($trouves.Count -eq 0) {',
      '    $filtre = "SamAccountName -eq \'$Recherche\' -or UserPrincipalName -eq \'$Recherche\' -or Name -eq \'$Recherche\'"',
      '    try { $trouves = @(Get-ADObject -Filter $filtre -Properties DistinguishedName,ObjectClass,Name,SamAccountName,whenChanged -ErrorAction SilentlyContinue) } catch { }',
      '  }',
      "  if ($trouves.Count -eq 0 -and $Recherche -notlike '*$') {",
      '    $filtreOrdi = "Name -eq \'$Recherche`$\' -or SamAccountName -eq \'$Recherche`$\'"',
      '    try { $trouves = @(Get-ADObject -Filter $filtreOrdi -Properties DistinguishedName,ObjectClass,Name,whenChanged -ErrorAction SilentlyContinue) } catch { }',
      '  }',
      '}',
      "if ($ClasseVoulue -ne '*') {",
      '  $trouves = @($trouves | Where-Object { $_.ObjectClass -eq $ClasseVoulue })',
      '}',
      "",
      'if ($trouves.Count -eq 0) {',
      "  [void](Add-KjemoResultat -Categorie 'Recherche' -Controle $Recherche -Etat 'PROBLEME' -Valeur 'aucun objet trouve' -Commentaire 'Verifie l''orthographe, le type demande, et les droits de lecture du compte utilise.')",
      '} elseif ($trouves.Count -gt 1) {',
      '  [void](Add-KjemoResultat -Categorie \'Recherche\' -Controle $Recherche -Etat \'ATTENTION\' -Valeur "$($trouves.Count) objets correspondent" -Commentaire \'Plusieurs objets portent ce nom. Reprends la recherche avec le nom distinctif complet : aucun deplacement ne sera tente.\')',
      '  foreach ($o in $trouves) {',
      "    [void](Add-KjemoResultat -Categorie 'Recherche' -Controle $o.ObjectClass -Etat 'INFO' -Valeur $o.DistinguishedName)",
      '  }',
      '} else {',
      "  [void](Add-KjemoResultat -Categorie 'Recherche' -Controle 'Objet trouve' -Etat 'OK' -Valeur $trouves[0].DistinguishedName -Commentaire $trouves[0].ObjectClass)",
      '}',
      "",
      '# --- 2. L\'OU de destination existe-t-elle ? ---------------------------------',
      '$ouOk = $false',
      'if ($moduleOk) {',
      '  try { if (Get-ADOrganizationalUnit -Identity $DnCible -ErrorAction SilentlyContinue) { $ouOk = $true } } catch { }',
      '}',
      'if ($ouOk) {',
      "  [void](Add-KjemoResultat -Categorie 'Destination' -Controle 'OU cible' -Etat 'OK' -Valeur $DnCible)",
      '} else {',
      "  [void](Add-KjemoResultat -Categorie 'Destination' -Controle 'OU cible' -Etat 'PROBLEME' -Valeur $DnCible -Commentaire 'OU introuvable. Cree-la d''abord avec l''outil de hierarchie d''OU.')",
      '}',
      "",
      '# --- 3. Le deplacement a-t-il un sens ? -------------------------------------',
      '$objet = $null',
      '$dnAvant = $null',
      '$deplacementUtile = $false',
      'if ($trouves.Count -eq 1) {',
      '  $objet = $trouves[0]',
      '  $dnAvant = $objet.DistinguishedName',
      "  $parentActuel = ($dnAvant -split '(?<!\\\\),', 2)[1]",
      '  if ($parentActuel -eq $DnCible) {',
      "    [void](Add-KjemoResultat -Categorie 'Deplacement' -Controle 'Necessite' -Etat 'IGNORE' -Valeur 'l''objet est deja dans l''OU demandee' -Commentaire 'Rien a faire : aucun deplacement ne sera tente.')",
      '  } elseif ($DnCible.EndsWith($dnAvant)) {',
      "    [void](Add-KjemoResultat -Categorie 'Deplacement' -Controle 'Necessite' -Etat 'PROBLEME' -Valeur 'destination situee sous l''objet lui-meme' -Commentaire 'Deplacer une OU dans sa propre descendance est impossible.')",
      '  } else {',
      '    $deplacementUtile = $true',
      '    [void](Add-KjemoResultat -Categorie \'Deplacement\' -Controle \'Trajet\' -Etat \'INFO\' -Valeur ("$parentActuel  ->  $DnCible"))',
      "    [void](Add-KjemoResultat -Categorie 'Deplacement' -Controle 'Consequences' -Etat 'ATTENTION' -Valeur 'strategies de groupe et delegations' -Commentaire 'Apres deplacement, l''objet recoit les GPO liees a la nouvelle OU et perd celles de l''ancienne. Les delegations posees sur l''ancienne OU ne le suivent pas.')",
      "    [void](Add-KjemoResultat -Categorie 'Annulation' -Controle 'Emplacement d''origine' -Etat 'INFO' -Valeur $parentActuel -Commentaire 'Note ce nom distinctif : c''est lui qui permet de revenir en arriere.')",
      '  }',
      '}',
      "",
      '# --- 4. Deplacement, uniquement en mode Appliquer ---------------------------',
      "if ($Mode -eq 'Appliquer' -and $deplacementUtile -and $ouOk) {",
      '  try {',
      '    Move-ADObject -Identity $dnAvant -TargetPath $DnCible -ErrorAction Stop',
      "    [void](Add-KjemoResultat -Categorie 'Action' -Controle 'Deplacement' -Etat 'OK' -Valeur 'effectue')",
      '  } catch {',
      "    [void](Add-KjemoResultat -Categorie 'Action' -Controle 'Deplacement' -Etat 'PROBLEME' -Valeur $_.Exception.Message -Commentaire 'Objet protege contre la suppression accidentelle, ou droits insuffisants sur l''une des deux OU.')",
      '  }',
      '  $apres = $null',
      '  try { $apres = Get-ADObject -Filter "ObjectGUID -eq \'$($objet.ObjectGUID)\'" -Properties DistinguishedName -ErrorAction SilentlyContinue } catch { }',
      '  if ($apres) {',
      "    $etatV = 'PROBLEME'",
      "    if ($apres.DistinguishedName -like \"*,$DnCible\") { $etatV = 'OK' }",
      "    [void](Add-KjemoResultat -Categorie 'Verification' -Controle 'Emplacement apres deplacement' -Etat $etatV -Valeur $apres.DistinguishedName)",
      '  }',
      '} else {',
      "  Write-Host ''",
      "  Write-Host '--- Simulation (-WhatIf) ---'",
      '  if ($deplacementUtile -and $ouOk) {',
      '    Move-ADObject -Identity $dnAvant -TargetPath $DnCible -WhatIf',
      '  }',
      '}',
      blocModeDiagnostic(),
      blocAucuneSuppression('objet'),
    ];

    return assembler({
      titre: 'Rechercher et deplacer un objet Active Directory',
      outil: 'ad-object-search-move',
      diagnostic: v.mode !== 'Appliquer',
      admin: true,
      parametres: [
        ['Domaine', psB64(domaine)],
        ['Recherche', psB64(recherche)],
        ['OuCible', psB64(dnCible)],
        ['Mode', psB64(v.mode)],
      ],
      corps,
      prefixeFichier: 'kjemo-ad-deplacement',
      formats: 'Console',
    });
  },
  gui: [
    'Utilisateurs et ordinateurs Active Directory (dsa.msc) > Action > Rechercher.',
    'Affichage > Fonctionnalités avancées, pour voir l’onglet Objet et le nom distinctif.',
    'Clic droit sur l’objet > Déplacer, puis choisir l’OU de destination.',
    'Un objet protégé contre la suppression accidentelle refuse le déplacement : décocher la protection dans l’onglet Objet, déplacer, puis la remettre.',
  ],
  keywords: [
    'deplacer', 'move-adobject', 'rechercher un utilisateur', 'nom distinctif', 'dn',
    'changer d’ou', 'get-adobject', 'objet introuvable', 'gpo apres deplacement',
  ],
  requiresAdmin: true,
  os: OS_AD,
  prereqs: PREREQS_AD.concat([
    'Droit d’écriture sur l’OU d’origine et sur l’OU de destination : un déplacement touche les deux.',
    'Savoir quelles stratégies de groupe sont liées à l’OU de destination — c’est ce que le déplacement change vraiment.',
  ]),
  commonErrors: ERREURS_AD.concat([
    {
      message: 'Move-ADObject : Access is denied',
      code: '0x5',
      cause: 'Droits insuffisants sur l’une des deux OU, ou objet protégé contre la suppression accidentelle.',
      fix: 'Vérifier la protection dans dsa.msc (Fonctionnalités avancées > onglet Objet), et la délégation sur les deux OU.',
    },
    {
      message: 'Move-ADObject : The object already exists',
      cause: 'Un objet du même nom se trouve déjà dans l’OU de destination.',
      fix: 'Renommer l’un des deux avant de déplacer. Deux objets ne peuvent pas porter le même nom dans une même OU.',
    },
    {
      message: 'L’utilisateur perd des paramètres après le déplacement',
      cause: 'Les stratégies de groupe liées à l’ancienne OU ne s’appliquent plus.',
      fix: 'Comparer les GPO liées aux deux OU avant de déplacer, et lancer gpupdate sur le poste concerné après.',
      command: 'gpresult /r /scope:user',
    },
  ]),
  reversible: true,
  verifyAfter: [
    'Le script relit l’objet par son GUID — qui ne change pas au déplacement — et affiche son nouveau nom distinctif.',
    'L’emplacement d’origine est consigné dans le rapport, sous « Annulation ».',
    'Sur un poste concerné, gpresult montre les nouvelles stratégies appliquées.',
  ],
  rollback: {
    summary: 'Un déplacement se défait exactement comme il s’est fait : en renvoyant l’objet à son emplacement d’origine, que le rapport a consigné. Le GUID et le SID de l’objet ne changent pas : rien n’est perdu.',
    diagnostic: '# Ou se trouve l\'objet aujourd\'hui ?\nGet-ADObject -Filter "Name -eq \'<nom>\'" -Properties DistinguishedName,whenChanged | Format-List Name,ObjectClass,DistinguishedName,whenChanged',
    command: '# Renvoyer l\'objet a son emplacement d\'origine, releve dans le rapport.\nMove-ADObject -Identity \'<dn-actuel>\' -TargetPath \'<dn-ou-origine>\'\n\n# Puis rafraichir les strategies sur le poste concerne.\n#   gpupdate /force  n\'est pas necessaire : gpupdate suffit.',
    warning: 'Le retour est immédiat dans l’annuaire, mais les stratégies de groupe ne se rappliquent qu’au prochain rafraîchissement, ou à la prochaine ouverture de session.',
  },
  checks: [
    'Chercher d’abord, déplacer ensuite : le mode Diagnostic montre le trajet complet.',
    'Vérifier quelles GPO sont liées à l’OU de destination.',
    'Noter l’emplacement d’origine : c’est ce qui rend le geste réversible.',
  ],
  source: 'https://learn.microsoft.com/powershell/module/activedirectory/move-adobject',
  sources: [
    { label: 'Move-ADObject', url: 'https://learn.microsoft.com/powershell/module/activedirectory/move-adobject' },
    { label: 'Get-ADObject', url: 'https://learn.microsoft.com/powershell/module/activedirectory/get-adobject' },
    { label: 'Syntaxe des noms distinctifs', url: 'https://learn.microsoft.com/windows/win32/ad/object-names-and-identities' },
  ],
};

/**
 * 9. ad-delegation-audit — qui a le droit de faire quoi, et où.
 *
 * La délégation Active Directory se pose en trois clics et s'oublie en trois
 * ans. Cet outil lit les ACL des OU et les rend lisibles : ce qui est hérité
 * et ce qui ne l'est pas, ce qui est large au point d'être équivalent à
 * l'administration du domaine, et ce qui pointe vers un compte qui n'existe
 * plus. Il lit ; il ne réécrit aucune ACL.
 */
export const outilAuditDelegation = {
  id: 'ad-delegation-audit',
  icon: '⚿',
  category: 'Active Directory',
  subcategory: 'Maintenance des objets',
  title: 'Audit des délégations sur les unités d’organisation',
  risk: 'diagnostic',
  summary: 'Lit les listes de contrôle d’accès des OU, distingue l’hérité de l’explicite, signale les droits très larges et les identités qui ne se résolvent plus. N’écrit aucune ACL.',
  fields: [
    champDomaine(),
    {
      id: 'delOu', label: 'OU examinée (facultatif)', default: '',
      help: 'Chemin parent/enfant, relatif au domaine. Laisse vide pour examiner toutes les OU du domaine.',
    },
    {
      id: 'delPortee', label: 'Étendue', type: 'select', default: 'Explicites',
      options: [
        ['Explicites', 'Délégations explicites seulement — ce qui a été posé à la main'],
        ['Toutes', 'Toutes les entrées, héritées comprises — beaucoup plus long'],
      ],
      help: 'Une délégation posée volontairement est explicite. L’hérité vient de plus haut dans l’arborescence.',
    },
    champFormat('delFormat'),
  ],
  validate(v) {
    const errors = {};
    verifier(errors, 'adDomain', validerFqdn(v.adDomain));
    const ou = String(v.delOu ?? '').trim();
    if (ou !== '') verifier(errors, 'delOu', validerCheminOu(ou));
    verifier(errors, 'delPortee', validerChoix(v.delPortee, ['Explicites', 'Toutes'], 'L’étendue'));
    verifier(errors, 'delFormat', validerFormatRapport(v.delFormat));
    return errors;
  },
  generate(v) {
    assertValid(this, v);
    const domaine = validerFqdn(v.adDomain).value;
    const ou = String(v.delOu ?? '').trim();
    const racine = ou === '' ? domainToDn(domaine) : cheminOuVersDn(validerCheminOu(ou).value, domaine);

    const corps = [
      `$Domaine = ${psB64(domaine)}`,
      `$Racine = ${psB64(racine)}`,
      `$Portee = ${psB64(v.delPortee)}`,
      `$KjemoFormat = ${psB64(v.delFormat)}`,
      "",
      blocModuleAd(),
      "",
      '# --- 1. Table des droits etendus et des classes ----------------------------',
      "# Les ACL d'Active Directory designent les objets et les droits par GUID.",
      '# Sans traduction, le rapport serait illisible : on construit donc la table',
      '# a partir du schema et du conteneur des droits etendus.',
      '$tableGuid = @{}',
      'if ($moduleOk) {',
      '  try {',
      '    $rootDse = Get-ADRootDSE -ErrorAction Stop',
      "    foreach ($e in (Get-ADObject -SearchBase $rootDse.schemaNamingContext -LDAPFilter '(schemaIDGUID=*)' -Properties lDAPDisplayName,schemaIDGUID -ErrorAction SilentlyContinue)) {",
      '      $tableGuid[[System.Guid]$e.schemaIDGUID] = $e.lDAPDisplayName',
      '    }',
      '    $ctrl = "CN=Extended-Rights," + $rootDse.configurationNamingContext',
      "    foreach ($e in (Get-ADObject -SearchBase $ctrl -LDAPFilter '(objectClass=controlAccessRight)' -Properties displayName,rightsGuid -ErrorAction SilentlyContinue)) {",
      '      $tableGuid[[System.Guid]$e.rightsGuid] = $e.displayName',
      '    }',
      '  } catch { }',
      '}',
      '[void](Add-KjemoResultat -Categorie \'Lecture\' -Controle \'Table des droits\' -Etat \'INFO\' -Valeur ("$($tableGuid.Count) identifiants traduits"))',
      "",
      '# --- 2. Les OU a examiner ---------------------------------------------------',
      '$unites = @()',
      'if ($moduleOk) {',
      '  try { $unites = @(Get-ADOrganizationalUnit -SearchBase $Racine -Filter * -Properties DistinguishedName -ErrorAction Stop) } catch {',
      "    [void](Add-KjemoResultat -Categorie 'Lecture' -Controle 'OU' -Etat 'PROBLEME' -Valeur $_.Exception.Message)",
      '  }',
      '}',
      "[void](Add-KjemoResultat -Categorie 'Perimetre' -Controle 'Unites examinees' -Etat 'INFO' -Valeur $unites.Count -Commentaire $Racine)",
      "",
      '# --- 3. Droits juges trop larges -------------------------------------------',
      "# GenericAll donne tout. WriteDacl permet de se donner tout. WriteOwner",
      "# permet de devenir proprietaire, donc de se donner WriteDacl. Les trois",
      '# equivalent, sur une OU, a en etre administrateur.',
      "$droitsLarges = @('GenericAll', 'WriteDacl', 'WriteOwner')",
      "$identitesLarges = @('Tout le monde', 'Everyone', 'Utilisateurs authentifies', 'Authenticated Users', 'Utilisateurs du domaine', 'Domain Users', 'ANONYMOUS LOGON')",
      '$nbExplicites = 0',
      '$nbLarges = 0',
      '$nbOrphelines = 0',
      "",
      'foreach ($u in $unites) {',
      '  $acl = $null',
      '  try { $acl = Get-Acl -Path ("AD:\\" + $u.DistinguishedName) -ErrorAction SilentlyContinue } catch { }',
      '  if (-not $acl) {',
      "    [void](Add-KjemoResultat -Categorie 'Delegations' -Controle $u.DistinguishedName -Etat 'ATTENTION' -Valeur 'ACL illisible' -Commentaire 'Droits insuffisants pour lire le descripteur de securite de cette OU.')",
      '    continue',
      '  }',
      '  [void](Add-KjemoResultat -Categorie \'Proprietaire\' -Controle $u.DistinguishedName -Etat \'INFO\' -Valeur $acl.Owner)',
      '  foreach ($ace in $acl.Access) {',
      "    if ($Portee -eq 'Explicites' -and $ace.IsInherited) { continue }",
      '    if (-not $ace.IsInherited) { $nbExplicites = $nbExplicites + 1 }',
      "",
      '    $identite = $ace.IdentityReference.Value',
      '    $orpheline = $false',
      "    if ($identite -match '^S-1-5-21-') { $orpheline = $true ; $nbOrphelines = $nbOrphelines + 1 }",
      "",
      "    $cible = 'tous les objets'",
      '    if ($ace.ObjectType -ne [System.Guid]::Empty -and $tableGuid.ContainsKey($ace.ObjectType)) {',
      '      $cible = $tableGuid[$ace.ObjectType]',
      '    }',
      "    $heritage = 'explicite'",
      "    if ($ace.IsInherited) { $heritage = 'herite' }",
      "",
      '    $large = $false',
      '    foreach ($d in $droitsLarges) {',
      "      if ($ace.ActiveDirectoryRights.ToString() -like \"*$d*\" -and $ace.AccessControlType -eq 'Allow') { $large = $true }",
      '    }',
      '    $identiteLarge = $false',
      '    foreach ($i in $identitesLarges) {',
      '      if ($identite -like "*$i") { $identiteLarge = $true }',
      '    }',
      "",
      "    $etatA = 'INFO'",
      "    $note = ''",
      '    if ($orpheline) {',
      "      $etatA = 'ATTENTION'",
      "      $note = 'Identite non resolue : le compte ou le groupe a ete supprime, l''entree reste. A nettoyer apres verification.'",
      '    } elseif ($large -and $identiteLarge) {',
      "      $etatA = 'PROBLEME'",
      "      $note = 'Droit tres large accorde a un groupe tres large : equivaut a donner l''administration de cette OU a tout le monde.'",
      '      $nbLarges = $nbLarges + 1',
      '    } elseif ($large) {',
      "      $etatA = 'ATTENTION'",
      "      $note = 'Droit tres large : GenericAll, WriteDacl et WriteOwner permettent de se donner tous les autres droits.'",
      '      $nbLarges = $nbLarges + 1',
      '    }',
      "",
      '    [void](Add-KjemoResultat -Categorie \'Delegations\' -Controle ("$identite sur " + $u.DistinguishedName) -Etat $etatA -Valeur ("$($ace.AccessControlType) $($ace.ActiveDirectoryRights) sur $cible [$heritage]") -Commentaire $note)',
      '  }',
      '}',
      "",
      '# --- 4. Conclusion ----------------------------------------------------------',
      '[void](Add-KjemoResultat -Categorie \'Conclusion\' -Controle \'Delegations explicites\' -Etat \'INFO\' -Valeur $nbExplicites)',
      "$etat = 'OK'",
      "if ($nbLarges -gt 0) { $etat = 'ATTENTION' }",
      "[void](Add-KjemoResultat -Categorie 'Conclusion' -Controle 'Droits tres larges' -Etat $etat -Valeur $nbLarges)",
      "$etat = 'OK'",
      "if ($nbOrphelines -gt 0) { $etat = 'ATTENTION' }",
      "[void](Add-KjemoResultat -Categorie 'Conclusion' -Controle 'Identites non resolues' -Etat $etat -Valeur $nbOrphelines -Commentaire 'Une entree qui affiche un SID brut designe un compte supprime.')",
      "",
      "Write-Host ''",
      "Write-Host '--- Corriger une delegation ---'",
      "Write-Host 'Ce script ne reecrit aucune ACL, et c''est volontaire : une ACL Active'",
      "Write-Host 'Directory se compose de dizaines d''entrees, et la remplacer en bloc est'",
      "Write-Host 'la facon la plus rapide de casser un domaine. Pour corriger :'",
      "Write-Host '  1. dsa.msc, clic droit sur l''OU > Deleguer le controle : l''assistant'",
      "Write-Host '     ajoute une delegation sans toucher aux autres entrees.'",
      "Write-Host '  2. Fonctionnalites avancees > onglet Securite > Avance, pour retirer'",
      "Write-Host '     une entree precise, une seule a la fois.'",
      "Write-Host '  3. Verifier ensuite en relancant ce rapport.'",
    ];

    return assembler({
      titre: 'Audit des delegations Active Directory',
      outil: 'ad-delegation-audit',
      diagnostic: true,
      admin: false,
      parametres: [
        ['Domaine', psB64(domaine)],
        ['Racine', psB64(racine)],
        ['Portee', psB64(v.delPortee)],
        ['Format', psB64(v.delFormat)],
      ],
      corps,
      prefixeFichier: 'kjemo-ad-delegations',
    });
  },
  gui: [
    'Utilisateurs et ordinateurs Active Directory (dsa.msc) > Affichage > Fonctionnalités avancées.',
    'Clic droit sur une OU > Propriétés > onglet Sécurité > Avancé : la liste complète des entrées, avec la colonne « Hérité de ».',
    'Clic droit sur une OU > Déléguer le contrôle : l’assistant ajoute une délégation courante sans toucher au reste de l’ACL.',
    'Une entrée qui affiche un SID brut au lieu d’un nom désigne un compte supprimé.',
  ],
  keywords: [
    'delegation', 'acl', 'permissions active directory', 'deleguer le controle',
    'genericall', 'writedacl', 'writeowner', 'sid orphelin', 'heritage',
    'qui peut reinitialiser les mots de passe', 'audit des droits',
  ],
  requiresAdmin: false,
  os: OS_AD,
  prereqs: PREREQS_AD.concat([
    'Droit de lire les descripteurs de sécurité des OU examinées. Un compte sans ce droit verra « ACL illisible ».',
    'Sur un grand domaine, l’étendue « Toutes les entrées » produit un rapport très long : commencer par les délégations explicites.',
  ]),
  commonErrors: ERREURS_AD.concat([
    {
      message: 'Get-Acl : Cannot find path \'AD:\\OU=...\' because it does not exist',
      cause: 'Le lecteur AD: n’est pas monté, ou le nom distinctif est incorrect.',
      fix: 'Importer le module ActiveDirectory avant, ce que fait ce script, et vérifier le chemin d’OU.',
    },
    {
      message: 'Les identités s’affichent en S-1-5-21-…',
      cause: 'Le compte ou le groupe correspondant a été supprimé : le SID reste inscrit dans l’ACL.',
      fix: 'Retirer l’entrée une par une dans l’onglet Sécurité avancé, après avoir vérifié qu’elle ne correspond à rien d’attendu.',
    },
    {
      message: 'Une délégation existe mais l’opérateur n’arrive toujours pas à agir',
      cause: 'La délégation porte sur une classe d’objet ou un attribut différent de ce qu’il tente, ou l’héritage est bloqué plus bas.',
      fix: 'Lire la colonne « sur … » du rapport : elle nomme la classe ou le droit étendu visé par chaque entrée.',
    },
  ]),
  reversible: true,
  verifyAfter: [
    'Le rapport annonce le nombre d’OU examinées : il doit correspondre au périmètre attendu.',
    'Chaque entrée porte son identité, son droit, sa cible et la mention hérité ou explicite.',
    'Les compteurs finaux donnent le nombre de droits très larges et d’identités non résolues.',
  ],
  rollback: {
    summary: 'Ce script lit ; il ne modifie aucune ACL. Il n’y a rien à annuler. La seule trace possible est le fichier de rapport.',
    diagnostic: '# Relire une ACL precise pour comparer avant et apres une correction manuelle.\n(Get-Acl -Path \'AD:\\<dn-de-l-ou>\').Access | Where-Object { -not $_.IsInherited } | Format-Table IdentityReference,ActiveDirectoryRights,AccessControlType',
    command: '# Retirer un rapport devenu inutile.\nGet-ChildItem -Path ([Environment]::GetFolderPath(\'Desktop\')) -Filter \'kjemo-ad-delegations*\' | Remove-Item -Confirm',
    warning: 'Le rapport décrit précisément qui peut faire quoi dans l’annuaire. C’est exactement la carte que cherche un attaquant : il se range comme un document sensible.',
  },
  checks: [
    'Commencer par les délégations explicites : ce sont celles que quelqu’un a posées.',
    'Traiter en priorité les droits très larges accordés à des groupes très larges.',
    'Les corrections se font une entrée à la fois, dans l’interface graphique.',
  ],
  source: 'https://learn.microsoft.com/windows-server/identity/ad-ds/plan/delegating-administration-of-account-ous-and-resource-ous',
  sources: [
    { label: 'Déléguer l’administration des OU', url: 'https://learn.microsoft.com/windows-server/identity/ad-ds/plan/delegating-administration-of-account-ous-and-resource-ous' },
    { label: 'Get-Acl', url: 'https://learn.microsoft.com/powershell/module/microsoft.powershell.security/get-acl' },
    { label: 'Get-ADRootDSE', url: 'https://learn.microsoft.com/powershell/module/activedirectory/get-adrootdse' },
  ],
};

/**
 * 10. ad-computer-cleanup — les ordinateurs qui ne reviendront pas.
 *
 * Un objet ordinateur qui ne s'est plus authentifié depuis un an est presque
 * toujours une machine réinstallée, volée ou mise au rebut. Presque. C'est
 * pourquoi ce script exporte d'abord, désactive ensuite, met en quarantaine si
 * on le lui demande — et ne supprime jamais.
 */
export const outilNettoyageOrdinateurs = {
  id: 'ad-computer-cleanup',
  icon: '▣',
  category: 'Active Directory',
  subcategory: 'Maintenance des objets',
  title: 'Recenser et mettre en quarantaine les ordinateurs inactifs',
  risk: 'caution',
  summary: 'Recense les objets ordinateurs inactifs, désactivés ou en doublon, les exporte en CSV, puis peut les désactiver et les déplacer vers une OU de quarantaine. Ne supprime jamais un objet.',
  fields: [
    champDomaine(),
    {
      id: 'ordOu', label: 'Limiter à une OU (facultatif)', default: '',
      help: 'Chemin parent/enfant, relatif au domaine. Laisse vide pour examiner tout le domaine.',
    },
    {
      id: 'ordInactif', label: 'Ordinateur considéré inactif après (jours)', default: '180',
      help: 'Un poste allumé change son mot de passe de compte tous les 30 jours : au-delà de 90 jours sans contact, la machine ne parle plus au domaine.',
    },
    {
      id: 'ordQuarantaine', label: 'OU de quarantaine', default: 'Administration/Quarantaine',
      help: 'OU où déplacer les objets désactivés, le temps de confirmer qu’aucun service ne dépendait d’eux. Elle doit exister.',
    },
    {
      id: 'ordAction', label: 'Action en mode Appliquer', type: 'select', default: 'DesactiverSeulement',
      options: [
        ['DesactiverSeulement', 'Désactiver seulement — l’objet reste en place'],
        ['DesactiverEtDeplacer', 'Désactiver et déplacer en quarantaine'],
      ],
      help: 'Aucune de ces deux actions ne supprime quoi que ce soit. La suppression reste une décision humaine, prise plus tard.',
    },
    champMode(),
  ],
  validate(v) {
    const errors = {};
    verifier(errors, 'adDomain', validerFqdn(v.adDomain));
    const ou = String(v.ordOu ?? '').trim();
    if (ou !== '') verifier(errors, 'ordOu', validerCheminOu(ou));
    verifier(errors, 'ordInactif', validerJours(v.ordInactif, { min: 30, max: 3650, label: 'Le seuil d’inactivité' }));
    verifier(errors, 'ordQuarantaine', validerCheminOu(v.ordQuarantaine));
    verifier(errors, 'ordAction', validerChoix(v.ordAction, ['DesactiverSeulement', 'DesactiverEtDeplacer'], 'L’action'));
    verifier(errors, 'mode', validerModeExecution(v.mode));
    return errors;
  },
  generate(v) {
    assertValid(this, v);
    const domaine = validerFqdn(v.adDomain).value;
    const ou = String(v.ordOu ?? '').trim();
    const racine = ou === '' ? domainToDn(domaine) : cheminOuVersDn(validerCheminOu(ou).value, domaine);
    const dnQuarantaine = cheminOuVersDn(validerCheminOu(v.ordQuarantaine).value, domaine);

    const corps = [
      `$Mode = ${psB64(v.mode)}`,
      `$Domaine = ${psB64(domaine)}`,
      `$Racine = ${psB64(racine)}`,
      `$DnQuarantaine = ${psB64(dnQuarantaine)}`,
      `$Action = ${psB64(v.ordAction)}`,
      `$SeuilInactif = ${validerJours(v.ordInactif, { min: 30, max: 3650 }).value}`,
      "$KjemoFormat = 'Console'",
      "",
      blocModuleAd(),
      "",
      '# --- 1. Inventaire ----------------------------------------------------------',
      '$limite = (Get-Date).AddDays(-$SeuilInactif)',
      '$ordis = @()',
      'if ($moduleOk) {',
      '  try {',
      '    $ordis = @(Get-ADComputer -SearchBase $Racine -Filter * -Properties Enabled,LastLogonDate,PasswordLastSet,OperatingSystem,DistinguishedName,whenCreated,Description -ErrorAction Stop)',
      '  } catch {',
      "    [void](Add-KjemoResultat -Categorie 'Inventaire' -Controle 'Lecture' -Etat 'PROBLEME' -Valeur $_.Exception.Message)",
      '  }',
      '}',
      "[void](Add-KjemoResultat -Categorie 'Inventaire' -Controle 'Objets ordinateurs' -Etat 'INFO' -Valeur $ordis.Count -Commentaire $Racine)",
      "",
      '# --- 2. Inactifs -------------------------------------------------------------',
      '# Le mot de passe du compte ordinateur se renouvelle tous les 30 jours quand',
      '# la machine est en service : PasswordLastSet est donc un indicateur plus sur',
      '# que LastLogonDate, qui se replique avec du retard.',
      '$inactifs = @($ordis | Where-Object {',
      '  $_.Enabled -eq $true -and (',
      '    ($_.PasswordLastSet -ne $null -and $_.PasswordLastSet -lt $limite) -or',
      '    ($_.PasswordLastSet -eq $null -and $_.whenCreated -lt $limite)',
      '  )',
      '})',
      "$etat = 'OK'",
      "if ($inactifs.Count -gt 0) { $etat = 'ATTENTION' }",
      '[void](Add-KjemoResultat -Categorie \'Inactifs\' -Controle "Sans contact depuis plus de $SeuilInactif jours" -Etat $etat -Valeur $inactifs.Count)',
      'foreach ($o in ($inactifs | Sort-Object PasswordLastSet)) {',
      "  $quand = 'jamais'",
      "  if ($o.PasswordLastSet -ne $null) { $quand = $o.PasswordLastSet.ToString('yyyy-MM-dd') }",
      '  [void](Add-KjemoResultat -Categorie \'Inactifs\' -Controle $o.Name -Etat \'ATTENTION\' -Valeur ("dernier contact : $quand") -Commentaire ("$($o.OperatingSystem) — $($o.DistinguishedName)"))',
      '}',
      "",
      '# --- 3. Deja desactives -------------------------------------------------------',
      '$deja = @($ordis | Where-Object { $_.Enabled -eq $false })',
      "[void](Add-KjemoResultat -Categorie 'Deja desactives' -Controle 'Total' -Etat 'INFO' -Valeur $deja.Count -Commentaire 'Objets desactives lors d''un passage precedent, ou a la main.')",
      'foreach ($o in ($deja | Sort-Object Name)) {',
      "  [void](Add-KjemoResultat -Categorie 'Deja desactives' -Controle $o.Name -Etat 'INFO' -Valeur $o.DistinguishedName)",
      '}',
      "",
      '# --- 4. Doublons --------------------------------------------------------------',
      "# Deux objets pour une meme machine arrivent apres une reinstallation faite",
      "# sans retirer l'ancien compte. Le plus recent est celui qui fonctionne.",
      '$groupes = $ordis | Group-Object -Property Name | Where-Object { $_.Count -gt 1 }',
      "$etat = 'OK'",
      "if ($groupes) { $etat = 'ATTENTION' }",
      '$nbDoublons = 0',
      'if ($groupes) { $nbDoublons = @($groupes).Count }',
      "[void](Add-KjemoResultat -Categorie 'Doublons' -Controle 'Noms portes par plusieurs objets' -Etat $etat -Valeur $nbDoublons)",
      'foreach ($g in $groupes) {',
      '  foreach ($o in $g.Group) {',
      "    $quand = 'jamais'",
      "    if ($o.PasswordLastSet -ne $null) { $quand = $o.PasswordLastSet.ToString('yyyy-MM-dd') }",
      "    [void](Add-KjemoResultat -Categorie 'Doublons' -Controle $g.Name -Etat 'ATTENTION' -Valeur (\"$($o.DistinguishedName) — dernier contact : $quand\") -Commentaire 'Ne retire jamais le plus recent : c''est celui par lequel la machine s''authentifie.')",
      '  }',
      '}',
      "",
      '# --- 5. Export avant toute action ---------------------------------------------',
      "# L'export precede l'action, toujours : c'est lui qui permet de revenir en",
      "# arriere, et de savoir ce qui a ete touche.",
      "$dossier = [Environment]::GetFolderPath('Desktop')",
      "$horodatage = (Get-Date).ToString('yyyyMMdd-HHmmss')",
      '$cheminExport = Join-Path $dossier "kjemo-ad-ordinateurs-avant-$horodatage.csv"',
      'try {',
      '  $ordis | Select-Object Name,Enabled,OperatingSystem,PasswordLastSet,LastLogonDate,whenCreated,DistinguishedName,Description |',
      '    Sort-Object Name | Export-Csv -Path $cheminExport -NoTypeInformation -Encoding UTF8',
      "  [void](Add-KjemoResultat -Categorie 'Sauvegarde' -Controle 'Export avant action' -Etat 'OK' -Valeur $cheminExport)",
      '} catch {',
      "  [void](Add-KjemoResultat -Categorie 'Sauvegarde' -Controle 'Export avant action' -Etat 'PROBLEME' -Valeur $_.Exception.Message -Commentaire 'Sans export, aucune action ne sera tentee.')",
      '  $cheminExport = $null',
      '}',
      "",
      '# --- 6. OU de quarantaine ------------------------------------------------------',
      '$quarantaineOk = $false',
      'if ($moduleOk) {',
      '  try { if (Get-ADOrganizationalUnit -Identity $DnQuarantaine -ErrorAction SilentlyContinue) { $quarantaineOk = $true } } catch { }',
      '}',
      "if ($Action -eq 'DesactiverEtDeplacer') {",
      '  if ($quarantaineOk) {',
      "    [void](Add-KjemoResultat -Categorie 'Quarantaine' -Controle 'OU de quarantaine' -Etat 'OK' -Valeur $DnQuarantaine)",
      '  } else {',
      "    [void](Add-KjemoResultat -Categorie 'Quarantaine' -Controle 'OU de quarantaine' -Etat 'PROBLEME' -Valeur $DnQuarantaine -Commentaire 'Cree cette OU avant, avec l''outil de hierarchie d''OU. Aucun deplacement ne sera tente.')",
      '  }',
      '}',
      "",
      '# --- 7. Ce que la desactivation implique ---------------------------------------',
      "[void](Add-KjemoResultat -Categorie 'Avertissement' -Controle 'Canal securise' -Etat 'INFO' -Valeur 'desactiver rompt l''authentification de la machine' -Commentaire 'Si la machine revient en service, elle ne pourra plus ouvrir de session de domaine tant que son objet est desactive. La reactiver suffit : le SID est conserve.')",
      "[void](Add-KjemoResultat -Categorie 'Avertissement' -Controle 'Suppression' -Etat 'INFO' -Valeur 'jamais automatique' -Commentaire 'Supprimer l''objet detruit son SID. Rejoindre le domaine ensuite cree un nouvel objet, et tout ce qui reposait sur l''ancien SID est perdu : c''est irreversible.')",
      "",
      '# --- 8. Action, uniquement en mode Appliquer -----------------------------------',
      "if ($Mode -eq 'Appliquer' -and $cheminExport -ne $null) {",
      '  foreach ($o in $inactifs) {',
      '    try {',
      '      Disable-ADAccount -Identity $o.DistinguishedName -ErrorAction Stop',
      "      [void](Add-KjemoResultat -Categorie 'Action' -Controle $o.Name -Etat 'OK' -Valeur 'desactive')",
      '    } catch {',
      "      [void](Add-KjemoResultat -Categorie 'Action' -Controle $o.Name -Etat 'PROBLEME' -Valeur $_.Exception.Message)",
      '      continue',
      '    }',
      "    if ($Action -eq 'DesactiverEtDeplacer' -and $quarantaineOk) {",
      '      try {',
      '        Move-ADObject -Identity $o.DistinguishedName -TargetPath $DnQuarantaine -ErrorAction Stop',
      "        [void](Add-KjemoResultat -Categorie 'Action' -Controle $o.Name -Etat 'OK' -Valeur 'deplace en quarantaine')",
      '      } catch {',
      "        [void](Add-KjemoResultat -Categorie 'Action' -Controle $o.Name -Etat 'PROBLEME' -Valeur $_.Exception.Message -Commentaire 'Objet probablement protege contre la suppression accidentelle.')",
      '      }',
      '    }',
      '  }',
      "  Write-Host ''",
      "  Write-Host '--- Verification apres action ---'",
      '  foreach ($o in $inactifs) {',
      '    $apres = $null',
      '    try { $apres = Get-ADComputer -Identity $o.ObjectGUID -Properties Enabled,DistinguishedName -ErrorAction SilentlyContinue } catch { }',
      '    if ($apres) {',
      '      [void](Add-KjemoResultat -Categorie \'Verification\' -Controle $apres.Name -Etat \'INFO\' -Valeur ("actif : $($apres.Enabled) — $($apres.DistinguishedName)"))',
      '    }',
      '  }',
      '} else {',
      "  Write-Host ''",
      "  Write-Host '--- Simulation (-WhatIf) ---'",
      '  foreach ($o in $inactifs) {',
      '    Disable-ADAccount -Identity $o.DistinguishedName -WhatIf',
      "    if ($Action -eq 'DesactiverEtDeplacer' -and $quarantaineOk) {",
      '      Move-ADObject -Identity $o.DistinguishedName -TargetPath $DnQuarantaine -WhatIf',
      '    }',
      '  }',
      '}',
      blocModeDiagnostic(),
      blocAucuneSuppression('ordinateur'),
    ];

    return assembler({
      titre: 'Recenser et mettre en quarantaine les ordinateurs inactifs',
      outil: 'ad-computer-cleanup',
      diagnostic: v.mode !== 'Appliquer',
      admin: true,
      parametres: [
        ['Domaine', psB64(domaine)],
        ['Racine', psB64(racine)],
        ['SeuilInactifJours', psB64(String(v.ordInactif))],
        ['Action', psB64(v.ordAction)],
        ['Mode', psB64(v.mode)],
      ],
      corps,
      prefixeFichier: 'kjemo-ad-ordinateurs',
      formats: 'Console',
    });
  },
  gui: [
    'Utilisateurs et ordinateurs Active Directory (dsa.msc) > OU Computers, ou l’OU concernée.',
    'Requêtes enregistrées > Nouveau > Requête : « Jours depuis la dernière ouverture de session ».',
    'Clic droit sur un objet > Désactiver le compte.',
    'Clic droit > Déplacer, vers l’OU de quarantaine.',
    'La suppression se fait à la main, plus tard, une fois qu’on est sûr que la machine ne revient pas.',
  ],
  keywords: [
    'ordinateur inactif', 'objet ordinateur', 'get-adcomputer', 'poste obsolete',
    'menage annuaire', 'quarantaine', 'desactiver un ordinateur', 'doublon',
    'passwordlastset', 'machine reinstallee',
  ],
  requiresAdmin: true,
  os: OS_AD,
  prereqs: PREREQS_AD.concat([
    'Droit de désactiver et de déplacer des objets ordinateurs dans le périmètre examiné.',
    'Une OU de quarantaine créée à l’avance, si l’action de déplacement est retenue.',
    'Un délai d’observation convenu : la quarantaine ne sert à rien si personne ne la relit.',
  ]),
  commonErrors: ERREURS_AD.concat([
    {
      message: 'Disable-ADAccount : Access is denied',
      code: '0x5',
      cause: 'Droits insuffisants sur l’OU qui contient l’objet ordinateur.',
      fix: 'Utiliser un compte délégué sur cette OU, ou vérifier la délégation.',
    },
    {
      message: 'Move-ADObject : Access is denied, alors que la désactivation a fonctionné',
      cause: 'L’objet est protégé contre la suppression accidentelle : cette protection bloque aussi le déplacement.',
      fix: 'Décocher la protection dans dsa.msc (Fonctionnalités avancées > onglet Objet), déplacer, puis la remettre.',
    },
    {
      message: 'Une machine désactivée par erreur ne peut plus ouvrir de session',
      cause: 'Le compte ordinateur désactivé refuse l’authentification de la machine auprès du domaine.',
      fix: 'Réactiver l’objet : le SID est conservé, la machine retrouve sa place sans rejoindre le domaine à nouveau.',
      command: 'Enable-ADAccount -Identity \'<nom-machine>$\'',
    },
    {
      message: 'PasswordLastSet est vide sur un objet récent',
      cause: 'L’objet ordinateur a été pré-créé mais aucune machine ne l’a encore utilisé pour joindre le domaine.',
      fix: 'Le script retombe alors sur la date de création. Vérifier qu’il ne s’agit pas d’une pré-création en attente.',
    },
  ]),
  reversible: true,
  verifyAfter: [
    'Un fichier CSV est écrit sur le Bureau AVANT toute action : il contient l’état complet de tous les objets ordinateurs.',
    'Le script relit chaque objet touché par son GUID et affiche son état et son emplacement.',
    'Aucun objet n’est supprimé : le rapport le dit explicitement en fin d’exécution.',
  ],
  rollback: {
    summary: 'La désactivation et le déplacement se défont tous les deux : réactiver l’objet et le renvoyer à son OU d’origine, que le CSV d’export consigne. Le SID de l’objet ne change pas, donc rien n’est perdu.',
    diagnostic: '# Relire le CSV produit avant l\'action, puis l\'etat actuel d\'un objet.\nGet-ChildItem -Path ([Environment]::GetFolderPath(\'Desktop\')) -Filter \'kjemo-ad-ordinateurs-avant-*.csv\' | Sort-Object LastWriteTime -Descending | Select-Object -First 1\nGet-ADComputer -Identity \'<nom-machine>\' -Properties Enabled,DistinguishedName,whenChanged | Format-List Name,Enabled,DistinguishedName,whenChanged',
    command: '# Reactiver un objet desactive par erreur.\nEnable-ADAccount -Identity \'<nom-machine>$\'\n\n# Le renvoyer a son emplacement d\'origine, lu dans le CSV.\nMove-ADObject -Identity \'<dn-actuel>\' -TargetPath \'<dn-origine>\'',
    exceptional: '# AVERTISSEMENT CRITIQUE — suppression d\'un objet ordinateur.\n#\n# Ce script ne supprime jamais. Si la suppression devient necessaire, elle se\n# fait a la main, et il faut savoir ce qu\'elle detruit :\n#\n#   - Le SID de la machine disparait. Toute ACL, toute delegation, tout groupe\n#     qui designait cet ordinateur perd sa reference.\n#   - Le canal securise est rompu definitivement. Si la machine existe encore,\n#     elle devra rejoindre le domaine a nouveau, ce qui cree un objet NEUF,\n#     avec un SID different : ce n\'est pas une restauration.\n#   - Les certificats et les inscriptions lies au compte machine deviennent\n#     invalides.\n#\n# Avant d\'y penser :\n#   1. Le CSV d\'export existe et a ete relu.\n#   2. La corbeille Active Directory est activee.\n#   3. L\'objet est reste en quarantaine assez longtemps pour qu\'une machine\n#      encore en service se soit manifestee.\n#\n# Reference officielle :\n# https://learn.microsoft.com/powershell/module/activedirectory/remove-adcomputer\n#\n# Commande, a executer manuellement, objet par objet :\n#   Remove-ADComputer -Identity \'<nom-machine>\' -Confirm',
    warning: 'Désactiver un objet ordinateur empêche la machine correspondante d’ouvrir une session de domaine dès son prochain démarrage. C’est précisément l’effet recherché pour une machine hors service — et un incident si la machine était encore en usage.',
  },
  checks: [
    'Lire le rapport en mode Diagnostic avant tout : la liste des inactifs est ce qui décide.',
    'Un CSV complet est écrit avant chaque action ; sans lui, le script n’agit pas.',
    'Désactiver et déplacer, jamais supprimer : la suppression se décide plus tard, à la main.',
  ],
  source: 'https://learn.microsoft.com/powershell/module/activedirectory/get-adcomputer',
  sources: [
    { label: 'Get-ADComputer', url: 'https://learn.microsoft.com/powershell/module/activedirectory/get-adcomputer' },
    { label: 'Disable-ADAccount', url: 'https://learn.microsoft.com/powershell/module/activedirectory/disable-adaccount' },
    { label: 'Move-ADObject', url: 'https://learn.microsoft.com/powershell/module/activedirectory/move-adobject' },
    { label: 'Remove-ADComputer', url: 'https://learn.microsoft.com/powershell/module/activedirectory/remove-adcomputer' },
  ],
};

/**
 * 11. ad-dns-health — le DNS vu depuis Active Directory.
 *
 * Active Directory ne trouve ses contrôleurs que par le DNS : ce sont les
 * enregistrements SRV qui disent où est un contrôleur, et le service Netlogon
 * qui les publie. Une panne d'ouverture de session, une réplication arrêtée,
 * un poste qui ne rejoint pas le domaine : dans la grande majorité des cas, la
 * cause est ici.
 *
 * Cet outil ne refait pas le diagnostic DNS générique du LOT 2 : il regarde ce
 * que le DNS doit contenir POUR Active Directory.
 */
export const outilDnsAd = {
  id: 'ad-dns-health',
  icon: '☷',
  category: 'Active Directory',
  subcategory: 'Santé du domaine',
  title: 'Santé du DNS pour Active Directory',
  risk: 'diagnostic',
  summary: 'Vérifie ce dont Active Directory a besoin du DNS : suffixe de domaine, serveurs DNS des contrôleurs, enregistrements SRV, zones directe et inverse, redirecteurs et cohérence entre l’annuaire et la zone.',
  fields: [
    champDomaine(),
    {
      id: 'dnsAdJours', label: 'Fenêtre des erreurs DNS examinées (jours)', default: '7',
      help: 'Journaux DNS Server et System, si la machine est un contrôleur.',
    },
    champFormat('dnsAdFormat'),
  ],
  validate(v) {
    const errors = {};
    verifier(errors, 'adDomain', validerFqdn(v.adDomain));
    verifier(errors, 'dnsAdJours', validerJours(v.dnsAdJours, { min: 1, max: 90, label: 'La fenêtre d’examen' }));
    verifier(errors, 'dnsAdFormat', validerFormatRapport(v.dnsAdFormat));
    return errors;
  },
  generate(v) {
    assertValid(this, v);
    const domaine = validerFqdn(v.adDomain).value;

    const corps = [
      `$Domaine = ${psB64(domaine)}`,
      `$Jours = ${validerJours(v.dnsAdJours, { min: 1, max: 90 }).value}`,
      `$KjemoFormat = ${psB64(v.dnsAdFormat)}`,
      "",
      blocModuleAd(),
      "",
      '# --- 0. Cette machine est-elle un controleur de domaine ? -------------------',
      "# La question decide de la suite : certains controles n'ont de sens que sur",
      '# un controleur, et les lancer ailleurs produit des faux problemes.',
      '$estDC = $false',
      'try {',
      '  $role = (Get-CimInstance Win32_ComputerSystem -ErrorAction Stop).DomainRole',
      '  if ($role -eq 4 -or $role -eq 5) { $estDC = $true }',
      '} catch { }',
      'if ($estDC) {',
      "  [void](Add-KjemoResultat -Categorie 'Contexte' -Controle 'Role de la machine' -Etat 'INFO' -Valeur 'controleur de domaine')",
      '} else {',
      "  [void](Add-KjemoResultat -Categorie 'Contexte' -Controle 'Role de la machine' -Etat 'INFO' -Valeur 'membre ou hors domaine' -Commentaire 'Les controles propres au service DNS du controleur seront ignores, et non declares en echec.')",
      '}',
      "",
      '# --- 1. Suffixe DNS et appartenance -----------------------------------------',
      '$cs = $null',
      'try { $cs = Get-CimInstance Win32_ComputerSystem -ErrorAction SilentlyContinue } catch { }',
      'if ($cs) {',
      "  $etat = 'PROBLEME'",
      "  if ($cs.PartOfDomain) { $etat = 'OK' }",
      '  [void](Add-KjemoResultat -Categorie \'Identite\' -Controle \'Domaine de la machine\' -Etat $etat -Valeur $cs.Domain -Commentaire ("attendu : $Domaine"))',
      '  if ($cs.PartOfDomain -and $cs.Domain -ne $Domaine) {',
      "    [void](Add-KjemoResultat -Categorie 'Identite' -Controle 'Coherence' -Etat 'ATTENTION' -Valeur 'domaine different de celui demande' -Commentaire 'Les controles qui suivent portent sur le domaine demande, pas sur celui de cette machine.')",
      '  }',
      '}',
      '$suffixe = $null',
      'try { $suffixe = (Get-DnsClientGlobalSetting -ErrorAction SilentlyContinue).SuffixSearchList } catch { }',
      'if ($suffixe) {',
      '  [void](Add-KjemoResultat -Categorie \'Identite\' -Controle \'Liste de recherche de suffixes\' -Etat \'INFO\' -Valeur ($suffixe -join \', \'))',
      '}',
      "",
      '# --- 2. Serveurs DNS declares sur les cartes actives -------------------------',
      "# Regle de base : un controleur pointe vers un AUTRE controleur en premier,",
      '# et vers lui-meme ensuite. Un controleur qui ne pointe que vers 127.0.0.1',
      '# peut se retrouver isole de la replication au demarrage.',
      '$cartes = @()',
      'try { $cartes = @(Get-DnsClientServerAddress -AddressFamily IPv4 -ErrorAction SilentlyContinue | Where-Object { $_.ServerAddresses.Count -gt 0 }) } catch { }',
      'if ($cartes.Count -eq 0) {',
      "  [void](Add-KjemoResultat -Categorie 'Client DNS' -Controle 'Serveurs declares' -Etat 'PROBLEME' -Valeur 'aucun' -Commentaire 'Sans serveur DNS, rien de ce qui suit ne peut fonctionner.')",
      '}',
      'foreach ($c in $cartes) {',
      "  $liste = $c.ServerAddresses -join ', '",
      "  $etat = 'OK'",
      "  $note = ''",
      "  if ($liste -match '^(127\\.0\\.0\\.1|::1)') {",
      '    if ($estDC) {',
      "      $etat = 'ATTENTION'",
      "      $note = 'Un controleur qui se resout d''abord lui-meme peut demarrer avant que sa propre zone ne soit chargee. Microsoft recommande de placer un autre controleur en premier, et 127.0.0.1 en dernier.'",
      '    } else {',
      "      $etat = 'PROBLEME'",
      "      $note = 'Une machine membre ne doit pas pointer vers elle-meme : elle doit pointer vers les serveurs DNS du domaine.'",
      '    }',
      '  }',
      "  if ($liste -match '(8\\.8\\.8\\.8|1\\.1\\.1\\.1|9\\.9\\.9\\.9)') {",
      "    $etat = 'PROBLEME'",
      "    $note = $note + ' Un serveur DNS public est declare sur une machine de domaine : il ne connait aucun des enregistrements SRV d''Active Directory. Les redirecteurs se configurent sur le serveur DNS, pas sur les cartes reseau.'",
      '  }',
      '  [void](Add-KjemoResultat -Categorie \'Client DNS\' -Controle $c.InterfaceAlias -Etat $etat -Valeur $liste -Commentaire $note)',
      '}',
      "",
      '# --- 3. Enregistrements SRV indispensables -----------------------------------',
      "# Ce sont eux que tout client interroge pour trouver un controleur. Netlogon",
      '# les publie a chaque demarrage ; il peut les republier a la demande.',
      "$srv = @(\"_ldap._tcp.dc._msdcs.$Domaine\", \"_kerberos._tcp.dc._msdcs.$Domaine\", \"_ldap._tcp.$Domaine\", \"_kerberos._tcp.$Domaine\", \"_gc._tcp.$Domaine\", \"_kpasswd._tcp.$Domaine\")",
      '$srvManquants = 0',
      'foreach ($nom in $srv) {',
      '  $reponse = $null',
      '  try { $reponse = @(Resolve-DnsName -Name $nom -Type SRV -ErrorAction SilentlyContinue | Where-Object { $_.QueryType -eq \'SRV\' }) } catch { }',
      '  if ($reponse -and $reponse.Count -gt 0) {',
      '    [void](Add-KjemoResultat -Categorie \'Enregistrements SRV\' -Controle $nom -Etat \'OK\' -Valeur (($reponse | ForEach-Object { "$($_.NameTarget):$($_.Port)" }) -join \', \'))',
      '  } else {',
      '    $srvManquants = $srvManquants + 1',
      "    [void](Add-KjemoResultat -Categorie 'Enregistrements SRV' -Controle $nom -Etat 'PROBLEME' -Valeur 'absent' -Commentaire 'Sans cet enregistrement, les clients ne trouvent pas de controleur. Sur un controleur, relancer le service Netlogon le republie.')",
      '  }',
      '}',
      "$etat = 'OK'",
      "if ($srvManquants -gt 0) { $etat = 'PROBLEME' }",
      "[void](Add-KjemoResultat -Categorie 'Enregistrements SRV' -Controle 'Synthese' -Etat $etat -Valeur \"$srvManquants enregistrement(s) manquant(s) sur $($srv.Count)\")",
      "",
      '# --- 4. Resolution des controleurs ------------------------------------------',
      '$controleurs = @()',
      'if ($moduleOk) {',
      '  try { $controleurs = @(Get-ADDomainController -Filter * -Server $Domaine -ErrorAction SilentlyContinue) } catch { }',
      '}',
      "[void](Add-KjemoResultat -Categorie 'Controleurs' -Controle 'Recenses dans l''annuaire' -Etat 'INFO' -Valeur $controleurs.Count)",
      'foreach ($dc in $controleurs) {',
      '  $direct = $null',
      '  try { $direct = @(Resolve-DnsName -Name $dc.HostName -Type A -ErrorAction SilentlyContinue | Where-Object { $_.QueryType -eq \'A\' }) } catch { }',
      '  if ($direct -and $direct.Count -gt 0) {',
      "    $ipResolue = ($direct | ForEach-Object { $_.IPAddress }) -join ', '",
      "    $etat = 'OK'",
      "    $note = ''",
      '    if ($dc.IPv4Address -and $ipResolue -notlike "*$($dc.IPv4Address)*") {',
      "      $etat = 'PROBLEME'",
      "      $note = 'Le DNS ne renvoie pas l''adresse que l''annuaire connait : enregistrement perime, souvent apres un changement d''adresse.'",
      '    }',
      '    [void](Add-KjemoResultat -Categorie \'Controleurs\' -Controle ("Resolution directe — " + $dc.HostName) -Etat $etat -Valeur $ipResolue -Commentaire $note)',
      '  } else {',
      "    [void](Add-KjemoResultat -Categorie 'Controleurs' -Controle (\"Resolution directe — \" + $dc.HostName) -Etat 'PROBLEME' -Valeur 'echec' -Commentaire 'Le nom du controleur ne se resout pas : verifier la zone directe.')",
      '  }',
      '  if ($dc.IPv4Address) {',
      '    $inverse = $null',
      '    try { $inverse = @(Resolve-DnsName -Name $dc.IPv4Address -Type PTR -ErrorAction SilentlyContinue) } catch { }',
      '    if ($inverse -and $inverse.Count -gt 0) {',
      '      [void](Add-KjemoResultat -Categorie \'Controleurs\' -Controle ("Resolution inverse — " + $dc.IPv4Address) -Etat \'OK\' -Valeur (($inverse | ForEach-Object { $_.NameHost }) -join \', \'))',
      '    } else {',
      "      [void](Add-KjemoResultat -Categorie 'Controleurs' -Controle (\"Resolution inverse — \" + $dc.IPv4Address) -Etat 'ATTENTION' -Valeur 'aucun enregistrement PTR' -Commentaire 'La zone inverse n''est pas obligatoire, mais son absence complique tous les diagnostics, et gene certaines applications.')",
      '    }',
      '  }',
      '}',
      "",
      '# --- 5. Zones et redirecteurs, sur un controleur -----------------------------',
      '$dnsServeurDispo = $false',
      "if ($estDC -and (Get-Command Get-DnsServerZone -ErrorAction SilentlyContinue)) { $dnsServeurDispo = $true }",
      'if ($dnsServeurDispo) {',
      '  $zones = @()',
      '  try { $zones = @(Get-DnsServerZone -ErrorAction SilentlyContinue) } catch { }',
      "  [void](Add-KjemoResultat -Categorie 'Zones' -Controle 'Zones hebergees' -Etat 'INFO' -Valeur $zones.Count)",
      '  $zoneDomaine = $zones | Where-Object { $_.ZoneName -eq $Domaine }',
      '  if ($zoneDomaine) {',
      "    $etat = 'OK'",
      "    $note = ''",
      '    if (-not $zoneDomaine.IsDsIntegrated) {',
      "      $etat = 'ATTENTION'",
      "      $note = 'Zone non integree a Active Directory : elle ne beneficie ni de la replication de l''annuaire, ni des mises a jour dynamiques securisees.'",
      '    }',
      '    [void](Add-KjemoResultat -Categorie \'Zones\' -Controle ("Zone du domaine — " + $Domaine) -Etat $etat -Valeur ("integree a AD : $($zoneDomaine.IsDsIntegrated) ; type : $($zoneDomaine.ZoneType)") -Commentaire $note)',
      '  } else {',
      "    [void](Add-KjemoResultat -Categorie 'Zones' -Controle (\"Zone du domaine — \" + $Domaine) -Etat 'PROBLEME' -Valeur 'absente de ce serveur')",
      '  }',
      '  $zoneMsdcs = $zones | Where-Object { $_.ZoneName -eq ("_msdcs." + $Domaine) }',
      '  if ($zoneMsdcs) {',
      '    [void](Add-KjemoResultat -Categorie \'Zones\' -Controle (\'Zone _msdcs\') -Etat \'OK\' -Valeur ("integree a AD : $($zoneMsdcs.IsDsIntegrated)"))',
      '  } else {',
      "    [void](Add-KjemoResultat -Categorie 'Zones' -Controle 'Zone _msdcs' -Etat 'INFO' -Valeur 'pas de zone _msdcs distincte' -Commentaire 'Normal quand les enregistrements _msdcs sont contenus dans la zone du domaine.')",
      '  }',
      '  $inverses = @($zones | Where-Object { $_.IsReverseLookupZone })',
      "  $etat = 'OK'",
      "  if ($inverses.Count -eq 0) { $etat = 'ATTENTION' }",
      "  [void](Add-KjemoResultat -Categorie 'Zones' -Controle 'Zones inverses' -Etat $etat -Valeur $inverses.Count -Commentaire 'Sans zone inverse, aucun PTR n''est enregistre : les diagnostics reseau deviennent aveugles.')",
      '  $fwd = $null',
      '  try { $fwd = Get-DnsServerForwarder -ErrorAction SilentlyContinue } catch { }',
      '  if ($fwd -and $fwd.IPAddress) {',
      "    [void](Add-KjemoResultat -Categorie 'Redirecteurs' -Controle 'Redirecteurs configures' -Etat 'OK' -Valeur (($fwd.IPAddress | ForEach-Object { $_.IPAddressToString }) -join ', ') -Commentaire 'C''est ici, et non sur les cartes reseau, que se declare la resolution vers l''exterieur.')",
      '  } else {',
      "    [void](Add-KjemoResultat -Categorie 'Redirecteurs' -Controle 'Redirecteurs configures' -Etat 'INFO' -Valeur 'aucun' -Commentaire 'Sans redirecteur, le serveur utilise les indications de racine. Les deux fonctionnent ; le redirecteur est generalement plus rapide.')",
      '  }',
      '} else {',
      "  [void](Add-KjemoResultat -Categorie 'Zones' -Controle 'Service DNS' -Etat 'IGNORE' -Valeur 'controle non applicable ici' -Commentaire 'Ces controles demandent le role Serveur DNS sur un controleur de domaine.')",
      '}',
      "",
      '# --- 6. Service Netlogon ------------------------------------------------------',
      '$netlogon = $null',
      'try { $netlogon = Get-Service Netlogon -ErrorAction SilentlyContinue } catch { }',
      'if ($netlogon) {',
      "  $etat = 'PROBLEME'",
      "  if ($netlogon.Status -eq 'Running') { $etat = 'OK' }",
      "  [void](Add-KjemoResultat -Categorie 'Services' -Controle 'Netlogon' -Etat $etat -Valeur $netlogon.Status -Commentaire 'Netlogon publie les enregistrements SRV du controleur. Arrete, les enregistrements vieillissent puis disparaissent.')",
      '}',
      "",
      '# --- 7. Erreurs DNS recentes, sur un controleur -------------------------------',
      'if ($estDC) {',
      '  $depuis = (Get-Date).AddDays(-$Jours)',
      '  foreach ($journal in @(\'DNS Server\', \'System\')) {',
      '    $evts = @()',
      '    try {',
      '      $evts = @(Get-WinEvent -FilterHashtable @{ LogName = $journal; Level = 1,2; StartTime = $depuis } -ErrorAction SilentlyContinue)',
      '    } catch { }',
      "    if ($journal -eq 'System') {",
      "      $evts = @($evts | Where-Object { $_.ProviderName -like '*DNS*' -or $_.ProviderName -like '*NETLOGON*' })",
      '    }',
      "    $etat = 'OK'",
      "    if ($evts.Count -gt 0) { $etat = 'ATTENTION' }",
      '    [void](Add-KjemoResultat -Categorie \'Journaux\' -Controle ("Erreurs — " + $journal) -Etat $etat -Valeur ("$($evts.Count) sur $Jours jour(s)"))',
      '    foreach ($e in ($evts | Select-Object -First 10)) {',
      '      [void](Add-KjemoResultat -Categorie \'Journaux\' -Controle ("$journal / evenement $($e.Id)") -Etat \'ATTENTION\' -Valeur $e.TimeCreated.ToString(\'yyyy-MM-dd HH:mm\') -Commentaire (($e.Message -split "`n")[0]))',
      '    }',
      '  }',
      '} else {',
      "  [void](Add-KjemoResultat -Categorie 'Journaux' -Controle 'Journaux DNS du controleur' -Etat 'IGNORE' -Valeur 'controle non applicable ici')",
      '}',
      "",
      '# --- 8. dcdiag /test:dns, uniquement sur un vrai controleur -------------------',
      "# Lance ailleurs, ce test echoue pour une raison qui n'est pas un probleme :",
      '# la machine n\'est pas un controleur. On ne le lance donc pas.',
      'if ($estDC -and (Get-Command dcdiag.exe -ErrorAction SilentlyContinue)) {',
      '  $brut = $null',
      "  try { $brut = & dcdiag.exe '/test:dns' '/v' 2>&1 | Out-String } catch { }",
      '  if ($brut) {',
      "    $dossier = [Environment]::GetFolderPath('Desktop')",
      "    $horodatage = (Get-Date).ToString('yyyyMMdd-HHmmss')",
      '    $chemin = Join-Path $dossier "kjemo-ad-dcdiag-dns-$horodatage.txt"',
      '    try {',
      '      $brut | Out-File -FilePath $chemin -Encoding UTF8',
      "      [void](Add-KjemoResultat -Categorie 'dcdiag' -Controle 'Sortie brute conservee' -Etat 'INFO' -Valeur $chemin -Commentaire 'La sortie complete est conservee telle quelle : l''interpretation ci-dessous ne la remplace pas.')",
      '    } catch { }',
      "    $echecs = @([regex]::Matches($brut, '(?m)^\\s*\\.+\\s*(\\S+)\\s+failed test\\s+(\\S+)'))",
      "    $reussites = @([regex]::Matches($brut, '(?m)^\\s*\\.+\\s*(\\S+)\\s+passed test\\s+(\\S+)'))",
      "    $etat = 'OK'",
      "    if ($echecs.Count -gt 0) { $etat = 'PROBLEME' }",
      '    [void](Add-KjemoResultat -Categorie \'dcdiag\' -Controle \'Tests DNS\' -Etat $etat -Valeur ("$($reussites.Count) reussi(s), $($echecs.Count) en echec"))',
      '    foreach ($m in $echecs) {',
      '      [void](Add-KjemoResultat -Categorie \'dcdiag\' -Controle ("Echec — " + $m.Groups[2].Value) -Etat \'PROBLEME\' -Valeur $m.Groups[1].Value)',
      '    }',
      '  }',
      '} else {',
      "  [void](Add-KjemoResultat -Categorie 'dcdiag' -Controle 'dcdiag /test:dns' -Etat 'IGNORE' -Valeur 'non lance' -Commentaire 'Ce test ne vaut que sur un controleur de domaine, avec les outils AD DS installes. Le lancer ailleurs produirait un faux echec.')",
      '}',
    ];

    return assembler({
      titre: 'Sante du DNS pour Active Directory',
      outil: 'ad-dns-health',
      diagnostic: true,
      admin: true,
      parametres: [
        ['Domaine', psB64(domaine)],
        ['FenetreJours', psB64(String(v.dnsAdJours))],
        ['Format', psB64(v.dnsAdFormat)],
      ],
      corps,
      prefixeFichier: 'kjemo-ad-dns',
    });
  },
  gui: [
    'Gestionnaire DNS (dnsmgmt.msc) sur un contrôleur : zone du domaine, dossier _msdcs, zones de recherche inversée.',
    'Clic droit sur le serveur > Propriétés > Redirecteurs.',
    'Dans la zone du domaine, vérifier la présence des dossiers _ldap, _kerberos et _gc sous _tcp.',
    'Services (services.msc) : Netlogon doit être démarré. Le redémarrer republie les enregistrements SRV.',
    'Paramètres réseau du contrôleur : un autre contrôleur en premier serveur DNS, lui-même en dernier.',
  ],
  keywords: [
    'srv', '_ldap._tcp', '_msdcs', 'netlogon', 'dns active directory', 'zone inverse',
    'ptr', 'redirecteur', 'dcdiag dns', 'impossible de rejoindre le domaine',
    'resolution du domaine', 'controleur introuvable',
  ],
  requiresAdmin: true,
  os: OS_AD,
  prereqs: PREREQS_AD.concat([
    'Les contrôles de zones et de redirecteurs demandent le rôle Serveur DNS : hors d’un contrôleur, ils sont déclarés « non applicable », jamais en échec.',
    'La lecture des journaux d’événements demande des droits d’administration locale.',
  ]),
  commonErrors: ERREURS_AD.concat([
    {
      message: 'Resolve-DnsName : _ldap._tcp.dc._msdcs.<domaine> : DNS name does not exist',
      code: 'DNS_ERROR_RR_NOT_FOUND',
      cause: 'Les enregistrements SRV du domaine ne sont pas publiés, ou la machine interroge un serveur DNS qui n’héberge pas la zone.',
      fix: 'Sur un contrôleur, redémarrer le service Netlogon pour republier. Sur un client, corriger les serveurs DNS déclarés.',
      command: 'Restart-Service Netlogon',
    },
    {
      message: 'Un serveur DNS public (8.8.8.8) est déclaré sur une carte du serveur',
      cause: 'Confusion fréquente entre serveur DNS de la carte réseau et redirecteur du service DNS.',
      fix: 'Retirer le serveur public de la carte, et le déclarer comme redirecteur dans le gestionnaire DNS.',
    },
    {
      message: 'dcdiag signale « failed test DNS » sur une machine membre',
      cause: 'dcdiag teste un contrôleur de domaine. Sur une machine membre, l’échec ne décrit rien de réel.',
      fix: 'Ce script ne lance dcdiag que si la machine est réellement un contrôleur : ailleurs, le test est marqué « non applicable ».',
    },
    {
      message: 'La résolution directe renvoie une adresse que l’annuaire ne connaît pas',
      cause: 'Enregistrement A périmé, généralement après un changement d’adresse du contrôleur.',
      fix: 'Supprimer l’enregistrement obsolète dans le gestionnaire DNS, puis redémarrer Netlogon sur le contrôleur concerné.',
    },
  ]),
  reversible: true,
  verifyAfter: [
    'Les six enregistrements SRV attendus sont présents et pointent vers des contrôleurs connus.',
    'Chaque contrôleur de l’annuaire se résout, et vers la même adresse que celle qu’il déclare.',
    'Aucun serveur DNS public n’est déclaré sur les cartes réseau des machines de domaine.',
  ],
  rollback: {
    summary: 'Ce script ne modifie rien : il interroge le DNS et lit des journaux. Les seules traces sont le rapport et, sur un contrôleur, la copie brute de la sortie dcdiag.',
    diagnostic: '# Retrouver les fichiers produits par cet outil.\nGet-ChildItem -Path ([Environment]::GetFolderPath(\'Desktop\')) -Filter \'kjemo-ad-d*\' | Format-Table Name,Length,LastWriteTime',
    command: '# Retirer les rapports devenus inutiles.\nGet-ChildItem -Path ([Environment]::GetFolderPath(\'Desktop\')) -Filter \'kjemo-ad-dns*\' | Remove-Item -Confirm',
    warning: 'La sortie brute de dcdiag nomme les contrôleurs, leurs adresses et leurs partitions : elle se range comme un document interne.',
  },
  checks: [
    'Commencer par les enregistrements SRV : leur absence explique la plupart des pannes de domaine.',
    'Vérifier les serveurs DNS déclarés sur les cartes, pas seulement les redirecteurs.',
    'Un contrôle « non applicable » n’est pas un échec : il signale seulement que la machine n’est pas un contrôleur.',
  ],
  source: 'https://learn.microsoft.com/troubleshoot/windows-server/active-directory/troubleshoot-dns-event-id-4013-dns-server',
  sources: [
    { label: 'Resolve-DnsName', url: 'https://learn.microsoft.com/powershell/module/dnsclient/resolve-dnsname' },
    { label: 'Get-DnsServerZone', url: 'https://learn.microsoft.com/powershell/module/dnsserver/get-dnsserverzone' },
    { label: 'Dépanner l’événement DNS 4013', url: 'https://learn.microsoft.com/troubleshoot/windows-server/active-directory/troubleshoot-dns-event-id-4013-dns-server' },
    { label: 'Get-ADDomainController', url: 'https://learn.microsoft.com/powershell/module/activedirectory/get-addomaincontroller' },
  ],
};

/**
 * 12. ad-dcdiag-report — dcdiag, conservé brut et rendu lisible.
 *
 * dcdiag est l'outil de référence, et sa sortie est longue et aride. Ce script
 * la conserve telle quelle — c'est elle qui fait foi — puis en tire un tableau
 * lisible. Un point important : dcdiag renvoie souvent 0 même quand un test a
 * échoué. Le code de sortie ne conclut donc rien ; seules les lignes comptent.
 */
export const outilDcdiag = {
  id: 'ad-dcdiag-report',
  icon: '≡',
  category: 'Active Directory',
  subcategory: 'Santé du domaine',
  title: 'Rapport dcdiag interprété',
  risk: 'diagnostic',
  summary: 'Exécute dcdiag sur un contrôleur, conserve la sortie brute intacte, puis distingue les tests réussis, en avertissement et en échec, avec une explication des échecs les plus fréquents.',
  fields: [
    champDomaine(),
    {
      id: 'dcdCible', label: 'Contrôleur examiné (facultatif)', default: '',
      help: 'Nom court ou nom complet d’un contrôleur. Laisse vide pour examiner celui sur lequel le script s’exécute.',
    },
    {
      id: 'dcdEtendue', label: 'Étendue des tests', type: 'select', default: 'Standard',
      options: [
        ['Standard', 'Standard — la batterie par défaut'],
        ['Complet', 'Complet — ajoute les tests longs, dont la réplication et la topologie'],
      ],
      help: 'L’étendue complète interroge les autres contrôleurs : elle est nettement plus lente.',
    },
    champFormat('dcdFormat'),
  ],
  validate(v) {
    const errors = {};
    verifier(errors, 'adDomain', validerFqdn(v.adDomain));
    const cible = String(v.dcdCible ?? '').trim();
    if (cible !== '') {
      if (cible.includes('.')) verifier(errors, 'dcdCible', validerFqdn(cible));
      else verifier(errors, 'dcdCible', validerNomHote(cible));
    }
    verifier(errors, 'dcdEtendue', validerChoix(v.dcdEtendue, ['Standard', 'Complet'], 'L’étendue'));
    verifier(errors, 'dcdFormat', validerFormatRapport(v.dcdFormat));
    return errors;
  },
  generate(v) {
    assertValid(this, v);
    const domaine = validerFqdn(v.adDomain).value;
    const cible = String(v.dcdCible ?? '').trim();

    const corps = [
      `$Domaine = ${psB64(domaine)}`,
      `$Cible = ${psB64(cible)}`,
      `$Etendue = ${psB64(v.dcdEtendue)}`,
      `$KjemoFormat = ${psB64(v.dcdFormat)}`,
      "",
      blocModuleAd(),
      "",
      '# --- 1. dcdiag est-il disponible, et ici a-t-il un sens ? --------------------',
      '$estDC = $false',
      'try {',
      '  $role = (Get-CimInstance Win32_ComputerSystem -ErrorAction Stop).DomainRole',
      '  if ($role -eq 4 -or $role -eq 5) { $estDC = $true }',
      '} catch { }',
      '$outil = Get-Command dcdiag.exe -ErrorAction SilentlyContinue',
      'if (-not $outil) {',
      "  [void](Add-KjemoResultat -Categorie 'Prerequis' -Controle 'dcdiag.exe' -Etat 'PROBLEME' -Valeur 'introuvable' -Commentaire 'dcdiag vient des outils d''administration AD DS. Sur un poste de gestion : RSAT-ADDS-Tools.')",
      '} else {',
      "  [void](Add-KjemoResultat -Categorie 'Prerequis' -Controle 'dcdiag.exe' -Etat 'OK' -Valeur $outil.Source)",
      '}',
      "if (-not $estDC -and $Cible -eq '') {",
      "  [void](Add-KjemoResultat -Categorie 'Prerequis' -Controle 'Cible' -Etat 'ATTENTION' -Valeur 'machine non controleur et aucune cible indiquee' -Commentaire 'dcdiag va tester cette machine, qui n''est pas un controleur : les echecs obtenus ne decriront rien de reel. Indique un controleur.')",
      '}',
      "",
      '# --- 2. Execution -------------------------------------------------------------',
      "# Les arguments sont passes un a un, jamais concatenes dans une chaine",
      "# interpretee : aucune construction dynamique de commande.",
      '$argsDcdiag = New-Object System.Collections.ArrayList',
      "if ($Cible -ne '') { [void]$argsDcdiag.Add(\"/s:$Cible\") }",
      "if ($Etendue -eq 'Complet') {",
      "  [void]$argsDcdiag.Add('/c')",
      "  [void]$argsDcdiag.Add('/v')",
      '}',
      "[void]$argsDcdiag.Add('/e:no')",
      '$brut = $null',
      '$codeSortie = $null',
      '$debutDcdiag = Get-Date',
      'if ($outil) {',
      '  try {',
      '    $brut = & dcdiag.exe @argsDcdiag 2>&1 | Out-String',
      '    $codeSortie = $LASTEXITCODE',
      '  } catch {',
      "    [void](Add-KjemoResultat -Categorie 'Execution' -Controle 'dcdiag' -Etat 'PROBLEME' -Valeur $_.Exception.Message)",
      '  }',
      '}',
      '$dureeDcdiag = ((Get-Date) - $debutDcdiag).TotalSeconds',
      "",
      '# --- 3. La sortie brute fait foi ---------------------------------------------',
      'if ($brut) {',
      "  $dossier = [Environment]::GetFolderPath('Desktop')",
      "  $horodatage = (Get-Date).ToString('yyyyMMdd-HHmmss')",
      '  $chemin = Join-Path $dossier "kjemo-ad-dcdiag-brut-$horodatage.txt"',
      '  try {',
      '    $brut | Out-File -FilePath $chemin -Encoding UTF8',
      "    [void](Add-KjemoResultat -Categorie 'Sortie brute' -Controle 'Fichier conserve' -Etat 'OK' -Valeur $chemin -Commentaire 'C''est cette sortie qui fait foi. L''interpretation ci-dessous en est un resume, pas un remplacement.')",
      '  } catch {',
      "    [void](Add-KjemoResultat -Categorie 'Sortie brute' -Controle 'Fichier conserve' -Etat 'ATTENTION' -Valeur $_.Exception.Message)",
      '  }',
      '  [void](Add-KjemoResultat -Categorie \'Execution\' -Controle \'Duree\' -Etat \'INFO\' -Valeur ("{0:N1} seconde(s)" -f $dureeDcdiag))',
      '}',
      "",
      '# --- 4. Le code de sortie ne conclut rien -------------------------------------',
      "# dcdiag renvoie frequemment 0 alors que des tests ont echoue. Un domaine",
      "# n'est donc JAMAIS declare sain sur la foi du code de sortie : ce sont les",
      '# lignes « passed test » et « failed test » qui comptent.',
      'if ($codeSortie -ne $null) {',
      "  [void](Add-KjemoResultat -Categorie 'Execution' -Controle 'Code de sortie' -Etat 'INFO' -Valeur $codeSortie -Commentaire 'Valeur indicative seulement : dcdiag renvoie souvent 0 malgre des tests en echec. La conclusion vient de l''analyse des lignes.')",
      '}',
      "",
      '# --- 5. Analyse ligne par ligne -----------------------------------------------',
      '$reussis = @()',
      '$echoues = @()',
      '$avertissements = @()',
      'if ($brut) {',
      "  $reussis = @([regex]::Matches($brut, '(?m)^\\s*\\.+\\s*(\\S+)\\s+passed test\\s+(\\S+)'))",
      "  $echoues = @([regex]::Matches($brut, '(?m)^\\s*\\.+\\s*(\\S+)\\s+failed test\\s+(\\S+)'))",
      "  $avertissements = @([regex]::Matches($brut, '(?m)^.*\\b(Warning|Avertissement)\\b.*$'))",
      '}',
      '[void](Add-KjemoResultat -Categorie \'Resultats\' -Controle \'Tests reussis\' -Etat \'OK\' -Valeur $reussis.Count)',
      "$etat = 'OK'",
      "if ($echoues.Count -gt 0) { $etat = 'PROBLEME' }",
      "[void](Add-KjemoResultat -Categorie 'Resultats' -Controle 'Tests en echec' -Etat $etat -Valeur $echoues.Count)",
      "$etat = 'OK'",
      "if ($avertissements.Count -gt 0) { $etat = 'ATTENTION' }",
      "[void](Add-KjemoResultat -Categorie 'Resultats' -Controle 'Lignes d''avertissement' -Etat $etat -Valeur $avertissements.Count)",
      'if (-not $brut) {',
      "  [void](Add-KjemoResultat -Categorie 'Resultats' -Controle 'Conclusion' -Etat 'PROBLEME' -Valeur 'aucune sortie analysable' -Commentaire 'Sans sortie, rien ne peut etre conclu : ni bon, ni mauvais.')",
      '} elseif ($echoues.Count -eq 0 -and $reussis.Count -eq 0) {',
      "  [void](Add-KjemoResultat -Categorie 'Resultats' -Controle 'Conclusion' -Etat 'ATTENTION' -Valeur 'aucune ligne de test reconnue' -Commentaire 'Sortie dans une langue ou un format non reconnus par l''analyse. La sortie brute conservee reste utilisable.')",
      '}',
      "",
      '# --- 6. Ce que veulent dire les echecs frequents ------------------------------',
      "$explications = @{}",
      "$explications['Connectivity'] = 'Le controleur ne repond pas : resolution DNS, pare-feu, ou machine arretee. Tous les autres tests en dependent.'",
      "$explications['Advertising'] = 'Le controleur ne s''annonce pas comme tel. Netlogon, ou les enregistrements SRV, ou SYSVOL non partage.'",
      "$explications['NetLogons'] = 'Les partages NETLOGON et SYSVOL ne sont pas accessibles. Sans eux, aucune strategie de groupe ne s''applique.'",
      "$explications['Replications'] = 'La replication entre controleurs echoue. A completer avec le rapport repadmin.'",
      "$explications['SysVolCheck'] = 'SYSVOL n''est pas dans l''etat attendu : replication DFSR arretee, ou partage manquant.'",
      "$explications['FrsEvent'] = 'Erreurs de replication de fichiers dans le journal. Anciennement FRS, aujourd''hui DFSR.'",
      "$explications['DFSREvent'] = 'Le service de replication DFSR signale des erreurs sur SYSVOL.'",
      "$explications['KccEvent'] = 'Le verificateur de coherence des connexions ne parvient pas a construire la topologie de replication.'",
      "$explications['MachineAccount'] = 'Le compte machine du controleur est incoherent dans l''annuaire.'",
      "$explications['Services'] = 'Un service indispensable est arrete : Netlogon, KDC, DNS, DFSR ou NTDS.'",
      "$explications['SystemLog'] = 'Des erreurs recentes figurent dans le journal Systeme. A lire, sans conclure d''emblee.'",
      "$explications['CheckSDRefDom'] = 'Incoherence dans les domaines de reference des descripteurs de securite.'",
      "$explications['CrossRefValidation'] = 'Une reference croisee de partition est incoherente dans la configuration de la foret.'",
      'foreach ($m in $echoues) {',
      '  $nomTest = $m.Groups[2].Value',
      '  $surQuoi = $m.Groups[1].Value',
      "  $note = 'Test non documente dans cet outil : lire la sortie brute conservee.'",
      '  if ($explications.ContainsKey($nomTest)) { $note = $explications[$nomTest] }',
      '  [void](Add-KjemoResultat -Categorie \'Echecs\' -Controle ("$nomTest sur $surQuoi") -Etat \'PROBLEME\' -Valeur \'failed test\' -Commentaire $note)',
      '}',
      'foreach ($m in ($avertissements | Select-Object -First 15)) {',
      '  [void](Add-KjemoResultat -Categorie \'Avertissements\' -Controle \'Ligne signalee\' -Etat \'ATTENTION\' -Valeur ($m.Value.Trim()))',
      '}',
      "",
      "Write-Host ''",
      "Write-Host '--- Portee de ce rapport ---'",
      "Write-Host 'Ce rapport interprete la sortie de dcdiag ; il ne la remplace pas. Un'",
      "Write-Host 'domaine n''est pas declare sain parce que le code de sortie vaut zero :'",
      "Write-Host 'seules les lignes de test comptent, et la sortie brute conservee fait foi.'",
    ];

    return assembler({
      titre: 'Rapport dcdiag interprete',
      outil: 'ad-dcdiag-report',
      diagnostic: true,
      admin: true,
      parametres: [
        ['Domaine', psB64(domaine)],
        ['Cible', psB64(cible)],
        ['Etendue', psB64(v.dcdEtendue)],
        ['Format', psB64(v.dcdFormat)],
      ],
      corps,
      prefixeFichier: 'kjemo-ad-dcdiag',
    });
  },
  gui: [
    'Il n’existe pas d’équivalent graphique complet à dcdiag.',
    'Observateur d’événements (eventvwr.msc) : journaux Directory Service, DFS Replication, DNS Server et Système.',
    'Sites et services Active Directory (dssite.msc) : topologie et connexions de réplication.',
    'Gestionnaire de serveur > AD DS : les alertes de rôle reprennent une partie des mêmes constats.',
  ],
  keywords: [
    'dcdiag', 'controleur de domaine', 'diagnostic ad', 'failed test', 'advertising',
    'netlogons', 'sysvolcheck', 'kccevent', 'replications', 'sante du domaine',
  ],
  requiresAdmin: true,
  os: OS_AD,
  prereqs: PREREQS_AD.concat([
    'dcdiag.exe, fourni par les outils d’administration AD DS (RSAT-ADDS-Tools) ou présent d’office sur un contrôleur.',
    'Droits d’administration sur le contrôleur examiné.',
    'En étendue complète, les autres contrôleurs sont interrogés : prévoir plusieurs minutes.',
  ]),
  commonErrors: ERREURS_AD.concat([
    {
      message: 'dcdiag : « failed test Connectivity »',
      cause: 'Le contrôleur ne répond pas : résolution DNS incorrecte, pare-feu, ou machine arrêtée.',
      fix: 'Commencer par la santé DNS d’Active Directory : tant que Connectivity échoue, les autres tests ne veulent rien dire.',
    },
    {
      message: 'dcdiag : « failed test Advertising »',
      cause: 'Le contrôleur ne s’annonce pas : Netlogon arrêté, enregistrements SRV absents, ou SYSVOL non partagé.',
      fix: 'Vérifier le service Netlogon, puis les partages SYSVOL et NETLOGON.',
      command: 'Get-Service Netlogon ; Get-SmbShare | Where-Object { $_.Name -in @(\'SYSVOL\',\'NETLOGON\') }',
    },
    {
      message: 'dcdiag renvoie le code 0 alors que des tests ont échoué',
      cause: 'Le code de sortie de dcdiag ne reflète pas fidèlement le résultat des tests.',
      fix: 'Ne jamais conclure sur le code de sortie. Ce script conclut sur les lignes « passed test » et « failed test », et le dit explicitement.',
    },
    {
      message: 'Aucune ligne de test n’est reconnue par l’analyse',
      cause: 'La sortie de dcdiag est produite dans une langue dont les libellés diffèrent de ceux recherchés.',
      fix: 'La sortie brute est conservée sur le Bureau : elle reste entièrement lisible et utilisable.',
    },
  ]),
  reversible: true,
  verifyAfter: [
    'Un fichier texte contenant la sortie brute est écrit sur le Bureau, et son chemin figure dans le rapport.',
    'Le nombre de tests réussis et en échec est cohérent avec ce que montre la sortie brute.',
    'Le rapport rappelle explicitement que le code de sortie ne conclut rien.',
  ],
  rollback: {
    summary: 'dcdiag est un outil de lecture : il ne modifie ni l’annuaire ni la configuration. Les seules traces sont le rapport et la copie brute.',
    diagnostic: '# Retrouver les sorties brutes conservees, la plus recente en premier.\nGet-ChildItem -Path ([Environment]::GetFolderPath(\'Desktop\')) -Filter \'kjemo-ad-dcdiag*\' | Sort-Object LastWriteTime -Descending | Format-Table Name,Length,LastWriteTime',
    command: '# Retirer les sorties devenues inutiles.\nGet-ChildItem -Path ([Environment]::GetFolderPath(\'Desktop\')) -Filter \'kjemo-ad-dcdiag*\' | Remove-Item -Confirm',
    warning: 'La sortie brute décrit la topologie du domaine, les noms des contrôleurs et leurs partitions : elle se range comme un document interne.',
  },
  checks: [
    'Lancer sur un contrôleur, ou indiquer un contrôleur : ailleurs, les échecs ne décrivent rien.',
    'Lire les échecs dans l’ordre : Connectivity d’abord, le reste en dépend.',
    'La sortie brute conservée fait foi ; le résumé est une aide à la lecture.',
  ],
  source: 'https://learn.microsoft.com/windows-server/administration/windows-commands/dcdiag',
  sources: [
    { label: 'dcdiag', url: 'https://learn.microsoft.com/windows-server/administration/windows-commands/dcdiag' },
    { label: 'Dépanner la réplication Active Directory', url: 'https://learn.microsoft.com/troubleshoot/windows-server/active-directory/troubleshoot-ad-replication-error-8524' },
    { label: 'Get-Command', url: 'https://learn.microsoft.com/powershell/module/microsoft.powershell.core/get-command' },
  ],
};

/**
 * 13. ad-repadmin-report — la réplication, vue des deux côtés.
 *
 * Une réplication qui s'arrête ne se voit pas tout de suite : les contrôleurs
 * continuent de répondre, chacun avec sa version de l'annuaire. Puis un mot de
 * passe changé ici n'est pas reconnu là, et un groupe modifié disparaît.
 * Ce script relève l'état, conserve la sortie brute, et traduit les codes
 * d'erreur les plus courants.
 */
export const outilRepadmin = {
  id: 'ad-repadmin-report',
  icon: '⇄',
  category: 'Active Directory',
  subcategory: 'Santé du domaine',
  title: 'État de la réplication entre contrôleurs',
  risk: 'diagnostic',
  summary: 'Relève l’état de la réplication avec repadmin, partenaire par partenaire : dernière réussite, échecs consécutifs, latence, contrôleurs injoignables, et traduction des erreurs RPC et DNS fréquentes.',
  fields: [
    champDomaine(),
    {
      id: 'repLatence', label: 'Latence jugée anormale au-delà de (heures)', default: '24',
      help: 'Une réplication saine se fait en minutes à l’intérieur d’un site, et selon la planification entre sites.',
    },
    champFormat('repFormat'),
  ],
  validate(v) {
    const errors = {};
    verifier(errors, 'adDomain', validerFqdn(v.adDomain));
    verifier(errors, 'repLatence', validerEntier(v.repLatence, 1, 720, 'La latence'));
    verifier(errors, 'repFormat', validerFormatRapport(v.repFormat));
    return errors;
  },
  generate(v) {
    assertValid(this, v);
    const domaine = validerFqdn(v.adDomain).value;

    const corps = [
      `$Domaine = ${psB64(domaine)}`,
      `$LatenceMax = ${validerEntier(v.repLatence, 1, 720).value}`,
      `$KjemoFormat = ${psB64(v.repFormat)}`,
      "",
      blocModuleAd(),
      "",
      '# --- 1. Les controleurs du domaine -------------------------------------------',
      '$controleurs = @()',
      'if ($moduleOk) {',
      '  try { $controleurs = @(Get-ADDomainController -Filter * -Server $Domaine -ErrorAction SilentlyContinue) } catch { }',
      '}',
      "$etat = 'OK'",
      "if ($controleurs.Count -eq 0) { $etat = 'PROBLEME' }",
      "if ($controleurs.Count -eq 1) { $etat = 'ATTENTION' }",
      "[void](Add-KjemoResultat -Categorie 'Topologie' -Controle 'Controleurs du domaine' -Etat $etat -Valeur $controleurs.Count -Commentaire 'Avec un seul controleur, il n''y a rien a repliquer — et aucune tolerance de panne.')",
      'foreach ($dc in $controleurs) {',
      '  [void](Add-KjemoResultat -Categorie \'Topologie\' -Controle $dc.HostName -Etat \'INFO\' -Valeur ("site : $($dc.Site) ; catalogue global : $($dc.IsGlobalCatalog) ; systeme : $($dc.OperatingSystem)"))',
      '}',
      "",
      '# --- 2. Etat de replication, par le module ActiveDirectory --------------------',
      "# Get-ADReplicationPartnerMetadata donne la meme information que repadmin,",
      '# sous forme d\'objets : c\'est la source principale de ce rapport.',
      '$partenaires = @()',
      'if ($moduleOk) {',
      '  try {',
      '    $partenaires = @(Get-ADReplicationPartnerMetadata -Target $Domaine -Scope Domain -ErrorAction SilentlyContinue)',
      '  } catch {',
      "    [void](Add-KjemoResultat -Categorie 'Replication' -Controle 'Lecture des metadonnees' -Etat 'ATTENTION' -Valeur $_.Exception.Message)",
      '  }',
      '}',
      "[void](Add-KjemoResultat -Categorie 'Replication' -Controle 'Liens examines' -Etat 'INFO' -Valeur $partenaires.Count)",
      "",
      '$limiteLatence = (Get-Date).AddHours(-$LatenceMax)',
      '$liensEnEchec = 0',
      '$liensEnRetard = 0',
      'foreach ($p in $partenaires) {',
      "  $etatL = 'OK'",
      "  $note = ''",
      '  if ($p.ConsecutiveReplicationFailures -gt 0) {',
      '    $liensEnEchec = $liensEnEchec + 1',
      "    $etatL = 'PROBLEME'",
      '    $note = "Echecs consecutifs : $($p.ConsecutiveReplicationFailures). Dernier code : $($p.LastReplicationResult)."',
      '  } elseif ($p.LastReplicationSuccess -ne $null -and $p.LastReplicationSuccess -lt $limiteLatence) {',
      '    $liensEnRetard = $liensEnRetard + 1',
      "    $etatL = 'ATTENTION'",
      '    $note = "Aucune replication reussie depuis plus de $LatenceMax heure(s) sur ce lien."',
      '  }',
      "  $quand = 'jamais'",
      "  if ($p.LastReplicationSuccess -ne $null) { $quand = $p.LastReplicationSuccess.ToString('yyyy-MM-dd HH:mm') }",
      '  [void](Add-KjemoResultat -Categorie \'Replication\' -Controle ("$($p.Server) <- $($p.PartnerAddress)") -Etat $etatL -Valeur ("partition : $($p.Partition) ; derniere reussite : $quand") -Commentaire $note)',
      '}',
      "",
      '# --- 3. Traduction des codes d\'erreur frequents -------------------------------',
      '$codes = @{}',
      "$codes[1722] = 'Le serveur RPC n''est pas disponible. Pare-feu, machine arretee, ou ports RPC dynamiques filtres entre les deux controleurs.'",
      "$codes[8524] = 'Echec de recherche DNS lors de l''appel a l''annuaire. Le GUID du controleur ne se resout pas dans la zone _msdcs.'",
      "$codes[8453] = 'Acces de replication refuse. Droits de replication manquants, ou compte machine du controleur incoherent.'",
      "$codes[8606] = 'Attributs insuffisants pour creer l''objet : symptome classique d''un objet en attente, ou lingering object.'",
      "$codes[8614] = 'Le controleur n''a pas replique depuis plus longtemps que la duree de vie des objets supprimes. Il ne doit PAS etre remis en ligne tel quel.'",
      "$codes[5] = 'Acces refuse.'",
      "$codes[58] = 'Le serveur specifie ne peut pas executer l''operation demandee.'",
      'foreach ($p in ($partenaires | Where-Object { $_.LastReplicationResult -ne 0 })) {',
      '  $code = [int]$p.LastReplicationResult',
      "  $explication = 'Code non documente dans cet outil. Voir la sortie brute de repadmin et la documentation Microsoft.'",
      '  if ($codes.ContainsKey($code)) { $explication = $codes[$code] }',
      '  [void](Add-KjemoResultat -Categorie \'Erreurs\' -Controle ("$($p.Server) <- $($p.PartnerAddress)") -Etat \'PROBLEME\' -Valeur ("code $code") -Commentaire $explication)',
      '}',
      "",
      '# --- 4. Controleurs injoignables ----------------------------------------------',
      '$injoignables = 0',
      'foreach ($dc in $controleurs) {',
      '  $joignable = $false',
      '  try { $joignable = Test-Connection -ComputerName $dc.HostName -Count 1 -Quiet -ErrorAction SilentlyContinue } catch { }',
      '  if (-not $joignable) {',
      '    $injoignables = $injoignables + 1',
      "    [void](Add-KjemoResultat -Categorie 'Joignabilite' -Controle $dc.HostName -Etat 'ATTENTION' -Valeur 'ne repond pas a l''echo' -Commentaire 'L''echo peut etre bloque par le pare-feu sans que le controleur soit en panne. A confirmer avec le test des ports.')",
      '  } else {',
      "    [void](Add-KjemoResultat -Categorie 'Joignabilite' -Controle $dc.HostName -Etat 'OK' -Valeur 'repond')",
      '  }',
      '}',
      "",
      '# --- 5. Sortie brute de repadmin, conservee telle quelle ----------------------',
      '$repadmin = Get-Command repadmin.exe -ErrorAction SilentlyContinue',
      'if ($repadmin) {',
      "  $dossier = [Environment]::GetFolderPath('Desktop')",
      "  $horodatage = (Get-Date).ToString('yyyyMMdd-HHmmss')",
      '  $chemin = Join-Path $dossier "kjemo-ad-repadmin-brut-$horodatage.txt"',
      '  $brut = $null',
      '  try {',
      "    $resume = & repadmin.exe '/replsummary' 2>&1 | Out-String",
      "    $detail = & repadmin.exe '/showrepl' '*' '/csv' 2>&1 | Out-String",
      '    $brut = "=== repadmin /replsummary ===`r`n" + $resume + "`r`n=== repadmin /showrepl * /csv ===`r`n" + $detail',
      '  } catch {',
      "    [void](Add-KjemoResultat -Categorie 'repadmin' -Controle 'Execution' -Etat 'ATTENTION' -Valeur $_.Exception.Message)",
      '  }',
      '  if ($brut) {',
      '    try {',
      '      $brut | Out-File -FilePath $chemin -Encoding UTF8',
      "      [void](Add-KjemoResultat -Categorie 'repadmin' -Controle 'Sortie brute conservee' -Etat 'OK' -Valeur $chemin -Commentaire 'Sortie non retouchee de repadmin : c''est elle qui fait foi.')",
      '    } catch { }',
      "    $lignesErreur = @([regex]::Matches($brut, '(?m)^.*\\b(error|erreur|failed|echec)\\b.*$'))",
      "    $etatR = 'OK'",
      "    if ($lignesErreur.Count -gt 0) { $etatR = 'ATTENTION' }",
      "    [void](Add-KjemoResultat -Categorie 'repadmin' -Controle 'Lignes signalant une erreur' -Etat $etatR -Valeur $lignesErreur.Count -Commentaire 'Compte indicatif : la lecture de la sortie brute reste necessaire.')",
      '  }',
      '} else {',
      "  [void](Add-KjemoResultat -Categorie 'repadmin' -Controle 'repadmin.exe' -Etat 'IGNORE' -Valeur 'introuvable' -Commentaire 'Le rapport repose alors uniquement sur le module ActiveDirectory, qui donne la meme information.')",
      '}',
      "",
      '# --- 6. Conclusion --------------------------------------------------------------',
      "$etat = 'OK'",
      "if ($liensEnRetard -gt 0) { $etat = 'ATTENTION' }",
      "if ($liensEnEchec -gt 0) { $etat = 'PROBLEME' }",
      "[void](Add-KjemoResultat -Categorie 'Conclusion' -Controle 'Etat de la replication' -Etat $etat -Valeur (\"$liensEnEchec lien(s) en echec, $liensEnRetard en retard, $injoignables controleur(s) sans reponse a l''echo\"))",
      "[void](Add-KjemoResultat -Categorie 'Conclusion' -Controle 'Duree de vie des objets supprimes' -Etat 'INFO' -Valeur 'a verifier avant toute remise en ligne' -Commentaire 'Un controleur qui n''a pas replique depuis plus longtemps que cette duree ne doit pas etre rebranche : il reintroduirait des objets supprimes. Il se reconstruit, il ne se repare pas.')",
    ];

    return assembler({
      titre: 'Etat de la replication entre controleurs',
      outil: 'ad-repadmin-report',
      diagnostic: true,
      admin: true,
      parametres: [
        ['Domaine', psB64(domaine)],
        ['LatenceMaxHeures', psB64(String(v.repLatence))],
        ['Format', psB64(v.repFormat)],
      ],
      corps,
      prefixeFichier: 'kjemo-ad-replication',
    });
  },
  gui: [
    'Sites et services Active Directory (dssite.msc) > Sites > serveurs > NTDS Settings.',
    'Clic droit sur une connexion > Répliquer maintenant : force une réplication et affiche l’erreur éventuelle.',
    'Observateur d’événements > Directory Service : les erreurs de réplication y sont journalisées.',
    'Gestionnaire de serveur > AD DS : les alertes de rôle reprennent les échecs les plus visibles.',
  ],
  keywords: [
    'repadmin', 'replication', 'replsummary', 'showrepl', 'erreur 1722', 'rpc',
    'erreur 8524', 'lingering object', 'tombstone', 'latence de replication',
    'controleur desynchronise', 'kcc',
  ],
  requiresAdmin: true,
  os: OS_AD,
  prereqs: PREREQS_AD.concat([
    'Droits de lecture des métadonnées de réplication : administrateur du domaine, ou délégation équivalente.',
    'repadmin.exe est utile mais pas indispensable : sans lui, le rapport repose sur le module ActiveDirectory.',
    'Les contrôleurs doivent être joignables pour que leur état soit relevé.',
  ]),
  commonErrors: ERREURS_AD.concat([
    {
      message: 'Erreur 1722 : le serveur RPC n’est pas disponible',
      code: '1722',
      cause: 'Le contrôleur ne répond pas sur les ports RPC : pare-feu, machine arrêtée, ou plage de ports dynamiques filtrée.',
      fix: 'Vérifier la joignabilité du port 135, puis la plage RPC dynamique entre les deux contrôleurs.',
      command: 'Test-NetConnection -ComputerName <controleur> -Port 135',
    },
    {
      message: 'Erreur 8524 : DNS lookup failure',
      code: '8524',
      cause: 'Le GUID du contrôleur ne se résout pas dans la zone _msdcs du domaine.',
      fix: 'Passer au diagnostic DNS d’Active Directory : c’est un problème de zone, pas de réplication.',
    },
    {
      message: 'Erreur 8614 : le contrôleur n’a pas répliqué depuis trop longtemps',
      code: '8614',
      cause: 'Le contrôleur est resté hors ligne plus longtemps que la durée de vie des objets supprimés.',
      fix: 'Ne pas le remettre en ligne tel quel : il réintroduirait des objets supprimés. Il se reconstruit, après retrait propre du domaine.',
    },
    {
      message: 'Un contrôleur ne répond pas à l’écho mais fonctionne',
      cause: 'Le pare-feu bloque ICMP sans bloquer les ports de service.',
      fix: 'Confirmer avec un test de port plutôt qu’avec un écho : c’est ce que dit le rapport.',
      command: 'Test-NetConnection -ComputerName <controleur> -Port 389',
    },
  ]),
  reversible: true,
  verifyAfter: [
    'Le rapport annonce le nombre de liens de réplication examinés et l’état de chacun.',
    'Une sortie brute de repadmin est conservée sur le Bureau quand l’outil est présent.',
    'Chaque code d’erreur relevé est accompagné de ce qu’il signifie.',
  ],
  rollback: {
    summary: 'Ce script lit l’état de la réplication ; il ne la déclenche pas et ne modifie aucune topologie. Rien à annuler, hormis les fichiers produits.',
    diagnostic: '# Retrouver les sorties brutes conservees.\nGet-ChildItem -Path ([Environment]::GetFolderPath(\'Desktop\')) -Filter \'kjemo-ad-replication*\' | Sort-Object LastWriteTime -Descending | Format-Table Name,Length,LastWriteTime',
    command: '# Retirer les rapports devenus inutiles.\nGet-ChildItem -Path ([Environment]::GetFolderPath(\'Desktop\')) -Filter \'kjemo-ad-replication*\' | Remove-Item -Confirm',
    warning: 'Le rapport décrit la topologie de réplication complète du domaine : quels contrôleurs parlent à quels autres, dans quels sites. Il se range comme un document interne.',
  },
  checks: [
    'Un lien en échec compte davantage qu’un lien en retard : commencer par les codes d’erreur.',
    'Un écho sans réponse ne prouve pas une panne : confirmer avec un test de port.',
    'Un contrôleur trop longtemps hors ligne ne se rebranche pas : il se reconstruit.',
  ],
  source: 'https://learn.microsoft.com/windows-server/administration/windows-commands/repadmin',
  sources: [
    { label: 'repadmin', url: 'https://learn.microsoft.com/windows-server/administration/windows-commands/repadmin' },
    { label: 'Get-ADReplicationPartnerMetadata', url: 'https://learn.microsoft.com/powershell/module/activedirectory/get-adreplicationpartnermetadata' },
    { label: 'Erreur de réplication 8524', url: 'https://learn.microsoft.com/troubleshoot/windows-server/active-directory/troubleshoot-ad-replication-error-8524' },
    { label: 'Erreur de réplication 1722', url: 'https://learn.microsoft.com/troubleshoot/windows-server/active-directory/replication-error-1722-rpc-server-unavailable' },
  ],
};

/**
 * 14. ad-secure-channel — le lien de confiance entre une machine et son domaine.
 *
 * Une machine de domaine possède un compte, avec un mot de passe qu'elle
 * renouvelle toute seule tous les trente jours. Quand ce mot de passe se
 * désynchronise — restauration d'instantané, machine rallumée après des mois,
 * objet recréé dans l'annuaire — la machine n'est plus reconnue : « la relation
 * d'approbation entre cette station de travail et le domaine principal a
 * échoué ».
 *
 * La réparation est simple, mais elle ne s'applique PAS à un contrôleur de
 * domaine : un contrôleur n'a pas de canal sécurisé au même sens, et tenter de
 * le réparer n'a pas de sens.
 */
export const outilCanalSecurise = {
  id: 'ad-secure-channel',
  icon: '⚭',
  category: 'Active Directory',
  subcategory: 'Santé du domaine',
  title: 'Vérifier et réparer le canal sécurisé du domaine',
  risk: 'caution',
  summary: 'Vérifie la relation d’approbation entre cette machine et son domaine, après avoir contrôlé le DNS, l’heure et la joignabilité d’un contrôleur. Ne répare jamais un contrôleur de domaine.',
  fields: [
    champDomaine(),
    {
      id: 'canDerive', label: 'Décalage d’horloge toléré (secondes)', default: '300',
      help: 'Kerberos refuse les tickets au-delà de cinq minutes d’écart par défaut : c’est la première cause de panne d’authentification.',
    },
    {
      id: 'canPorts', label: 'Tester les ports du domaine', type: 'select', default: 'Oui',
      options: [['Oui', 'Oui — RPC, LDAP, Kerberos, SMB, DNS'], ['Non', 'Non — la machine est isolée du réseau']],
    },
    champMode('Diagnostic n’écrit rien. Appliquer tente la réparation du canal, et demande les identifiants localement.'),
  ],
  validate(v) {
    const errors = {};
    verifier(errors, 'adDomain', validerFqdn(v.adDomain));
    verifier(errors, 'canDerive', validerEntier(v.canDerive, 30, 3600, 'Le décalage toléré'));
    verifier(errors, 'canPorts', validerChoix(v.canPorts, ['Oui', 'Non'], 'Le choix'));
    verifier(errors, 'mode', validerModeExecution(v.mode));
    return errors;
  },
  generate(v) {
    assertValid(this, v);
    const domaine = validerFqdn(v.adDomain).value;

    const corps = [
      `$Mode = ${psB64(v.mode)}`,
      `$Domaine = ${psB64(domaine)}`,
      `$DeriveMax = ${validerEntier(v.canDerive, 30, 3600).value}`,
      `$TesterPorts = ${psB64(v.canPorts)}`,
      "$KjemoFormat = 'Console'",
      "",
      '# --- 1. Quel est le role de cette machine ? ----------------------------------',
      "# La reponse decide si une reparation est envisageable. Un controleur de",
      "# domaine n'a pas de canal securise a reparer : la commande n'y a pas de sens,",
      '# et elle ne sera pas proposee.',
      '$role = -1',
      '$cs = $null',
      'try {',
      '  $cs = Get-CimInstance Win32_ComputerSystem -ErrorAction Stop',
      '  $role = $cs.DomainRole',
      '} catch { }',
      "$libelleRole = 'inconnu'",
      '$estDC = $false',
      '$estMembre = $false',
      "if ($role -eq 0) { $libelleRole = 'station de travail autonome' }",
      "elseif ($role -eq 1) { $libelleRole = 'station de travail membre du domaine' ; $estMembre = $true }",
      "elseif ($role -eq 2) { $libelleRole = 'serveur autonome' }",
      "elseif ($role -eq 3) { $libelleRole = 'serveur membre du domaine' ; $estMembre = $true }",
      "elseif ($role -eq 4) { $libelleRole = 'controleur de domaine secondaire' ; $estDC = $true }",
      "elseif ($role -eq 5) { $libelleRole = 'controleur de domaine principal' ; $estDC = $true }",
      "[void](Add-KjemoResultat -Categorie 'Contexte' -Controle 'Role de la machine' -Etat 'INFO' -Valeur $libelleRole)",
      'if ($cs) {',
      '  [void](Add-KjemoResultat -Categorie \'Contexte\' -Controle \'Domaine declare\' -Etat \'INFO\' -Valeur $cs.Domain -Commentaire ("attendu : $Domaine"))',
      '}',
      'if ($estDC) {',
      "  [void](Add-KjemoResultat -Categorie 'Contexte' -Controle 'Reparation' -Etat 'IGNORE' -Valeur 'sans objet sur un controleur de domaine' -Commentaire 'Un controleur ne possede pas de canal securise de machine membre. Aucune reparation ne sera tentee ici, meme en mode Appliquer. Un probleme d''authentification sur un controleur se traite par la replication et le DNS.')",
      '}',
      'if (-not $estMembre -and -not $estDC) {',
      "  [void](Add-KjemoResultat -Categorie 'Contexte' -Controle 'Appartenance' -Etat 'PROBLEME' -Valeur 'machine hors domaine' -Commentaire 'Sans appartenance au domaine, il n''y a pas de canal securise. La machine doit d''abord rejoindre le domaine, ce qui est une operation distincte.')",
      '}',
      "",
      '# --- 2. Avant toute chose : le DNS et un controleur joignable ----------------',
      "# Reparer le canal sans controleur joignable echoue toujours, et le message",
      "# d'erreur ne dit pas pourquoi. On verifie donc d'abord.",
      '$srvOk = $false',
      '$cible = $null',
      'try {',
      '  $reponses = @(Resolve-DnsName -Name "_ldap._tcp.dc._msdcs.$Domaine" -Type SRV -ErrorAction SilentlyContinue | Where-Object { $_.QueryType -eq \'SRV\' })',
      '  if ($reponses -and $reponses.Count -gt 0) {',
      '    $srvOk = $true',
      '    $cible = $reponses[0].NameTarget',
      '  }',
      '} catch { }',
      'if ($srvOk) {',
      "  [void](Add-KjemoResultat -Categorie 'Prealables' -Controle 'Enregistrements SRV du domaine' -Etat 'OK' -Valeur $cible)",
      '} else {',
      "  [void](Add-KjemoResultat -Categorie 'Prealables' -Controle 'Enregistrements SRV du domaine' -Etat 'PROBLEME' -Valeur 'introuvables' -Commentaire 'La machine ne peut pas localiser de controleur. Corriger le DNS avant toute reparation : c''est souvent la vraie cause.')",
      '}',
      'if (-not $cible -and $cs -and $cs.PartOfDomain) {',
      '  try { $cible = (Get-CimInstance Win32_NTDomain -ErrorAction SilentlyContinue | Where-Object { $_.DomainName } | Select-Object -First 1).DomainControllerName } catch { }',
      "  if ($cible) { $cible = $cible.TrimStart('\\\\') }",
      '}',
      "",
      '# --- 3. Les ports dont depend l\'authentification -----------------------------',
      "if ($TesterPorts -eq 'Oui' -and $cible) {",
      "  $ports = @('135:RPC — mappeur de points de terminaison', '389:LDAP', '636:LDAPS', '88:Kerberos', '445:SMB — SYSVOL et NETLOGON', '53:DNS', '3268:Catalogue global')",
      '  foreach ($entree in $ports) {',
      "    $numero = [int]($entree.Split(':')[0])",
      "    $libelle = $entree.Split(':')[1]",
      '    $ouvert = $false',
      '    try { $ouvert = (Test-NetConnection -ComputerName $cible -Port $numero -WarningAction SilentlyContinue -InformationLevel Quiet) } catch { }',
      "    $etatP = 'PROBLEME'",
      "    if ($ouvert) { $etatP = 'OK' }",
      "    $noteP = ''",
      "    if (-not $ouvert -and $numero -eq 636) { $etatP = 'ATTENTION' ; $noteP = 'LDAPS est facultatif : il demande un certificat sur le controleur.' }",
      '    [void](Add-KjemoResultat -Categorie \'Ports\' -Controle ("$numero — $libelle") -Etat $etatP -Valeur ("vers $cible") -Commentaire $noteP)',
      '  }',
      '} else {',
      "  [void](Add-KjemoResultat -Categorie 'Ports' -Controle 'Test des ports' -Etat 'IGNORE' -Valeur 'non demande, ou aucun controleur identifie')",
      '}',
      "",
      '# --- 4. L\'heure : la cause la plus frequente et la moins soupconnee ----------',
      '$derive = $null',
      'if ($cible) {',
      '  try {',
      "    $sortie = & w32tm.exe '/stripchart' \"/computer:$cible\" '/samples:1' '/dataonly' 2>&1 | Out-String",
      "    $m = [regex]::Match($sortie, '([-+]?[0-9]+[\\.,][0-9]+)s')",
      "    if ($m.Success) { $derive = [Math]::Abs([double]($m.Groups[1].Value -replace ',', '.')) }",
      '  } catch { }',
      '}',
      'if ($derive -ne $null) {',
      "  $etatH = 'OK'",
      "  $noteH = ''",
      '  if ($derive -gt $DeriveMax) {',
      "    $etatH = 'PROBLEME'",
      "    $noteH = 'Au-dela de la tolerance, Kerberos refuse les tickets : l''ouverture de session echoue sans que rien ne designe l''heure.'",
      '  }',
      '  [void](Add-KjemoResultat -Categorie \'Horloge\' -Controle ("Ecart avec " + $cible) -Etat $etatH -Valeur ("{0:N2} seconde(s)" -f $derive) -Commentaire $noteH)',
      '} else {',
      "  [void](Add-KjemoResultat -Categorie 'Horloge' -Controle 'Ecart avec le controleur' -Etat 'ATTENTION' -Valeur 'non mesure' -Commentaire 'w32tm n''a pas pu joindre le controleur, ou sa sortie n''a pas ete reconnue.')",
      '}',
      '$source = $null',
      "try { $source = & w32tm.exe '/query' '/source' 2>&1 | Out-String } catch { }",
      'if ($source) {',
      "  [void](Add-KjemoResultat -Categorie 'Horloge' -Controle 'Source de temps' -Etat 'INFO' -Valeur $source.Trim() -Commentaire 'Une machine membre synchronise son heure sur le domaine, jamais sur une source externe.')",
      '}',
      "",
      '# --- 5. Le canal securise lui-meme -------------------------------------------',
      '$canalOk = $null',
      'if ($estMembre) {',
      '  try {',
      '    $canalOk = Test-ComputerSecureChannel -Server $Domaine -ErrorAction Stop',
      '  } catch {',
      '    try { $canalOk = Test-ComputerSecureChannel -ErrorAction SilentlyContinue } catch { }',
      '  }',
      '  if ($canalOk -eq $true) {',
      "    [void](Add-KjemoResultat -Categorie 'Canal securise' -Controle 'Relation d''approbation' -Etat 'OK' -Valeur 'intacte' -Commentaire 'Le mot de passe du compte machine est synchronise avec l''annuaire.')",
      '  } elseif ($canalOk -eq $false) {',
      "    [void](Add-KjemoResultat -Categorie 'Canal securise' -Controle 'Relation d''approbation' -Etat 'PROBLEME' -Valeur 'rompue' -Commentaire 'Le mot de passe du compte machine ne correspond plus a celui de l''annuaire. Causes usuelles : restauration d''instantane, machine rallumee apres une longue absence, objet ordinateur recree ou desactive.')",
      '  } else {',
      "    [void](Add-KjemoResultat -Categorie 'Canal securise' -Controle 'Relation d''approbation' -Etat 'ATTENTION' -Valeur 'non determinee' -Commentaire 'Le test n''a pas pu aboutir : aucun controleur joignable, le plus souvent.')",
      '  }',
      '} else {',
      "  [void](Add-KjemoResultat -Categorie 'Canal securise' -Controle 'Relation d''approbation' -Etat 'IGNORE' -Valeur 'controle sans objet sur cette machine')",
      '}',
      "",
      '# --- 6. Ce que la reparation fait, et ce qu\'elle ne fait pas ------------------',
      "[void](Add-KjemoResultat -Categorie 'Avertissement' -Controle 'Portee de la reparation' -Etat 'INFO' -Valeur 'renouvelle le mot de passe du compte machine' -Commentaire 'La reparation ne retire pas la machine du domaine et ne recree aucun objet : elle resynchronise le secret partage. Le SID de la machine est conserve.')",
      "[void](Add-KjemoResultat -Categorie 'Avertissement' -Controle 'Retrait et adhesion' -Etat 'INFO' -Valeur 'derniere option, jamais la premiere' -Commentaire 'Retirer la machine du domaine puis l''y remettre detruit et recree l''objet ordinateur : nouveau SID, appartenances aux groupes perdues, certificats machine invalides, et profils utilisateurs locaux devenus orphelins.')",
      "",
      '# --- 7. Reparation, uniquement en mode Appliquer, et jamais sur un controleur -',
      "if ($Mode -eq 'Appliquer' -and $estMembre -and $canalOk -eq $false) {",
      "  Write-Host ''",
      "  Write-Host '--- Identifiants demandes localement ---'",
      "  Write-Host 'Un compte autorise a joindre des machines au domaine est necessaire.'",
      "  Write-Host 'Ces identifiants ne proviennent pas du site, n''y retournent pas, et ne'",
      "  Write-Host 'figurent dans aucun rapport ni fichier.'",
      "  $identifiants = Get-Credential -Message 'Compte du domaine autorise a rejoindre des machines'",
      '  if ($identifiants) {',
      '    try {',
      '      $resultat = Test-ComputerSecureChannel -Repair -Credential $identifiants -Server $Domaine -ErrorAction Stop',
      "      $etatR = 'PROBLEME'",
      "      if ($resultat) { $etatR = 'OK' }",
      "      [void](Add-KjemoResultat -Categorie 'Action' -Controle 'Reparation du canal' -Etat $etatR -Valeur $resultat)",
      '    } catch {',
      "      [void](Add-KjemoResultat -Categorie 'Action' -Controle 'Reparation du canal' -Etat 'PROBLEME' -Valeur $_.Exception.Message -Commentaire 'Verifie que l''objet ordinateur existe et n''est pas desactive, et que le compte utilise a le droit de joindre des machines.')",
      '    }',
      '    $identifiants = $null',
      '    [System.GC]::Collect()',
      '  } else {',
      "    [void](Add-KjemoResultat -Categorie 'Action' -Controle 'Reparation du canal' -Etat 'IGNORE' -Valeur 'aucun identifiant fourni')",
      '  }',
      "",
      '  # --- 8. Relecture apres reparation ----------------------------------------',
      '  $apres = $null',
      '  try { $apres = Test-ComputerSecureChannel -Server $Domaine -ErrorAction SilentlyContinue } catch { }',
      "  $etatV = 'PROBLEME'",
      "  if ($apres -eq $true) { $etatV = 'OK' }",
      "  [void](Add-KjemoResultat -Categorie 'Verification' -Controle 'Canal apres reparation' -Etat $etatV -Valeur $apres -Commentaire 'Une ouverture de session de domaine reussie reste la seule verification qui conclut vraiment.')",
      "} elseif ($Mode -eq 'Appliquer' -and $canalOk -eq $true) {",
      "  [void](Add-KjemoResultat -Categorie 'Action' -Controle 'Reparation du canal' -Etat 'IGNORE' -Valeur 'canal intact : aucune reparation necessaire')",
      "} elseif ($Mode -eq 'Appliquer' -and $estDC) {",
      "  [void](Add-KjemoResultat -Categorie 'Action' -Controle 'Reparation du canal' -Etat 'IGNORE' -Valeur 'refusee sur un controleur de domaine' -Commentaire 'La commande de reparation ne s''applique pas a un controleur. Ce script ne la lancera pas, quel que soit le mode.')",
      '} else {',
      "  Write-Host ''",
      "  Write-Host '--- Simulation ---'",
      "  Write-Host 'En mode Appliquer, et seulement si le canal est rompu sur une machine'",
      "  Write-Host 'membre, le script demanderait des identifiants a la console puis'",
      "  Write-Host 'executerait : Test-ComputerSecureChannel -Repair -Credential ...'",
      '}',
      blocModeDiagnostic(),
    ];

    return assembler({
      titre: 'Verifier et reparer le canal securise du domaine',
      outil: 'ad-secure-channel',
      diagnostic: v.mode !== 'Appliquer',
      admin: true,
      parametres: [
        ['Domaine', psB64(domaine)],
        ['DeriveMaxSecondes', psB64(String(v.canDerive))],
        ['TestDesPorts', psB64(v.canPorts)],
        ['Mode', psB64(v.mode)],
      ],
      corps,
      prefixeFichier: 'kjemo-ad-canal',
      formats: 'Console',
    });
  },
  gui: [
    'Panneau de configuration > Système > Paramètres système avancés > Nom de l’ordinateur : l’appartenance au domaine s’y lit.',
    'Il n’existe pas de bouton « réparer le canal sécurisé » : l’interface ne propose que Retirer du domaine, puis Rejoindre — ce qui recrée l’objet et n’est pas la même chose.',
    'Utilisateurs et ordinateurs Active Directory : vérifier que l’objet ordinateur existe et n’est pas désactivé.',
    'Date et heure > Heure d’Internet : sur une machine de domaine, la synchronisation vient du domaine, et ce réglage doit rester inactif.',
  ],
  keywords: [
    'relation d’approbation', 'canal securise', 'test-computersecurechannel',
    'trust relationship failed', 'impossible d’ouvrir une session', 'compte machine',
    'kerberos', 'decalage d’horloge', 'w32tm', 'instantane restaure',
  ],
  requiresAdmin: true,
  os: OS_AD,
  prereqs: PREREQS_AD.concat([
    'Le script s’exécute sur la machine dont le canal est en cause : c’est un contrôle local.',
    'Droits d’administration locale, et un compte du domaine autorisé à joindre des machines pour la réparation.',
    'Un contrôleur de domaine joignable : sans lui, ni le test ni la réparation ne peuvent aboutir.',
  ]),
  commonErrors: ERREURS_AD.concat([
    {
      message: 'La relation d’approbation entre cette station de travail et le domaine principal a échoué',
      cause: 'Le mot de passe du compte machine ne correspond plus à celui enregistré dans l’annuaire.',
      fix: 'Ouvrir une session avec un compte local administrateur, puis réparer le canal. Retirer la machine du domaine n’est pas nécessaire.',
    },
    {
      message: 'Test-ComputerSecureChannel : Cannot find the computer account',
      cause: 'L’objet ordinateur a été supprimé de l’annuaire, ou déplacé hors du domaine visé.',
      fix: 'Vérifier l’existence de l’objet. S’il a été supprimé, la restauration depuis la corbeille Active Directory est préférable à une nouvelle adhésion.',
    },
    {
      message: 'The time difference between the client and server is too great',
      code: 'KRB_AP_ERR_SKEW',
      cause: 'L’écart d’horloge dépasse la tolérance de Kerberos, cinq minutes par défaut.',
      fix: 'Resynchroniser l’heure sur le domaine avant toute autre tentative.',
      command: 'w32tm /resync',
    },
    {
      message: 'La réparation réussit, puis le problème revient après quelques jours',
      cause: 'La machine est restaurée régulièrement depuis un instantané antérieur au dernier renouvellement du mot de passe machine.',
      fix: 'Traiter la cause : cesser de restaurer cet instantané, ou en produire un nouveau après réparation.',
    },
  ]),
  reversible: true,
  verifyAfter: [
    'Le script relit le canal après réparation et affiche le résultat du test.',
    'Une ouverture de session de domaine sur la machine conclut vraiment.',
    'Aucun identifiant ne figure dans le rapport, dans la console ni dans un fichier.',
  ],
  rollback: {
    summary: 'La réparation renouvelle le mot de passe du compte machine : il n’y a pas d’ancienne valeur à restaurer, et il n’y en a pas besoin. Si la machine était déjà saine, la réparation n’a rien cassé. Le seul vrai risque est ailleurs : retirer la machine du domaine, ce que ce script ne fait jamais.',
    diagnostic: '# Constater l\'etat du canal et celui de l\'objet ordinateur dans l\'annuaire.\nTest-ComputerSecureChannel -Verbose\nGet-ADComputer -Identity $env:COMPUTERNAME -Properties Enabled,PasswordLastSet,DistinguishedName | Format-List Name,Enabled,PasswordLastSet,DistinguishedName',
    command: '# Il n\'y a rien a defaire. Si le canal est de nouveau rompu, on recommence le\n# diagnostic : DNS, heure, objet ordinateur, puis canal.\nw32tm /resync\nTest-ComputerSecureChannel -Server \'<domaine>\'',
    exceptional: '# AVERTISSEMENT CRITIQUE — retirer la machine du domaine puis l\'y remettre.\n#\n# C\'est la manoeuvre que l\'on trouve partout sur les forums, et c\'est la plus\n# destructrice des options disponibles. Elle detruit l\'objet ordinateur et en\n# cree un autre :\n#\n#   - Nouveau SID de machine. Toute permission, toute delegation, toute\n#     appartenance a un groupe accordee a l\'ancien objet est perdue.\n#   - Les profils utilisateurs locaux, lies aux anciens SID, deviennent\n#     orphelins : les personnes retrouvent un bureau vide.\n#   - Les certificats machine et les inscriptions automatiques sont invalides.\n#   - BitLocker, si la cle de recuperation est sauvegardee dans l\'annuaire,\n#     perd son point de sauvegarde.\n#\n# Avant d\'y penser :\n#   1. Le canal a reellement ete repare sans succes, plusieurs fois.\n#   2. Le DNS, l\'heure et l\'objet ordinateur ont ete verifies.\n#   3. Les profils locaux et la cle BitLocker ont ete sauvegardes.\n#\n# Reference officielle :\n# https://learn.microsoft.com/powershell/module/microsoft.powershell.management/reset-computermachinepassword\n#\n# Alternative documentee, bien moins destructrice, a executer manuellement sur\n# la machine, avec un compte du domaine :\n#   Reset-ComputerMachinePassword -Server \'<controleur>\' -Credential (Get-Credential)',
    warning: 'Retirer une machine du domaine détruit son objet ordinateur et son SID. Ce script ne le fait jamais, et la réparation du canal rend cette manœuvre inutile dans l’immense majorité des cas.',
  },
  checks: [
    'Vérifier le DNS et l’heure avant le canal : ce sont eux, le plus souvent, la vraie cause.',
    'Sur un contrôleur de domaine, la réparation est refusée : elle n’aurait aucun sens.',
    'Les identifiants se saisissent à la console, jamais dans ce formulaire.',
  ],
  source: 'https://learn.microsoft.com/powershell/module/microsoft.powershell.management/test-computersecurechannel',
  sources: [
    { label: 'Test-ComputerSecureChannel', url: 'https://learn.microsoft.com/powershell/module/microsoft.powershell.management/test-computersecurechannel' },
    { label: 'Reset-ComputerMachinePassword', url: 'https://learn.microsoft.com/powershell/module/microsoft.powershell.management/reset-computermachinepassword' },
    { label: 'w32tm', url: 'https://learn.microsoft.com/windows-server/networking/windows-time-service/windows-time-service-tools-and-settings' },
    { label: 'Get-Credential', url: 'https://learn.microsoft.com/powershell/module/microsoft.powershell.security/get-credential' },
  ],
};

/**
 * 15. ad-domain-full-report — l'état complet du domaine, en une exécution.
 *
 * C'est le rapport que l'on produit avant une intervention, après un incident,
 * ou quand on reprend un domaine dont personne ne connaît plus l'histoire. Il
 * regarde tout ce dont Active Directory dépend réellement : les contrôleurs et
 * leurs rôles, la réplication, le DNS, SYSVOL et NETLOGON, le temps, Kerberos,
 * LDAP et RPC, puis les comptes et les objets qui traînent.
 *
 * La synthèse est placée en tête du rapport : elle est calculée en dernier,
 * puis remise en tête, pour qu'on sache en trois lignes s'il faut lire la
 * suite.
 */
export const outilRapportDomaine = {
  id: 'ad-domain-full-report',
  icon: '◉',
  category: 'Active Directory',
  subcategory: 'Santé du domaine',
  title: 'Rapport complet de santé du domaine',
  risk: 'diagnostic',
  summary: 'Forêt, domaine, niveaux fonctionnels, contrôleurs, rôles FSMO, sites, catalogue global, réplication, DNS, SYSVOL et NETLOGON, temps, Kerberos, LDAP et RPC, comptes sensibles et objets obsolètes — en un seul rapport, synthèse en tête.',
  fields: [
    champDomaine(),
    {
      id: 'cplJours', label: 'Fenêtre des événements examinés (jours)', default: '7',
      help: 'Journaux Directory Service, DFS Replication, DNS Server, Système et Sécurité.',
    },
    {
      id: 'cplInactif', label: 'Objet considéré obsolète après (jours)', default: '180',
      help: 'Appliqué aux comptes d’utilisateurs et aux objets ordinateurs.',
    },
    {
      id: 'cplPorts', label: 'Tester les ports de chaque contrôleur', type: 'select', default: 'Oui',
      options: [['Oui', 'Oui — RPC, LDAP, LDAPS, Kerberos, SMB, DNS, catalogue global'], ['Non', 'Non']],
      help: 'Chaque contrôleur est interrogé sur sept ports : compter quelques secondes par contrôleur.',
    },
    champFormat('cplFormat'),
  ],
  validate(v) {
    const errors = {};
    verifier(errors, 'adDomain', validerFqdn(v.adDomain));
    verifier(errors, 'cplJours', validerJours(v.cplJours, { min: 1, max: 90, label: 'La fenêtre d’examen' }));
    verifier(errors, 'cplInactif', validerJours(v.cplInactif, { min: 30, max: 3650, label: 'Le seuil d’obsolescence' }));
    verifier(errors, 'cplPorts', validerChoix(v.cplPorts, ['Oui', 'Non'], 'Le choix'));
    verifier(errors, 'cplFormat', validerFormatRapport(v.cplFormat));
    return errors;
  },
  generate(v) {
    assertValid(this, v);
    const domaine = validerFqdn(v.adDomain).value;

    const corps = [
      `$Domaine = ${psB64(domaine)}`,
      `$Jours = ${validerJours(v.cplJours, { min: 1, max: 90 }).value}`,
      `$SeuilObsolete = ${validerJours(v.cplInactif, { min: 30, max: 3650 }).value}`,
      `$TesterPorts = ${psB64(v.cplPorts)}`,
      `$KjemoFormat = ${psB64(v.cplFormat)}`,
      "",
      blocModuleAd(),
      "",
      '# Compteurs de synthese, remplis au fil des controles.',
      '$pbCritiques = 0',
      '$pbAvertissements = 0',
      "",
      '# --- 1. Foret, domaine et niveaux fonctionnels -------------------------------',
      "# Le niveau fonctionnel decide des fonctionnalites disponibles : la corbeille",
      '# Active Directory, par exemple, demande le niveau 2008 R2 au minimum.',
      '$foret = $null',
      'if ($moduleOk) { try { $foret = Get-ADForest -ErrorAction SilentlyContinue } catch { } }',
      'if ($foret) {',
      '  [void](Add-KjemoResultat -Categorie \'Foret\' -Controle \'Foret\' -Etat \'INFO\' -Valeur $foret.Name -Commentaire ("niveau fonctionnel : $($foret.ForestMode) ; domaines : " + ($foret.Domains -join \', \')))',
      '  [void](Add-KjemoResultat -Categorie \'Foret\' -Controle \'Catalogues globaux\' -Etat \'INFO\' -Valeur (($foret.GlobalCatalogs) -join \', \'))',
      '}',
      'if ($domaineObj) {',
      '  [void](Add-KjemoResultat -Categorie \'Domaine\' -Controle \'Niveau fonctionnel\' -Etat \'INFO\' -Valeur $domaineObj.DomainMode)',
      '  [void](Add-KjemoResultat -Categorie \'Domaine\' -Controle \'Nom NetBIOS\' -Etat \'INFO\' -Valeur $domaineObj.NetBIOSName)',
      '}',
      '$corbeille = $null',
      'if ($moduleOk -and $foret) {',
      '  try {',
      "    $corbeille = Get-ADOptionalFeature -Filter \"Name -eq 'Recycle Bin Feature'\" -ErrorAction SilentlyContinue",
      '  } catch { }',
      '}',
      'if ($corbeille -and $corbeille.EnabledScopes.Count -gt 0) {',
      "  [void](Add-KjemoResultat -Categorie 'Domaine' -Controle 'Corbeille Active Directory' -Etat 'OK' -Valeur 'activee' -Commentaire 'Un objet supprime par erreur peut etre restaure avec ses appartenances et son SID.')",
      '} else {',
      '  $pbAvertissements = $pbAvertissements + 1',
      "  [void](Add-KjemoResultat -Categorie 'Domaine' -Controle 'Corbeille Active Directory' -Etat 'ATTENTION' -Valeur 'non activee' -Commentaire 'Sans elle, un objet supprime ne se recupere que par une restauration de sauvegarde. L''activation est irreversible : elle se decide, elle ne s''improvise pas.')",
      '}',
      "",
      '# --- 2. Controleurs et roles FSMO --------------------------------------------',
      "# Les cinq roles a maitre unique ne se repliquent pas : si le controleur qui",
      "# detient l'emulateur PDC est absent, les verrouillages de compte et la",
      '# synchronisation du temps cessent de fonctionner correctement.',
      '$controleurs = @()',
      'if ($moduleOk) { try { $controleurs = @(Get-ADDomainController -Filter * -Server $Domaine -ErrorAction SilentlyContinue) } catch { } }',
      "$etat = 'OK'",
      'if ($controleurs.Count -eq 0) { $etat = \'PROBLEME\' ; $pbCritiques = $pbCritiques + 1 }',
      'if ($controleurs.Count -eq 1) { $etat = \'ATTENTION\' ; $pbAvertissements = $pbAvertissements + 1 }',
      "[void](Add-KjemoResultat -Categorie 'Controleurs' -Controle 'Nombre' -Etat $etat -Valeur $controleurs.Count -Commentaire 'Un domaine a un seul controleur n''a aucune tolerance de panne.')",
      'foreach ($dc in $controleurs) {',
      '  [void](Add-KjemoResultat -Categorie \'Controleurs\' -Controle $dc.HostName -Etat \'INFO\' -Valeur ("site : $($dc.Site) ; catalogue global : $($dc.IsGlobalCatalog) ; lecture seule : $($dc.IsReadOnly)") -Commentaire $dc.OperatingSystem)',
      '}',
      'if ($domaineObj) {',
      '  [void](Add-KjemoResultat -Categorie \'Roles FSMO\' -Controle \'Emulateur PDC\' -Etat \'INFO\' -Valeur $domaineObj.PDCEmulator -Commentaire \'Reference de temps du domaine, et destinataire des verrouillages de compte.\')',
      "  [void](Add-KjemoResultat -Categorie 'Roles FSMO' -Controle 'Maitre RID' -Etat 'INFO' -Valeur $domaineObj.RIDMaster -Commentaire 'Distribue les plages d''identifiants : son absence finit par empecher toute creation d''objet.')",
      "  [void](Add-KjemoResultat -Categorie 'Roles FSMO' -Controle 'Maitre d''infrastructure' -Etat 'INFO' -Valeur $domaineObj.InfrastructureMaster)",
      '}',
      'if ($foret) {',
      '  [void](Add-KjemoResultat -Categorie \'Roles FSMO\' -Controle \'Maitre de schema\' -Etat \'INFO\' -Valeur $foret.SchemaMaster)',
      "  [void](Add-KjemoResultat -Categorie 'Roles FSMO' -Controle 'Maitre d''attribution de noms' -Etat 'INFO' -Valeur $foret.DomainNamingMaster)",
      '}',
      "",
      '# --- 3. Sites et sous-reseaux -------------------------------------------------',
      "# Un sous-reseau non declare envoie les clients vers n'importe quel controleur,",
      '# y compris a l\'autre bout d\'une liaison lente.',
      'if ($moduleOk) {',
      '  $sites = @()',
      '  $sousReseaux = @()',
      '  try { $sites = @(Get-ADReplicationSite -Filter * -ErrorAction SilentlyContinue) } catch { }',
      '  try { $sousReseaux = @(Get-ADReplicationSubnet -Filter * -Properties Site -ErrorAction SilentlyContinue) } catch { }',
      '  [void](Add-KjemoResultat -Categorie \'Sites\' -Controle \'Sites definis\' -Etat \'INFO\' -Valeur (($sites | ForEach-Object { $_.Name }) -join \', \'))',
      "  $etat = 'OK'",
      '  if ($sousReseaux.Count -eq 0 -and $sites.Count -gt 1) { $etat = \'ATTENTION\' ; $pbAvertissements = $pbAvertissements + 1 }',
      "  [void](Add-KjemoResultat -Categorie 'Sites' -Controle 'Sous-reseaux declares' -Etat $etat -Valeur $sousReseaux.Count -Commentaire 'Un client dont le sous-reseau n''est declare nulle part est oriente au hasard.')",
      '  $orphelins = @($sousReseaux | Where-Object { -not $_.Site })',
      '  if ($orphelins.Count -gt 0) {',
      '    $pbAvertissements = $pbAvertissements + 1',
      '    [void](Add-KjemoResultat -Categorie \'Sites\' -Controle \'Sous-reseaux sans site\' -Etat \'ATTENTION\' -Valeur $orphelins.Count -Commentaire (($orphelins | ForEach-Object { $_.Name }) -join \', \'))',
      '  }',
      '}',
      "",
      '# --- 4. Replication -----------------------------------------------------------',
      '$partenaires = @()',
      'if ($moduleOk) { try { $partenaires = @(Get-ADReplicationPartnerMetadata -Target $Domaine -Scope Domain -ErrorAction SilentlyContinue) } catch { } }',
      '$liensEchoues = @($partenaires | Where-Object { $_.ConsecutiveReplicationFailures -gt 0 })',
      "$etat = 'OK'",
      'if ($liensEchoues.Count -gt 0) { $etat = \'PROBLEME\' ; $pbCritiques = $pbCritiques + 1 }',
      '[void](Add-KjemoResultat -Categorie \'Replication\' -Controle \'Liens examines\' -Etat $etat -Valeur ("$($partenaires.Count) lien(s), $($liensEchoues.Count) en echec"))',
      'foreach ($p in $liensEchoues) {',
      "  [void](Add-KjemoResultat -Categorie 'Replication' -Controle (\"$($p.Server) <- $($p.PartnerAddress)\") -Etat 'PROBLEME' -Valeur (\"code $($p.LastReplicationResult) ; echecs consecutifs : $($p.ConsecutiveReplicationFailures)\") -Commentaire 'Le rapport de replication dedie traduit les codes d''erreur.')",
      '}',
      "",
      '# --- 5. DNS : ce dont Active Directory depend ---------------------------------',
      "$srv = @(\"_ldap._tcp.dc._msdcs.$Domaine\", \"_kerberos._tcp.dc._msdcs.$Domaine\", \"_gc._tcp.$Domaine\")",
      '$srvManquants = 0',
      'foreach ($nom in $srv) {',
      '  $r = $null',
      '  try { $r = @(Resolve-DnsName -Name $nom -Type SRV -ErrorAction SilentlyContinue | Where-Object { $_.QueryType -eq \'SRV\' }) } catch { }',
      '  if ($r -and $r.Count -gt 0) {',
      '    [void](Add-KjemoResultat -Categorie \'DNS\' -Controle $nom -Etat \'OK\' -Valeur (($r | ForEach-Object { $_.NameTarget }) -join \', \'))',
      '  } else {',
      '    $srvManquants = $srvManquants + 1',
      "    [void](Add-KjemoResultat -Categorie 'DNS' -Controle $nom -Etat 'PROBLEME' -Valeur 'absent')",
      '  }',
      '}',
      'if ($srvManquants -gt 0) { $pbCritiques = $pbCritiques + 1 }',
      'foreach ($dc in $controleurs) {',
      '  $a = $null',
      '  try { $a = @(Resolve-DnsName -Name $dc.HostName -Type A -ErrorAction SilentlyContinue | Where-Object { $_.QueryType -eq \'A\' }) } catch { }',
      '  $ptr = $null',
      '  if ($dc.IPv4Address) { try { $ptr = @(Resolve-DnsName -Name $dc.IPv4Address -Type PTR -ErrorAction SilentlyContinue) } catch { } }',
      "  $etatD = 'OK'",
      "  $noteD = ''",
      '  if (-not $a -or $a.Count -eq 0) { $etatD = \'PROBLEME\' ; $noteD = \'Resolution directe impossible.\' ; $pbCritiques = $pbCritiques + 1 }',
      "  elseif (-not $ptr -or $ptr.Count -eq 0) { $etatD = 'ATTENTION' ; $noteD = 'Aucun enregistrement PTR : la zone inverse est absente ou incomplete.' }",
      '  [void](Add-KjemoResultat -Categorie \'DNS\' -Controle ("Resolution — " + $dc.HostName) -Etat $etatD -Valeur ("directe : $(if ($a) { ($a | ForEach-Object { $_.IPAddress }) -join \', \' } else { \'echec\' })") -Commentaire $noteD)',
      '}',
      "",
      '# --- 6. SYSVOL et NETLOGON ----------------------------------------------------',
      "# SYSVOL porte les strategies de groupe et les scripts d'ouverture de session.",
      "# S'il n'est pas partage, aucune GPO ne s'applique — et rien ne le dit.",
      '$partages = @()',
      'try { $partages = @(Get-SmbShare -ErrorAction SilentlyContinue) } catch { }',
      "$estDC = $false",
      'try {',
      '  $r2 = (Get-CimInstance Win32_ComputerSystem -ErrorAction Stop).DomainRole',
      '  if ($r2 -eq 4 -or $r2 -eq 5) { $estDC = $true }',
      '} catch { }',
      'if ($estDC) {',
      "  foreach ($nomPartage in @('SYSVOL', 'NETLOGON')) {",
      '    $p = $partages | Where-Object { $_.Name -eq $nomPartage }',
      '    if ($p) {',
      '      [void](Add-KjemoResultat -Categorie \'SYSVOL\' -Controle ("Partage " + $nomPartage) -Etat \'OK\' -Valeur $p.Path)',
      '    } else {',
      '      $pbCritiques = $pbCritiques + 1',
      "      [void](Add-KjemoResultat -Categorie 'SYSVOL' -Controle (\"Partage \" + $nomPartage) -Etat 'PROBLEME' -Valeur 'absent' -Commentaire 'Sans ce partage, le controleur ne s''annonce pas et aucune strategie de groupe ne s''applique.')",
      '    }',
      '  }',
      "  foreach ($nomService in @('Netlogon', 'DFSR', 'NTDS', 'KDC', 'W32Time')) {",
      '    $s = $null',
      '    try { $s = Get-Service -Name $nomService -ErrorAction SilentlyContinue } catch { }',
      '    if ($s) {',
      "      $etatS = 'PROBLEME'",
      "      if ($s.Status -eq 'Running') { $etatS = 'OK' } else { $pbCritiques = $pbCritiques + 1 }",
      '      [void](Add-KjemoResultat -Categorie \'SYSVOL\' -Controle ("Service " + $s.DisplayName) -Etat $etatS -Valeur $s.Status)',
      '    } else {',
      '      [void](Add-KjemoResultat -Categorie \'SYSVOL\' -Controle ("Service " + $nomService) -Etat \'INFO\' -Valeur \'absent de cette machine\')',
      '    }',
      '  }',
      '  $etatsDfsr = @()',
      '  try {',
      "    $etatsDfsr = @(Get-CimInstance -Namespace 'root/MicrosoftDfs' -ClassName 'DfsrReplicatedFolderInfo' -ErrorAction SilentlyContinue | Where-Object { $_.ReplicationGroupName -like '*Domain System Volume*' })",
      '  } catch { }',
      '  foreach ($e in $etatsDfsr) {',
      "    $libelleEtat = 'inconnu'",
      "    if ($e.State -eq 0) { $libelleEtat = 'non initialise' }",
      "    elseif ($e.State -eq 1) { $libelleEtat = 'initialisation' }",
      "    elseif ($e.State -eq 2) { $libelleEtat = 'synchronisation initiale' }",
      "    elseif ($e.State -eq 3) { $libelleEtat = 'auto-recuperation' }",
      "    elseif ($e.State -eq 4) { $libelleEtat = 'normal' }",
      "    elseif ($e.State -eq 5) { $libelleEtat = 'en erreur' }",
      "    $etatR = 'ATTENTION'",
      "    if ($e.State -eq 4) { $etatR = 'OK' }",
      '    if ($e.State -eq 5) { $etatR = \'PROBLEME\' ; $pbCritiques = $pbCritiques + 1 }',
      '    [void](Add-KjemoResultat -Categorie \'SYSVOL\' -Controle \'Replication DFSR de SYSVOL\' -Etat $etatR -Valeur $libelleEtat -Commentaire ("dossier replique : $($e.ReplicatedFolderName)"))',
      '  }',
      '  if ($etatsDfsr.Count -eq 0) {',
      "    [void](Add-KjemoResultat -Categorie 'SYSVOL' -Controle 'Replication DFSR de SYSVOL' -Etat 'INFO' -Valeur 'etat non lisible' -Commentaire 'Espace WMI DFSR indisponible, ou SYSVOL replique encore par l''ancien service FRS.')",
      '  }',
      '  $depuis = (Get-Date).AddDays(-$Jours)',
      '  $evtDfsr = @()',
      "  try { $evtDfsr = @(Get-WinEvent -FilterHashtable @{ LogName = 'DFS Replication'; Level = 1,2,3; StartTime = $depuis } -ErrorAction SilentlyContinue) } catch { }",
      "  $etatE = 'OK'",
      "  if ($evtDfsr.Count -gt 0) { $etatE = 'ATTENTION' ; $pbAvertissements = $pbAvertissements + 1 }",
      '  [void](Add-KjemoResultat -Categorie \'SYSVOL\' -Controle \'Evenements DFS Replication\' -Etat $etatE -Valeur ("$($evtDfsr.Count) sur $Jours jour(s)"))',
      '  foreach ($e in ($evtDfsr | Select-Object -First 8)) {',
      '    [void](Add-KjemoResultat -Categorie \'SYSVOL\' -Controle ("DFSR / evenement $($e.Id)") -Etat \'ATTENTION\' -Valeur $e.TimeCreated.ToString(\'yyyy-MM-dd HH:mm\') -Commentaire (($e.Message -split "`n")[0]))',
      '  }',
      '} else {',
      '  $cheminSysvol = "\\\\$Domaine\\SYSVOL"',
      '  $accessible = $false',
      '  try { $accessible = Test-Path -LiteralPath $cheminSysvol -ErrorAction SilentlyContinue } catch { }',
      "  $etatS = 'PROBLEME'",
      '  if ($accessible) { $etatS = \'OK\' } else { $pbCritiques = $pbCritiques + 1 }',
      "  [void](Add-KjemoResultat -Categorie 'SYSVOL' -Controle 'Acces a SYSVOL depuis cette machine' -Etat $etatS -Valeur $cheminSysvol -Commentaire 'Depuis une machine membre, c''est ce que voit un client au moment d''appliquer les strategies de groupe.')",
      '  $cheminNetlogon = "\\\\$Domaine\\NETLOGON"',
      '  $accessible2 = $false',
      '  try { $accessible2 = Test-Path -LiteralPath $cheminNetlogon -ErrorAction SilentlyContinue } catch { }',
      "  $etatN = 'PROBLEME'",
      '  if ($accessible2) { $etatN = \'OK\' } else { $pbCritiques = $pbCritiques + 1 }',
      "  [void](Add-KjemoResultat -Categorie 'SYSVOL' -Controle 'Acces a NETLOGON depuis cette machine' -Etat $etatN -Valeur $cheminNetlogon)",
      '}',
      "",
      '# --- 7. Temps et Kerberos ------------------------------------------------------',
      "# Kerberos refuse tout ticket dont l'horodatage s'ecarte de plus de cinq",
      "# minutes. Une derive d'horloge ressemble alors a un probleme de mot de passe.",
      '$sourceTemps = $null',
      "try { $sourceTemps = (& w32tm.exe '/query' '/source' 2>&1 | Out-String).Trim() } catch { }",
      'if ($sourceTemps) {',
      "  [void](Add-KjemoResultat -Categorie 'Temps' -Controle 'Source de temps de cette machine' -Etat 'INFO' -Valeur $sourceTemps)",
      '}',
      'if ($domaineObj -and $domaineObj.PDCEmulator) {',
      '  $derive = $null',
      '  try {',
      "    $sortie = & w32tm.exe '/stripchart' \"/computer:$($domaineObj.PDCEmulator)\" '/samples:1' '/dataonly' 2>&1 | Out-String",
      "    $m = [regex]::Match($sortie, '([-+]?[0-9]+[\\.,][0-9]+)s')",
      "    if ($m.Success) { $derive = [Math]::Abs([double]($m.Groups[1].Value -replace ',', '.')) }",
      '  } catch { }',
      '  if ($derive -ne $null) {',
      "    $etatT = 'OK'",
      '    if ($derive -gt 300) { $etatT = \'PROBLEME\' ; $pbCritiques = $pbCritiques + 1 }',
      "    elseif ($derive -gt 60) { $etatT = 'ATTENTION' ; $pbAvertissements = $pbAvertissements + 1 }",
      "    [void](Add-KjemoResultat -Categorie 'Temps' -Controle 'Ecart avec l''emulateur PDC' -Etat $etatT -Valeur (\"{0:N2} seconde(s)\" -f $derive) -Commentaire 'Au-dela de 300 secondes, Kerberos refuse les tickets.')",
      '  } else {',
      "    [void](Add-KjemoResultat -Categorie 'Temps' -Controle 'Ecart avec l''emulateur PDC' -Etat 'ATTENTION' -Valeur 'non mesure')",
      '  }',
      '}',
      '$tickets = $null',
      "try { $tickets = & klist.exe 2>&1 | Out-String } catch { }",
      'if ($tickets) {',
      "  $nbTickets = @([regex]::Matches($tickets, '(?m)^#\\d+>')).Count",
      "  $etatK = 'OK'",
      "  if ($nbTickets -eq 0) { $etatK = 'ATTENTION' }",
      "  [void](Add-KjemoResultat -Categorie 'Kerberos' -Controle 'Tickets de la session courante' -Etat $etatK -Valeur \"$nbTickets ticket(s)\" -Commentaire 'Une session sans ticket Kerberos s''est authentifiee autrement, ou pas du tout aupres du domaine.')",
      '}',
      'if ($estDC) {',
      '  $depuisK = (Get-Date).AddDays(-$Jours)',
      '  $evtKrb = @()',
      "  try { $evtKrb = @(Get-WinEvent -FilterHashtable @{ LogName = 'Security'; Id = 4768,4771,4776; StartTime = $depuisK } -ErrorAction SilentlyContinue | Where-Object { $_.Id -ne 4768 }) } catch { }",
      "  $etatK2 = 'OK'",
      "  if ($evtKrb.Count -gt 50) { $etatK2 = 'ATTENTION' ; $pbAvertissements = $pbAvertissements + 1 }",
      '  [void](Add-KjemoResultat -Categorie \'Kerberos\' -Controle \'Echecs de pre-authentification\' -Etat $etatK2 -Valeur ("$($evtKrb.Count) evenement(s) 4771/4776 sur $Jours jour(s)") -Commentaire \'Un volume anormal designe soit un mot de passe perime quelque part, soit une tentative repetee.\')',
      '}',
      "",
      '# --- 8. LDAP, RPC et les ports du domaine ---------------------------------------',
      "if ($TesterPorts -eq 'Oui') {",
      "  $ports = @('135:RPC', '389:LDAP', '636:LDAPS', '88:Kerberos', '445:SMB', '53:DNS', '3268:Catalogue global')",
      '  foreach ($dc in $controleurs) {',
      '    $fermes = New-Object System.Collections.ArrayList',
      '    foreach ($entree in $ports) {',
      "      $numero = [int]($entree.Split(':')[0])",
      "      $libelle = $entree.Split(':')[1]",
      '      $ouvert = $false',
      '      try { $ouvert = (Test-NetConnection -ComputerName $dc.HostName -Port $numero -WarningAction SilentlyContinue -InformationLevel Quiet) } catch { }',
      '      if (-not $ouvert) { [void]$fermes.Add("$numero ($libelle)") }',
      '    }',
      '    if ($fermes.Count -eq 0) {',
      '      [void](Add-KjemoResultat -Categorie \'Ports\' -Controle $dc.HostName -Etat \'OK\' -Valeur \'les sept ports repondent\')',
      '    } else {',
      "      $etatP = 'ATTENTION'",
      '      if ($fermes.Count -gt 1) { $etatP = \'PROBLEME\' ; $pbCritiques = $pbCritiques + 1 }',
      '      [void](Add-KjemoResultat -Categorie \'Ports\' -Controle $dc.HostName -Etat $etatP -Valeur ("sans reponse : " + ($fermes -join \', \')) -Commentaire \'LDAPS seul sans reponse est frequent : il demande un certificat. Les autres ports sont indispensables.\')',
      '    }',
      '  }',
      '} else {',
      "  [void](Add-KjemoResultat -Categorie 'Ports' -Controle 'Test des ports' -Etat 'IGNORE' -Valeur 'non demande')",
      '}',
      '$ldapOk = $false',
      'if ($moduleOk) {',
      '  try {',
      '    $rootDse = Get-ADRootDSE -Server $Domaine -ErrorAction Stop',
      '    if ($rootDse) { $ldapOk = $true }',
      "    [void](Add-KjemoResultat -Categorie 'LDAP' -Controle 'Interrogation de l''annuaire' -Etat 'OK' -Valeur (\"contexte par defaut : $($rootDse.defaultNamingContext)\"))",
      '  } catch {',
      '    $pbCritiques = $pbCritiques + 1',
      "    [void](Add-KjemoResultat -Categorie 'LDAP' -Controle 'Interrogation de l''annuaire' -Etat 'PROBLEME' -Valeur $_.Exception.Message)",
      '  }',
      '}',
      '$depuisR = (Get-Date).AddDays(-$Jours)',
      '$evtRpc = @()',
      "try { $evtRpc = @(Get-WinEvent -FilterHashtable @{ LogName = 'System'; Level = 1,2; StartTime = $depuisR } -ErrorAction SilentlyContinue | Where-Object { $_.Message -match '1722|RPC' }) } catch { }",
      "$etatRpc = 'OK'",
      "if ($evtRpc.Count -gt 0) { $etatRpc = 'ATTENTION' ; $pbAvertissements = $pbAvertissements + 1 }",
      "[void](Add-KjemoResultat -Categorie 'RPC' -Controle 'Erreurs RPC recentes' -Etat $etatRpc -Valeur (\"$($evtRpc.Count) sur $Jours jour(s)\") -Commentaire 'L''erreur 1722 designe un controleur injoignable sur ses ports RPC.')",
      "",
      '# --- 9. Comptes et groupes sensibles ---------------------------------------------',
      'if ($moduleOk -and $domaineObj) {',
      "  $ridsSensibles = @('512:Admins du domaine', '519:Administrateurs de l''entreprise', '518:Administrateurs du schema')",
      '  foreach ($entree in $ridsSensibles) {',
      "    $rid = $entree.Split(':')[0]",
      "    $libelle = $entree.Split(':')[1]",
      '    $grp = $null',
      '    try { $grp = Get-ADGroup -Identity "$($domaineObj.DomainSID.Value)-$rid" -ErrorAction SilentlyContinue } catch { }',
      '    if (-not $grp) { continue }',
      '    $membres = @()',
      '    try { $membres = @(Get-ADGroupMember -Identity $grp -Recursive -ErrorAction SilentlyContinue) } catch { }',
      "    $etatG = 'OK'",
      "    if ($membres.Count -gt 5) { $etatG = 'ATTENTION' ; $pbAvertissements = $pbAvertissements + 1 }",
      '    [void](Add-KjemoResultat -Categorie \'Comptes sensibles\' -Controle $libelle -Etat $etatG -Valeur ("$($membres.Count) membre(s)") -Commentaire (($membres | ForEach-Object { $_.SamAccountName }) -join \', \'))',
      '  }',
      '  $limite = (Get-Date).AddDays(-$SeuilObsolete)',
      '  $usagersInactifs = @()',
      '  try { $usagersInactifs = @(Get-ADUser -Filter * -Properties Enabled,LastLogonDate -ErrorAction SilentlyContinue | Where-Object { $_.Enabled -eq $true -and $_.LastLogonDate -ne $null -and $_.LastLogonDate -lt $limite }) } catch { }',
      "  $etatU = 'OK'",
      "  if ($usagersInactifs.Count -gt 0) { $etatU = 'ATTENTION' ; $pbAvertissements = $pbAvertissements + 1 }",
      '  [void](Add-KjemoResultat -Categorie \'Objets obsoletes\' -Controle \'Comptes actifs sans connexion recente\' -Etat $etatU -Valeur ("$($usagersInactifs.Count) au-dela de $SeuilObsolete jour(s)") -Commentaire \'Le rapport de sante des comptes en donne le detail.\')',
      '  $ordisInactifs = @()',
      '  try { $ordisInactifs = @(Get-ADComputer -Filter * -Properties Enabled,PasswordLastSet -ErrorAction SilentlyContinue | Where-Object { $_.Enabled -eq $true -and $_.PasswordLastSet -ne $null -and $_.PasswordLastSet -lt $limite }) } catch { }',
      "  $etatO = 'OK'",
      "  if ($ordisInactifs.Count -gt 0) { $etatO = 'ATTENTION' ; $pbAvertissements = $pbAvertissements + 1 }",
      "  [void](Add-KjemoResultat -Categorie 'Objets obsoletes' -Controle 'Ordinateurs sans contact recent' -Etat $etatO -Valeur (\"$($ordisInactifs.Count) au-dela de $SeuilObsolete jour(s)\") -Commentaire 'L''outil de mise en quarantaine des ordinateurs en donne le detail.')",
      '}',
      "",
      '# --- 10. Evenements recents de l\'annuaire -----------------------------------------',
      'if ($estDC) {',
      '  $depuisD = (Get-Date).AddDays(-$Jours)',
      "  foreach ($journal in @('Directory Service', 'DNS Server', 'System')) {",
      '    $evts = @()',
      '    try { $evts = @(Get-WinEvent -FilterHashtable @{ LogName = $journal; Level = 1,2; StartTime = $depuisD } -ErrorAction SilentlyContinue) } catch { }',
      "    $etatJ = 'OK'",
      "    if ($evts.Count -gt 0) { $etatJ = 'ATTENTION' }",
      '    [void](Add-KjemoResultat -Categorie \'Journaux\' -Controle ("Erreurs — " + $journal) -Etat $etatJ -Valeur ("$($evts.Count) sur $Jours jour(s)"))',
      '    foreach ($e in ($evts | Select-Object -First 5)) {',
      '      [void](Add-KjemoResultat -Categorie \'Journaux\' -Controle ("$journal / evenement $($e.Id)") -Etat \'ATTENTION\' -Valeur $e.TimeCreated.ToString(\'yyyy-MM-dd HH:mm\') -Commentaire (($e.Message -split "`n")[0]))',
      '    }',
      '  }',
      '}',
      "",
      '# --- 11. Synthese, calculee en dernier puis remise en tete ------------------------',
      "# Les constats sont accumules dans l'ordre des controles. On les met de cote,",
      "# on inscrit la synthese, puis on les remet derriere elle : le rapport se lit",
      '# donc par le resume, et non par la fin.',
      '$detailsAccumules = @($KjemoResultats)',
      '$KjemoResultats.Clear()',
      "",
      "$verdict = 'OK'",
      "$phrase = 'Aucun probleme releve par les controles executes.'",
      'if ($pbAvertissements -gt 0) {',
      "  $verdict = 'ATTENTION'",
      '  $phrase = "$pbAvertissements point(s) a examiner."',
      '}',
      'if ($pbCritiques -gt 0) {',
      "  $verdict = 'PROBLEME'",
      '  $phrase = "$pbCritiques probleme(s) a traiter en priorite, et $pbAvertissements point(s) a examiner."',
      '}',
      "[void](Add-KjemoResultat -Categorie 'Synthese' -Controle 'Verdict' -Etat $verdict -Valeur $phrase -Commentaire 'Ce verdict porte sur les controles reellement executes sur cette machine, et sur rien d''autre.')",
      '[void](Add-KjemoResultat -Categorie \'Synthese\' -Controle \'Domaine\' -Etat \'INFO\' -Valeur $Domaine -Commentaire ("controleurs : $($controleurs.Count) ; liens de replication en echec : $($liensEchoues.Count)"))',
      '[void](Add-KjemoResultat -Categorie \'Synthese\' -Controle \'Perimetre du rapport\' -Etat \'INFO\' -Valeur ("execute depuis $env:COMPUTERNAME") -Commentaire \'Les controles propres a un controleur de domaine ne sont executes que sur un controleur : ailleurs ils sont marques IGNORE, jamais en echec.\')',
      "",
      'foreach ($d in $detailsAccumules) { [void]$KjemoResultats.Add($d) }',
    ];

    return assembler({
      titre: 'Rapport complet de sante du domaine',
      outil: 'ad-domain-full-report',
      diagnostic: true,
      admin: true,
      parametres: [
        ['Domaine', psB64(domaine)],
        ['FenetreJours', psB64(String(v.cplJours))],
        ['SeuilObsoleteJours', psB64(String(v.cplInactif))],
        ['TestDesPorts', psB64(v.cplPorts)],
        ['Format', psB64(v.cplFormat)],
      ],
      corps,
      prefixeFichier: 'kjemo-ad-rapport-domaine',
    });
  },
  gui: [
    'Gestionnaire de serveur > AD DS : les alertes de rôle reprennent une partie des constats.',
    'Sites et services Active Directory (dssite.msc) : sites, sous-réseaux et connexions de réplication.',
    'Gestionnaire DNS (dnsmgmt.msc) : zones, enregistrements SRV et redirecteurs.',
    'Observateur d’événements : Directory Service, DFS Replication, DNS Server, Système, Sécurité.',
    'Aucune de ces consoles ne produit un rapport unique et exportable : c’est ce que fait ce script.',
  ],
  keywords: [
    'rapport de domaine', 'sante active directory', 'fsmo', 'niveau fonctionnel',
    'sysvol', 'netlogon', 'dfsr', 'kerberos', 'ldap', 'rpc', 'catalogue global',
    'sites et services', 'corbeille active directory', 'audit de domaine',
  ],
  requiresAdmin: true,
  os: OS_AD,
  prereqs: PREREQS_AD.concat([
    'Compte disposant d’un droit de lecture étendu sur le domaine : administrateur du domaine, ou délégation équivalente.',
    'Les contrôles de SYSVOL, DFSR et des journaux d’annuaire ne s’exécutent que sur un contrôleur de domaine : ailleurs ils sont marqués « non applicable ».',
    'Sur un domaine étendu, le test des ports interroge chaque contrôleur : prévoir quelques secondes par contrôleur.',
  ]),
  commonErrors: ERREURS_AD.concat([
    {
      message: 'Get-ADReplicationPartnerMetadata : The server is not operational',
      cause: 'Le contrôleur visé ne répond pas sur LDAP, ou la résolution du domaine échoue.',
      fix: 'Traiter d’abord le DNS et les ports : le reste du rapport en dépend.',
    },
    {
      message: 'SYSVOL est présent mais aucune stratégie de groupe ne s’applique',
      cause: 'Le partage existe, mais la réplication DFSR est en erreur : le contenu diffère d’un contrôleur à l’autre.',
      fix: 'Lire l’état DFSR et les événements DFS Replication du rapport. La reconstruction de SYSVOL est une procédure exceptionnelle, jamais automatique.',
    },
    {
      message: 'La corbeille Active Directory apparaît comme non activée',
      cause: 'Elle n’a jamais été activée sur cette forêt.',
      fix: 'Son activation est irréversible et se décide au niveau de la forêt : c’est un choix d’administration, pas une correction de script.',
    },
    {
      message: 'Les contrôles SYSVOL, DFSR et Kerberos sont tous marqués « non applicable »',
      cause: 'Le script s’exécute depuis une machine membre, et non depuis un contrôleur.',
      fix: 'C’est le comportement voulu : le rapport ne déclare pas en échec ce qu’il n’a pas pu tester. Relancer depuis un contrôleur pour obtenir ces sections.',
    },
  ]),
  reversible: true,
  verifyAfter: [
    'La synthèse figure en tête du rapport, avec un verdict et le nombre de points relevés.',
    'Chaque contrôle non exécuté porte l’état IGNORE et la raison : aucun contrôle absent n’est compté comme réussi.',
    'Le rapport exporté ne contient aucun mot de passe, jeton ni identifiant : noms, dates, états et compteurs seulement.',
  ],
  rollback: {
    summary: 'Ce rapport ne modifie rien : il lit l’annuaire, le DNS, les services, les partages et les journaux. La seule trace est le fichier exporté.',
    diagnostic: '# Retrouver les rapports produits.\nGet-ChildItem -Path ([Environment]::GetFolderPath(\'Desktop\')) -Filter \'kjemo-ad-rapport-domaine*\' | Sort-Object LastWriteTime -Descending | Format-Table Name,Length,LastWriteTime',
    command: '# Retirer un rapport devenu inutile.\nGet-ChildItem -Path ([Environment]::GetFolderPath(\'Desktop\')) -Filter \'kjemo-ad-rapport-domaine*\' | Remove-Item -Confirm',
    exceptional: '# AVERTISSEMENT CRITIQUE — reconstruction de SYSVOL.\n#\n# Ce script NE RECONSTRUIT JAMAIS SYSVOL, et aucun outil de ce projet ne le\n# fait. La restauration autoritaire ou non autoritaire de SYSVOL est une\n# procedure exceptionnelle, qui se conduit a la main, apres sauvegarde, et en\n# comprenant ce qu\'elle fait :\n#\n#   - Une restauration AUTORITAIRE designe un controleur comme source de\n#     verite : son contenu ecrase celui de tous les autres. Si l\'on se trompe\n#     de controleur, on propage le mauvais contenu a tout le domaine, et les\n#     strategies de groupe des autres controleurs sont perdues.\n#   - Une restauration NON AUTORITAIRE fait l\'inverse : le controleur se\n#     resynchronise depuis ses partenaires, et son contenu local est remplace.\n#   - Dans les deux cas, les strategies de groupe cessent de s\'appliquer\n#     pendant la resynchronisation.\n#\n# Avant d\'y penser :\n#   1. Une sauvegarde de l\'etat du systeme de chaque controleur existe.\n#   2. L\'etat DFSR et les evenements DFS Replication ont ete lus.\n#   3. On sait quel controleur detient le contenu correct.\n#\n# Procedures officielles :\n# https://learn.microsoft.com/troubleshoot/windows-server/group-policy/force-authoritative-non-authoritative-synchronization\n#\n# La marche a suivre est celle de cette page, executee manuellement, controleur\n# par controleur. Aucune commande n\'est proposee ici : ce serait donner une\n# apparence de routine a une operation qui n\'en est pas une.',
    warning: 'Le rapport décrit la topologie complète du domaine, ses rôles FSMO, ses contrôleurs et ses comptes privilégiés. C’est une carte précise de l’infrastructure : elle se range comme un document sensible.',
  },
  checks: [
    'Lire la synthèse d’abord : elle dit s’il faut lire la suite.',
    'Un contrôle marqué « non applicable » signale seulement que la machine ne permettait pas de le faire.',
    'Rien n’est modifié, et SYSVOL n’est jamais reconstruit automatiquement.',
  ],
  source: 'https://learn.microsoft.com/powershell/module/activedirectory/get-addomain',
  sources: [
    { label: 'Get-ADDomain', url: 'https://learn.microsoft.com/powershell/module/activedirectory/get-addomain' },
    { label: 'Get-ADForest', url: 'https://learn.microsoft.com/powershell/module/activedirectory/get-adforest' },
    { label: 'Get-ADReplicationPartnerMetadata', url: 'https://learn.microsoft.com/powershell/module/activedirectory/get-adreplicationpartnermetadata' },
    { label: 'Synchronisation autoritaire et non autoritaire de SYSVOL', url: 'https://learn.microsoft.com/troubleshoot/windows-server/group-policy/force-authoritative-non-authoritative-synchronization' },
    { label: 'Corbeille Active Directory', url: 'https://learn.microsoft.com/windows-server/identity/ad-ds/get-started/adac/introduction-to-active-directory-administrative-center-enhancements--level-100-' },
  ],
};

// ---------------------------------------------------------------------------
// Catalogue exporté — complété au fil des sous-rubriques du LOT 3
// ---------------------------------------------------------------------------
export const toolsAd = [
  outilOuHierarchie,
  outilUtilisateursCsv,
  outilGroupes,
  outilMembresCsv,
  outilCheminsProfils,
  outilSanteComptes,
  outilRecuperationCompte,
  outilRechercheDeplacement,
  outilAuditDelegation,
  outilNettoyageOrdinateurs,
  outilDnsAd,
  outilDcdiag,
  outilRepadmin,
  outilCanalSecurise,
  outilRapportDomaine,
];
